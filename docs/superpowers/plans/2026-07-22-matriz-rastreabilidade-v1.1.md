# Matriz de Rastreabilidade — Protótipo v1.1 → Sistema Real

> **Status:** Vigente · **Data:** 2026-07-22
> Anexo do plano mestre [`2026-07-22-implementacao-completa-prototipo-v1.1.md`](2026-07-22-implementacao-completa-prototipo-v1.1.md).
> Fonte de UI/UX: protótipo `F:\Projetos\alpha-carnes-prototipo`, branch `feature/completude-v1.1`, `src/app/routes.tsx` (confirmada checked out, working tree limpo).
> Fonte funcional: `docs_v2/alphacarnes_contexto_funcional_e_recomendacoes_prototipo_v1.1.md` (v1.1).
> Estado real auditado: branch `feature/absorcao-prototipo-v2` de `F:\Projetos\AlphaCarnes` (`app/backend/`, `app/frontend/`).

## Legenda de classificação

- **Ausente** — não existe no projeto real; precisa ser criado (backend e/ou frontend, conforme coluna Observação).
- **Divergente** — existe, mas implementado sob premissa antiga/incompleta; precisa correção/migração.
- **Conforme** — existe e já reflete o v1.1 (pode ainda exigir alinhamento visual ao protótipo — anotado).

Perfis RBAC usam os slugs canônicos do doc 013 / seed F1: `administrador`, `compras`, `gestor`, `comercial`, `recebimento_pesagem`, `corte`, `expedicao`, `conferente`, `faturamento`, `logistica`, `diretoria`.

## Matriz — 41 entradas de rota do protótipo (39 telas de conteúdo + `/login` + `/` redirect)

| # | Rota (protótipo) | Tela / fluxo | Módulo NestJS | Entidades/tabelas | Endpoints / eventos | Perfis RBAC | Class. | Ref. doc | Observação / provisório |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `/login` | Login (painel de marca + formulário) | `auth` | `usuarios`, `refresh_tokens` | `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`, `GET /auth/me` | todos | **Conforme** | doc 013; ADR-005 | Backend F1 completo; UI real existe — alinhar visual ao protótipo (painel de marca à esquerda) na Onda 2 |
| 2 | `/` (index) | Redirect → `/gestao/dashboard` | — (frontend) | — | — | todos | **Divergente** | protótipo `routes.tsx` | Frontend real redireciona para home admin própria; padronizar destino `/gestao/dashboard` |
| 3 | `/comercial/clientes` | Clientes (master-detail, abas, herança de representante/rota) | `cadastros/clientes` | `clientes`, `representantes`, `rotas` | CRUD `/clientes` (existe) | `comercial`, `gestor`, `administrador`; consulta: `faturamento` | **Divergente** | v1.1 §5.6/§6.8; doc 04 §1.1 | `nomeFantasia` já existe no schema; falta herança automática representante→rota no fluxo de pedido, abas do protótipo (Gerais/Fiscais/Contatos/Preferências ampliadas). Terminologia: nunca "Marca" |
| 4 | `/comercial/pedidos` | Pedidos de Venda (reserva imediata, overbooking c/ confirmação, adendo, vínculo a Operação, estados v1.1 §10.1) | `comercial/pedidos` (+ `operacoes`) | `pedidos_venda`, `pedidos_venda_itens`, `reservas_disponibilidade`, `adendos_pedido` (nova), `pendencias_overbooking` (nova), `operacoes` (nova) | Existentes: `GET/POST /comercial/pedidos`, `PATCH .../itens/:id`, `DELETE /:id`. Novos Onda 1: `POST /comercial/pedidos/confirmar-overbooking`, `POST /:id/itens`, `POST /:id/itens/confirmar-overbooking`, `POST /:id/finalizar`; Onda 4: `POST /:id/adendos`. Eventos `RESERVA_ATUALIZADA` (existe), `OVERBOOKING_CONFIRMADO`, `ADENDO_REGISTRADO`, `PEDIDO_FINALIZADO` (novos) | `comercial`, `gestor`, `administrador`; consulta: `faturamento`, `expedicao` | **Divergente MAIOR** | v1.1 §6.3/§6.4/§6.9/§10.1 | **D1/AD-05**: tentativa deficitária retorna challenge 409 sem mutação; confirmação persiste e não bloqueia finalização. **AD-03:** unicidade do pedido aberto = cliente+produto+operação; adendo completo entra na Onda 4 |
| 5 | `/comercial/tabela-precos` | Tabela de Preços (A/B/C/D, rascunho→publicada, histórico) | `precos` (novo) | `tabelas_preco`, `tabelas_preco_itens`, `tabelas_preco_publicacoes` (novas) | `GET/POST /precos/tabelas`, `POST /tabelas/:id/publicar`, `POST /tabelas/:id/copiar`, `GET /tabelas/:id/historico`; evento `TABELA_PRECO_PUBLICADA` | `gestor`, `administrador`; consulta: `comercial` (interno; externo não vê — protótipo `hiddenItems`) | **Ausente** | doc 04 §1.3; v0.1 02 §2.2 | Preço por kg/unidade depende do campo em `produtos` (lacuna registrada em docs_v2/05 §7) |
| 6 | `/comercial/disponibilidade` | Disponibilidade (mapa "teatro" agrupado por estado F/V/R/C/D/O/E/! + drill-down lista + grade tabular) | `comercial/disponibilidade` | `disponibilidades_virtuais`, `reservas_disponibilidade`, `pecas`, `operacoes`, `pendencias_overbooking` | Existe: `GET /comercial/disponibilidade`. Novos: `GET /comercial/disponibilidade/mapa?operacaoId=`, `GET .../mapa/:itemId/detalhe`; eventos existentes `DISPONIBILIDADE_GERADA`/`RESERVA_ATUALIZADA` | `comercial` (escopo por representante), `gestor`, `diretoria`, `administrador` | **Divergente** | v1.1 §7 (obrigatório); doc 04 §1.4 | Saldo existe (F3); mapa visual por estado, composição do saldo e drill-down ausentes. **Usar catálogo MVP correto** (TZ/DT/PA/Banda de Porco/derivados TZ/caixarias) — a aba "Grade Tabular" do protótipo usa catálogo legado (Dianteiro/Traseiro/Central/Cupim/Picanha): NÃO copiar |
| 7 | `/comercial/espelho` | Espelho Comercial (pedido×atendimento, agrupamentos, exportar) | `comercial/espelho` (novo submódulo) | `pedidos_venda` + `clientes` + `rotas` + `representantes` (consulta) | `GET /comercial/espelho?agrupar=cliente|rota|representante`; export CSV | `comercial` (escopo), `gestor`, `expedicao` (consulta), `administrador` | **Ausente** | doc 04 §1.5 | Somente leitura/agregação — sem tabela nova |
| 8 | `/gestao/dashboard` | Painel Geral da Operação (10 KPIs + pedidos em andamento + alertas + atividades) | `gestao/dashboard` (+ `operacoes`) | agregações; `operacoes` | Existe `GET /gestao/dashboard?dataOperacao=`; migrar para `?operacaoId=` + KPIs do protótipo (compras, disponibilidade, reservas, overbookings, recebimentos, divergências, desossa, SIF, faturamentos) | `gestor`, `diretoria`, `administrador`; recortes p/ demais | **Divergente** | v1.1 §8.1 | Resumo existe; KPIs por Operação e feed de alertas/atividades incompletos |
| 9 | `/gestao/operacoes` | Operações (cadência configurável, extraordinárias, status Aberta/Em andamento/Fechada) | `operacoes` (novo módulo) | `operacoes` (nova) | `GET/POST /operacoes`, `POST /operacoes/extraordinaria`, `PATCH /:id/status`, `POST /operacoes/gerar-cadencia`; evento `OPERACAO_CRIADA/FECHADA` | `gestor`, `compras`, `administrador`; consulta geral | **Ausente ESTRUTURAL** | v1.1 §7.2/§16.2; protótipo `data/operacoes.ts` | **D2** — pivô de Compra/Pedido/Disponibilidade/SIF/Dashboard. Cadência seg/qua/sex é **provisória** (§16.2) → parâmetro |
| 10 | `/gestao/compras` | Compras / Compra Programada (vinculada a Operação, desdobramento Boi Casado, edição com painel de impacto) | `comercial/compras-programadas` (+ `operacoes`) | `compras_programadas`, `compras_programadas_itens`, `operacoes`, `regras_desdobramento_comercial` | Existentes: CRUD + `/confirmar`. Novos: `GET /:id/impacto` (painel de impacto pré-salvamento), vínculo `operacaoId`; eventos `COMPRA_CONFIRMADA` (existe), `COMPRA_ALTERADA_IMPACTO` (novo) | `compras`, `gestor`, `administrador`; consulta: `comercial` | **Divergente** | v1.1 §6.1/§8.3; doc 04 §2.2 | **Composição do boi casado CONFIRMADA pelo cliente (AD-01): 1 boi = 2 TZ + 2 DT + 2 PA** — permanece parametrizável (regra de desdobramento), mas o valor padrão está validado; badge "Provisório" desta composição sai das telas |
| 11 | `/gestao/overbooking` | Pendências de Overbooking (fontes de cobertura, decisão, histórico) | `comercial/overbooking` (novo submódulo) | `pendencias_overbooking`, `pendencias_overbooking_historico` (novas) | `GET /comercial/overbooking`, `PATCH /:id/status`, `POST /:id/decisao` (compra complementar / redistribuição / novo pedido), `GET /:id/historico`; eventos `PENDENCIA_OVERBOOKING_ABERTA/RESOLVIDA` | `gestor` (dono do fluxo — "Fabrício"), `administrador`; consulta: `comercial`, `compras` | **Ausente** | v1.1 §6.4/§8.5 | Estados: aberto, em análise, compra complementar programada, redistribuição decidida, novo pedido criado, resolvido, cancelado |
| 12 | `/gestao/aprovacoes` | Aprovações & Ocorrências (fila administrativa Pedido×NF×Pesagem imutável + aprovações operacionais) | `operacao/recebimento/ocorrencia` (existe) + `gestao/aprovacoes` (novo agregador) | `ocorrencias_fornecedor`(+histórico — existem), `conclusoes_conferencia`, `conclusoes_conferencia_nfs` (novas), `aprovacoes_operacionais` (nova: estorno fora da regra, reabertura, ajuste relevante) | Existentes: `GET/POST/PATCH /operacao/ocorrencias-fornecedor`, `/encerrar`. Novos: `GET /gestao/aprovacoes` (fila unificada), `POST /aprovacoes/:id/decidir` | `gestor`, `administrador`; registro: `recebimento_pesagem`; consulta: `diretoria` | **Divergente** | v1.1 §6.10.6/§6.10.7/§8.6; doc 04 §2.3 | Ocorrência F4a existe; comparativo tripla imutável e fila unificada ausentes. Tratativa NUNCA altera totais históricos |
| 13 | `/gestao/relatorios` | Relatórios & SIF (lista, status, geração, versionamento, retificação) | `sif` (novo módulo) | `relatorios_sif`, `relatorios_sif_versoes` (novas) | `GET /sif/relatorios`, `POST /:id/gerar`, `POST /:id/retificar`, `GET /:id/versoes`, `GET /:id/preview` | `gestor`, `faturamento`, `administrador`; consulta: `diretoria` | **Ausente** | v1.1 §6.16/§8.10/§16.10 | **Provisório**: modelos oficiais SIF pendentes (§16.10) — nomes/layouts provisórios sinalizados, configuráveis sem redesenho |
| 14 | `/recebimento/recebimento-carga` | Recebimento de Carga (nasce do Pedido ao Fornecedor, NF complementar, conferência Pedido×NF×Pesagem, estados do lote, ocorrências) | `operacao/recebimento` | `recebimentos`, `recebimentos_itens`, `divergencias_recebimento` (existem) + `pedidos_fornecedor`, `pedidos_fornecedor_itens`, `notas_fiscais_fornecedor`, `notas_fiscais_fornecedor_itens`, `conclusoes_conferencia`, `conclusoes_conferencia_nfs` (novas) | Existentes: fluxo completo F4a + `PATCH /:id/nfe`. Novos: `GET /operacao/recebimentos/:id/conferencia` (acumuladores tripla em tempo real), `POST /:id/concluir-pesagem` (abre revisão obrigatória), `POST /:id/conferencia/concluir` (sem/com divergência); eventos F4a existentes + `CONFERENCIA_TRIPLA_CONCLUIDA` | `recebimento_pesagem`, `gestor`, `administrador`; consulta: `compras`, `faturamento` | **Divergente** | v1.1 §6.10 (todo), §10 estados | **D3** — hoje nasce de `compras_programadas`; v1.1 exige Pedido ao Fornecedor formal + acumuladores por produto + revisão final obrigatória + estados §6.10.5. Pendentes §16.8/§16.9: N caminhões/NFs por pedido e N pedidos por caminhão |
| 15 | `/recebimento/pesagem-destinacao` | Pesagem e Destinação (origem do peso, características, acumulado, associação pedido/estoque/desossa, Troca de Peça, estorno) | `operacao/pesagem` | `pecas`, `associacoes_peca_historico`, `etiquetas_impressoes` (existem) + `trocas_peca` (nova); características em `pecas.caracteristicasJson` | Existentes: fluxo F4b completo. Novos: `POST /operacao/pesagem/trocas` (atômica), `GET /recebimentos/:id/acumuladores`, `POST /pecas/:id/estornar`; eventos F4b + `PECA_TROCADA`, `PESAGEM_ESTORNADA` | `recebimento_pesagem`, `gestor`, `administrador` | **Divergente** | v1.1 §6.11/§6.12/§6.13/§8.7/§8.8 | **D5**: resíduo "Buscar cliente / marca…" (`pesagem-destinacao-client.tsx:714`) → "Buscar cliente". Troca de Peça (função atômica §6.13) ausente. Características (mais pesada/gorda/acabamento) ausentes |
| 16 | `/recebimento/etiquetas` | Etiquetas do recebimento (5 estados: Ativa/Reimpressa/Cancelada/Pendente/Bloqueada, histórico) | `operacao/pesagem/etiqueta` | `etiquetas_impressoes` (ampliar estados) | Existentes: emissão/reimpressão. Novos: `GET /operacao/etiquetas?filtros`, `POST /etiquetas/:id/cancelar`; evento `ETIQUETA_INVALIDADA` | `recebimento_pesagem`, `gestor`, `administrador` | **Divergente** | v1.1 §6.15/§10.4 | Backend parcial (F4b); UI real é placeholder. Estados v1.1 §10.4: emitida, ativa, invalidada por troca, reimpressa, cancelada. Pendente §16.12: campos finais da etiqueta |
| 17 | `/desossa/dashboard` | Dashboard da Desossa (painel aeroporto/Modo TV, faltas por produto, sugestão por regra) | `operacao/desossa` | cálculo sobre `pedidos_venda_itens` + `regras_transformacao` + estoque (existe `faltas.calc`) | Existe `GET /desossa/faltas`. Novos: `GET /desossa/painel?modoTv=`, sugestão por regra; evento `FALTAS_DESOSSA_ATUALIZADAS` (broadcast p/ telão) | `corte`, `gestor`, `administrador`; consulta: `comercial` | **Divergente** | v0.1 02 §5; docs_v2 01 §5.2 | Cálculo de faltas existe; painel aeroporto/Modo TV e visão por regra sugerida ausentes. Nunca exibir "em produção"/travas |
| 18 | `/desossa/pesagem-destinacao` | Pesagem e Destinação da desossa (seleção de TZ, regra com exclusividade, checklist de saídas esperadas, divergência de transformação, etiqueta com peça mãe) | `operacao/corte` + `operacao/desossa` | `transformacoes`, `subitens`, `regras_transformacao`(+saidas) (existem) + `divergencias_transformacao` (nova) | Existentes: fluxo F4c completo (iniciar/subitens/pesar/associar/concluir). Novos: `POST /corte/:id/regra` (trava exclusividade), `GET /corte/:id/checklist` (esperado×registrado), `POST /corte/:id/divergencia`; eventos F4c + `DIVERGENCIA_TRANSFORMACAO_ABERTA` | `corte`, `gestor`, `administrador` | **Divergente** | v1.1 §6.6/§6.14/§8.9 | **D6/regra provisória §16**: exatamente 2 alternativas (A: Coxão-bola+Jacaré; B: Coxão-bola c/ alcatra+Filé curto) com **exclusividade por unidade de TZ**; parametrizáveis, badge "Provisório". Momento da escolha da regra: pendente §16.7 (entrada ou saída — tornar obrigatória antes de gerar produtos) |
| 19 | `/desossa/etiquetas` | Etiquetas da desossa (estado "invalidada por troca", peça mãe) | `operacao/pesagem/etiqueta` | `etiquetas_impressoes` (subitens — existe) + estado invalidada por troca | reusa 16 + filtro por transformação; etiqueta referencia peça mãe (F4c já referencia) | `corte`, `gestor`, `administrador` | **Divergente** | v1.1 §6.13/§10.4 | UI placeholder; estado "invalidada por troca" depende de `trocas_peca` (linha 15) |
| 20 | `/estoque/consulta` | Consulta de Estoque (FIFO, destinar a pedido, filtros, histórico) | `operacao/estoque` | consulta agregada `pecas`+`subitens` (existe) + `entradas_itens` (nova) | Existe `GET /estoque/consulta`. Novos: `POST /estoque/:itemId/destinar` (a pedido, respeitando FIFO), `GET /estoque/:itemId/historico` | `expedicao` e `recebimento_pesagem` com recorte `ESTOQUE_*` (AD-04), `gestor`, `administrador` | **Divergente** | doc 04 §5.1; v1.1 §16.4 | Consulta existe; destinar-a-pedido e ordenação FIFO ausentes. **Pendente §16.4**: ordem detalhada de consumo FIFO entre peças físicas — parametrizável |
| 21 | `/estoque/entrada-itens` | Entrada de Itens (caixarias, direto a estoque ou pedido) | `operacao/estoque` | `entradas_itens` (nova) | `POST /estoque/entradas`, `GET /estoque/entradas`; evento `ENTRADA_ITENS_REGISTRADA` | estoque/`expedicao`, `gestor`, `administrador` | **Ausente** | doc 04 §5.2; v1.1 §4.1 caixarias | Caixas: por unidade, sem balança/desossa |
| 22 | `/estoque/ajustes` | Ajustes de Estoque (aprovação por limiar, motivos, histórico) | `operacao/estoque` | `ajustes_estoque` (nova) | `POST /estoque/ajustes`, `POST /ajustes/:id/aprovar`, `POST /ajustes/:id/rejeitar`; evento `AJUSTE_ESTOQUE_*` | cria: estoque; aprova: `gestor`/`administrador` (segregação SF) | **Ausente** | doc 04 §5.3 | Limiar de aprovação = parâmetro do sistema |
| 23 | `/carga/planejamento` | Planejamento de Carga (alocação de pedidos a caminhões, prioridade, rota) | `operacao/expedicao` | `caminhoes`, `caminhoes_pedidos`, `carga_itens` (existem F5) + FK cadastro de caminhões | Existentes: fluxo F5 (criar caminhão/abrir carga/vincular pedido/adicionar item). Alinhar UI/fluxo ao protótipo (status Montando/Pronto p/ conferência) | `expedicao`, `gestor`, `administrador` | **Divergente** | doc 04 §6.1 | Backend F5 sólido; UI real existe mas deve ficar fiel ao protótipo; reconciliar `caminhoes` operacionais F5 com cadastro (linha 33) |
| 24 | `/carga/conferencia` | Conferência (bipagem por etiqueta/QR, marco de fechamento) | `operacao/expedicao` | `conferencias_carga` (existe) | Existentes: iniciar/registrar-item/concluir + fechar/reabrir (F5, ADR-009) | `conferente`, `expedicao`, `gestor`, `administrador` | **Conforme** (backend) / **Divergente** (UI) | doc 04 §6.2; quality-gates F5 | Fechamento congela mutações (provado por teste F5); alinhar UI de bipagem ao protótipo |
| 25 | `/carga/enviar-faturamento` | Enviar para Faturamento (transição da carga conferida) | `operacao/expedicao` | `caminhoes.status` (existe: `liberado_faturamento`) | Existe `POST /caminhoes/:id/liberar-faturamento`; UI ausente (placeholder) | `expedicao`, `gestor`, `administrador` | **Divergente** | roadmap F5→F6 (DP-05) | Backend pronto; construir UI fiel (PipelineBar do protótipo) |
| 26 | `/faturamento/pre-faturamento` | Pré-Faturamento (consolidação, integração fiscal externa, reprocesso) | `operacao/faturamento` | `faturamentos`, `notas_fiscais` (existem) | Existentes: consolidação/emitir/cancelar/reprocessar via porta `NfseGateway` + fake (ADR-011). Novo: **implementar o adapter SOAP real do EISS** (node-soap, hoje stub que lança `NfseTransporteError`) + homologação | `faturamento`, `gestor`, `administrador` | **Divergente** | v1.1 §6.17/§8.11; `docs/integrações/nfse-osasco/` | **D4 RESOLVIDO por AD-02**: sistema fiscal externo confirmado = **EISS Osasco** (SOAP, doc completo em `docs/integrações/nfse-osasco/` — webservice, XML, homologação, códigos de erro). Pendência §16.11 fechada. Modelagem acoplada ao EISS está correta; gap = adapter real + credenciais de homologação + feature flag RTC (Reforma Tributária, métodos `RTC_*` do WSDL) |
| 27 | `/faturamento/notas-xml` | Notas/XML (vínculo pedido↔peças↔pesos↔item fiscal, cancelamento com trava pós-liberação) | `operacao/faturamento` | `notas_fiscais` + vínculo rastreável (existe parcial) | Existentes: cancelar/reprocessar. Novos: `GET /faturamento/notas?filtros`, `GET /notas/:id/rastreabilidade` (pedido→peças→pesos→item fiscal), download XML/DANFE quando o sistema externo fornecer | `faturamento`, `gestor`, `administrador`; consulta: `logistica`, `diretoria` | **Divergente** | v1.1 §6.17/§8.11 | UI placeholder; trava "não cancelar após caminhão liberado" a implementar (protótipo modela) |
| 28 | `/faturamento/seguro-manual` | Seguro Manual (status, anexos, notas vinculadas) | `operacao/faturamento` (F6b) | `seguros_carga` (nova) | `GET/POST /faturamento/seguros`, `PATCH /:id/status` (pendente→enviado→confirmado), anexos; evento `SEGURO_ATUALIZADO` | `faturamento`, `logistica`, `gestor`, `administrador` | **Ausente** | doc 04 §7.3; quality-gates F6b; v0.1 §16.5 fluxo pendente | F6b nunca implementada. Fluxo operacional do seguro pendente (docs_v2/05 §3.5) — campos flexíveis |
| 29 | `/faturamento/liberacao` | Liberação do Caminhão (checklist calculado: carga conferida + NF autorizada + seguro + docs) | `operacao/expedicao` + `operacao/faturamento` (F6b) | `caminhoes.status` (existe `liberado_saida`) + checklist calculado | Existe `POST /caminhoes/:id/liberar-saida`. Novo: `GET /faturamento/liberacao/:caminhaoId/checklist` (cada requisito com status); bloqueio por requisito faltante | `logistica`, `faturamento`, `gestor`, `administrador` | **Divergente** | doc 04 §7.4; quality-gates F6b (DP-06) | Transição existe; checklist calculado com seguro/NF/conferência/docs ausente (depende de 28) |
| 30 | `/cadastros/representantes` | Representantes (canal, vínculos a clientes/usuários) | `cadastros/representantes` | `representantes` (existe F7) | CRUD existe | `administrador`, `gestor` | **Divergente** | doc 04 §8.1 | Backend pronto; UI real é placeholder — construir fiel ao protótipo |
| 31 | `/cadastros/produtos` | Produtos (catálogo MVP, 8 abas, fiscal NCM/CFOP/CST/CEST, 4 tipos operacionais) | `cadastros/produtos` | `produtos` (existe F7) — ampliar campos fiscais + preço kg/unidade | CRUD existe; ampliar DTO/schema (dados fiscais, `unidadePreco` já existe — validar contra protótipo) | `administrador`, `gestor` | **Divergente** | doc 04 §8.2; v1.1 §6.5/§16.14 | Lacuna registrada (docs_v2/05 §7): preço por kg/unidade. **Pendente §16.14**: catálogo oficial completo — seed com catálogo MVP marcado provisório |
| 32 | `/cadastros/fornecedores` | Fornecedores/Frigoríficos (nota de qualidade, tolerância, ocorrências/ano) | `cadastros/fornecedores` | `fornecedores` (existe) — ampliar qualidade/tolerância | CRUD existe; ampliar campos | `administrador`, `gestor`, `compras` | **Divergente** | doc 04 §8.3 | UI real existe (abas JSON tipado); campos de qualidade do protótipo ausentes |
| 33 | `/cadastros/caminhoes` | Caminhões (placa, capacidade, rota padrão) | `cadastros/caminhoes` (novo CRUD) | `caminhoes_cadastro` (nova) — reconciliar com `caminhoes` operacional F5 (viagem/carga do dia referencia o cadastro) | CRUD novo; `operacao/expedicao` passa a referenciar `caminhao_cadastro_id` | `administrador`, `gestor`, `expedicao` | **Ausente** | doc 04 §8.4 | Decisão de modelagem no plano mestre: cadastro (frota) ≠ caminhão-do-dia (entidade F5) |
| 34 | `/cadastros/motoristas` | Motoristas (documento, telefone, caminhão padrão) | `cadastros/motoristas` (novo) | `motoristas` (nova) | CRUD novo | `administrador`, `gestor`, `expedicao` | **Ausente** | doc 04 §8.5 | — |
| 35 | `/cadastros/rotas` | Rotas/Itinerários (paradas, vínculos padrão) | `cadastros/rotas` | `rotas` (existe F7) — ampliar paradas/sequência | CRUD existe; ampliar | `administrador`, `gestor` | **Divergente** | doc 04 §8.6 | UI real existe; paradas/sequência de clientes do protótipo ausentes |
| 36 | `/cadastros/regras-transformacao` | Regras de Transformação (2 abas: desdobramento de compra + transformação TZ, simuladores funcionais) | `cadastros/regras-desdobramento` + `operacao/desossa` | `regras_desdobramento_comercial` (existe), `regras_transformacao`(+saidas) (existe) | CRUDs existem. Novos: `POST /simular-desdobramento`, `POST /simular-transformacao`; join de nomes de itens na listagem (lacuna docs_v2/05 §7) | `administrador`, `gestor` | **Divergente** | v1.1 §6.6/§17.3; doc 04 §8.7 | **D6**: estrutura conforme, dados vazios (sem seed), criação/edição via UI incompleta, simuladores ausentes. Seed: 2 regras provisórias A/B + desdobramento boi casado **confirmado (AD-01: 2 TZ + 2 DT + 2 PA)** |
| 37 | `/cadastros/modelos-etiqueta` | Modelos de Etiqueta (12 campos configuráveis, preview ao vivo) | `cadastros/modelos-etiqueta` (novo) | `modelos_etiqueta` (nova; campos em JSONB) | CRUD novo + `GET /:id/preview`; impressão consome modelo ativo por tipo | `administrador`, `gestor` | **Ausente** | doc 04 §8.8; v1.1 §6.15/§16.12 | **Pendente §16.12**: campos finais da etiqueta — por isso configurável, com preview |
| 38 | `/admin/usuarios` | Usuários (perfil, representantes permitidos, status) | `usuarios` | `usuarios`, `usuarios_perfis` (existem) | CRUD + aprovar (SF-01) existem; adicionar "representantes permitidos" (escopo comercial) | `administrador` | **Divergente** | doc 013; docs_v2 03 §5.1 | Backend F1 sólido; falta escopo por representante no usuário (dimensão do protótipo/docs_v2 03 §1.2) |
| 39 | `/admin/perfis` | Perfis de Acesso (11 perfis, permissões nomeadas) | `perfis` | `perfis`, `permissoes`, `perfis_permissoes` (existem) | `GET /perfis`, `PUT /:slug/permissoes` existem; UI placeholder | `administrador` | **Divergente** | doc 013 (11 perfis); ADR-007/008 | UI real placeholder; protótipo simula 9 → matriz final segue os **11 do doc 013** (reconciliação no plano mestre §5) |
| 40 | `/admin/parametros` | Parâmetros (cadência de Operação, overbooking, FIFO, itens provisórios) | `parametros` | `parametros` (existe, chave/valor JSONB) | CRUD existe; definir catálogo de parâmetros v1.1 | `administrador` | **Divergente** | v1.1 §13.6/§16; doc 04 §9.3 | UI placeholder. **AD-05:** rótulo definitivo “Exigir confirmação de overbooking: sim”; não existe opção de bloquear venda confirmada. **AD-06:** não existe TTL de reserva; liberação é explícita |
| 41 | `/admin/auditoria` | Auditoria (filtros, valor anterior/novo, justificativa) | `auditoria` | tabela de auditoria (existe) | `GET /auditoria` existe | `administrador`, `diretoria`, `gestor` | **Conforme** | v1.1 §12; doc 04 §9.4 | Backend + UI reais; alinhar filtros/visual ao protótipo na Onda 2 |

## Mecânicas transversais (obrigatórias — não são telas)

| Mecânica | Módulo | Entidades | Classificação | Ref. | Observação |
|---|---|---|---|---|---|
| Entidade **Operação** como pivô (Compra/Pedido/Disponibilidade/SIF/Dashboard) | `operacoes` | `operacoes` + FK `operacao_id` nas tabelas de fato (backfill de `dataOperacao`) | **Ausente ESTRUTURAL (D2)** | v1.1 §7.2 | Onda 1 |
| **Troca de Peça** atômica (preserva pesos, invalida/reemite etiqueta, histórico) | `operacao/pesagem` | `trocas_peca` | **Ausente** | v1.1 §6.13/§8.8/§9.5 | Transação única; componente `TrocaPeca` do protótipo |
| Padrão de sinalização **"Provisório"** (badge âmbar + title com referência ao doc) | frontend (DS) | — | **Ausente** (DS v2 não tem o badge) | protótipo (13+ usos); v1.1 §18.21 | Componente compartilhado na Onda 2; remoção do badge exige decisão registrada (ex.: AD-01 remove o badge da composição do boi) |
| **Terminologia**: "Nome Fantasia"/"Razão Social" no cadastro; "Buscar cliente" na busca; nunca "Marca" | todos | — | **Divergente (D5)** | v1.1 §1.3/§6.8 | Varredura completa na Onda 1 + regra de gate (constituição) |
| Consumo automático físico → virtual → overbooking | `comercial/pedidos` | `reservas_disponibilidade` | **Divergente (D1)** | v1.1 §6.2 | Prioridade automática, sem escolha manual do vendedor |
| Auditoria de eventos obrigatórios v1.1 §12 | `common/auditoria` | auditoria (existe) | **Conforme** (mecanismo) / **Divergente** (cobertura dos novos eventos) | v1.1 §12 | Cada onda adiciona seus eventos auditáveis |

## Checagem de fechamento

Percorridas **todas** as entradas de `src/app/routes.tsx` do protótipo (`feature/completude-v1.1`), na ordem declarada no arquivo:

- `/login` → linha 1 · `/` (index redirect → `/gestao/dashboard`) → linha 2 · **39 rotas de conteúdo** → linhas 3–41 (os 9 grupos de menu somam 5+6+3+3+3+3+4+8+4 = 39 telas).
- **Total: 41/41 entradas de rota cobertas (100%).** Nenhuma rota do protótipo ficou sem linha na matriz; nenhuma usa `PlaceholderPage`.
- Itens do checklist mestre da missão sem rota própria (mecânicas transversais) cobertos na tabela dedicada acima.

### Distribuição da classificação (41 entradas + 6 transversais)

| Classificação | Entradas de rota | Transversais |
|---|---|---|
| Conforme | 3 (login; conferência — backend; auditoria) | 1 parcial (auditoria-mecanismo) |
| Divergente | 27 (26 telas + o redirect da linha 2) | 3 |
| Ausente | 11 (tabela-precos, espelho, operacoes, overbooking, relatorios-sif, entrada-itens, ajustes, seguro-manual, caminhoes, motoristas, modelos-etiqueta) | 2 |

> Nota: a granularidade backend/frontend está na coluna Observação — várias linhas são "Divergente" por terem backend conforme e UI placeholder, ou vice-versa.
