# Execução — Status por Onda

> **Escritor único: Executor.** Vocabulário de status: `aguardando_inicio → planejando → aguardando_portao1 → plano_aprovado → implementando → aguardando_portao2 → mergeada` | `bloqueada`.
> Roadmap e grafo de dependências: [`roadmap-canonico.md`](../governance/roadmap-canonico.md) §8. Rito: [`pipeline-execucao.md`](../governance/pipeline-execucao.md).

| Onda | Escopo resumido | Depende de | Status | Plano tático | PR | SHA merge | Observações |
|---|---|---|---|---|---|---|---|
| 0 | Pipeline de governança (constituição, gates, skills, workflows, estado vivo) | — | mergeada | (plano mestre §6) | [#10](https://github.com/sammuka/alpha-carnes/pull/10) | `0f8491ff2141473b5b7e3cf784b9c878fce35549` | CI completo no head `72e2ac961b59e47ae1737b2d287f2134df53a7e4`; dois Monitores independentes aprovaram o mesmo objeto |
| 1 | Correção estrutural: Operação (D2), overbooking v1.1 (D1), Pedido ao Fornecedor + conferência tripla (D3), terminologia (D5) | 0 | mergeada | [`2026-07-22-onda1-correcao-estrutural.md`](../superpowers/plans/2026-07-22-onda1-correcao-estrutural.md) | [#13](https://github.com/sammuka/alpha-carnes/pull/13) | `231f6843d29296d0b87ff69f1246cb54b2a6bc7e` | Portão 2 aprovado no head `22d3f51` (veredito `3c220de`); CI verde no head do veredito (run `30134001208`). Plano sha256 `2d52f9df…`. AD-07/AD-08. Plano tático integrado antes pelo PR [#12](https://github.com/sammuka/alpha-carnes/pull/12); D9 pelo PR [#11](https://github.com/sammuka/alpha-carnes/pull/11) |
| 2 | Shell + DS fiel ao protótipo (layout, menu 9 grupos, tokens, componentes compartilhados) | 1 | mergeada | [`2026-07-25-onda2-shell-ds.md`](../superpowers/plans/2026-07-25-onda2-shell-ds.md) | [#16](https://github.com/sammuka/alpha-carnes/pull/16) | `18f98bbb69647b405894da7b3d6eff1743c544d7` | Portão 2 aprovado (veredito `8106d5d`); CI verde no head pré-merge `a9b5d2b` (run `30156283019`). Plano sha256 `9a9f8b36…` (Portão 1 `061a2d5`, plano via PR [#15](https://github.com/sammuka/alpha-carnes/pull/15)). Shell 9×39, tokens DS, RBAC real, login JWT fiel; evidências reais do protótipo |
| 3 | Cadastros & Regras completos + Admin | 2 | aguardando_portao1 | [`2026-07-25-onda3-cadastros-admin.md`](../superpowers/plans/2026-07-25-onda3-cadastros-admin.md) | [#20](https://github.com/sammuka/alpha-carnes/pull/20) | — | **Emenda pós-Portão 1 recheck3 (veredito `e5acc67`):** Task 9.7 — `parametros-onda3.e2e-spec.ts` e `auditoria-facetas.e2e-spec.ts` passam a incluir preâmbulo completo autossuficiente (imports, `describe`, `beforeAll`/`afterAll`, `const srv`) espelhando Task 4.9; Task 10.3 — `criarServiceCom` pinado literalmente em `simulador-desdobramento.spec.ts` (stub `db` com chain `select→from→innerJoin→where→orderBy`) e `simulador-desossa.spec.ts` (`jest.spyOn` em `listarAtivasComSaidas`); achados de rótulo/Task 7 já fechados em recheck anterior; 27 tasks, 46 decisões, 86 invariantes DoD, **15** divergências autorizadas; sha256 pin anterior `f3eac263…` — **será recalculado após o commit**. **Emenda pós-Portão 1 recheck2 (veredito `e618498`):** Tasks 7.3 e 9.8 passam a usar `request(app.getHttpServer())` nos blocos que editam `cadastros-diversos.e2e-spec.ts` e `rbac.e2e-spec.ts` — zero `request(srv())` nesses arquivos existentes; padrão alinhado ao repo real. **Emenda pós-Portão 1 Task 7 (veredito `177bb9e`):** fecha os 3 achados do Monitor — (1) **bloqueante Princípio I** — backend continua devolvendo slug em `ultimaDivergencia.tipo`; rótulo humano via mapa frontend pinado `ROTULOS_TIPO_DIVERGENCIA` (`peso_divergente` → "Falta de Peso"); Decisão 18 emendada; (2) fallback `descricao` com `truncate max-w-[220px]` + `title` no JSX literal da Task 16.3; (3) DoD-24/Task 7.3 estendido com insert Drizzle de ocorrência sem `divergenciaId` + teste Jest do mapa (Task 16.2.6); divergência **D16.b** declarada (data `toLocaleDateString` vs "Há N dias" do protótipo). Join/coalesce mantidos; zero `ocorrencias_fornecedor.tipo`. Branch `feature/onda3-plano-fix-ocorrencia-tipo` (worktree `.worktrees/onda3-plan`); sha256 pin anterior `f3eac263…` — **será recalculado após o commit**. **Emenda pós-bloqueio Worker (Task 7):** plano citava coluna inexistente `ocorrencias_fornecedor.tipo`; emenda anterior alinha Decisão 18 e Task 7.1 — `ultimaDivergencia.tipo` via `LEFT JOIN divergencias_recebimento.tipo` com fallback `ocorrencias_fornecedor.descricao`. Worker Tasks 1–6 concluídas em `feature/onda3-cadastros-admin` @ `051c1a8` (Task 7 revertida localmente). **Emenda pós-bloqueio Worker (Task 1):** `npm run db:migrate` falhou com `SQLSTATE 0A000` — PostgreSQL 18 não permite subquery em CHECK; emenda remove `chk_modelos_etiqueta_campos` do SQL 0015 e do schema Drizzle (Tasks 1.5/1.7), realinha Decisão 16, R2, DoD-20 e dívida 3 para validação Zod-only; branch `feature/onda3-plano-fix-check-pg18` (worktree `.worktrees/onda3-plan`); sha256 pin anterior `99a733b6…` — **será recalculado após o commit**. Dependência Onda 2 `mergeada`. Plano tático revisado após Portão 1 recheck5 `ajustar` (veredito `33752de`): PR anterior [#18](https://github.com/sammuka/alpha-carnes/pull/18) (branch `feature/onda3-plano-cadastros-admin`), 27 tasks, 46 decisões, 86 invariantes DoD, **15** divergências autorizadas (sha256 anteriores `885a94e9…`, `b774c48c…`, `2aa62549…`, `8e50240c…` e `9cf09176…`). Esta revisão fecha as 2 correções do recheck5, ambas concentradas na decisão 46 e na Task 12.1, sem tocar arquitetura, matriz, migration, mapa DoD nem telas já auditadas: (1) **bloqueante** — a Task 12.1 mandava declarar os tokens no bloco `:root`, que **não existe** no `app/frontend/src/app/globals.css` real (todos os tokens vivem dentro de `@theme`, linhas 3–134); em Tailwind 4 a diferença é funcional, só o `@theme` gera classe utilitária, então pela letra antiga o Worker entregaria as telas **sem a cor do protótipo** (Princípio I) e com o CI verde, porque o inventário de `tokens-ds.test.ts` faz `includes` cego ao bloco e nenhum `.tsx` escreve hex — o passo passa a dizer `@theme`, explica a consequência, e o teste passa a recortar o trecho entre `@theme {` e o `}` de fecho antes de conferir cada declaração, para o defeito não poder voltar; (2) o inventário deixa de creditar à Onda 3 quatro tokens que já são da Onda 2, com o hex idêntico (`--color-success-strong` `#15803D`, `--color-success-surface` `#F0FDF4`, `--color-danger-surface` `#FFF1F2` e `--color-violet-surface` `#F5F3FF`, linhas 112–117 do `globals.css`, bloco "Sinalizações densas") — as contagens da decisão 46 vão de "14 já têm token / 18 não têm / 19 novos" para **18 já têm token / 14 não têm / 15 tokens novos**, os quatro saem da tabela e do bloco CSS da 12.1 e passam à lista das reusadas por valor exato (as telas continuam usando `bg-success-surface`, `text-success-strong` e `bg-danger-surface` por reuso, sem redeclaração), e `violet-surface` fica fora do inventário também por não ter uso algum — o par de cores do *chip* de canal de `Representantes.tsx:71` depende do vocabulário fixo "Interno"/"Representante" que a decisão 44 recusa. Registrada de propósito a justificativa de `--color-brand-navy-deep` (`#1E3A5F`): é nome novo da família `brand-navy-*` da paleta canônica, não redeclaração de `--color-sidebar-gradient-start` (parada de gradiente da sidebar), no mesmo precedente da Onda 2 que dá três nomes semânticos ao `#265389`. Revisão anterior (`fbfcccf`) fechou as 4 correções do recheck4: (1) os chips do master de Fornecedores voltam à paleta do protótipo (`Fornecedores.tsx:74-76`) — chip ativo preenchido escuro (`bg-login-panel text-white`) e os outros dois em outline cinza (`text-login-text`), com a única paleta Tailwind crua do plano (`border-green-200 bg-green-50 text-green-700`) eliminada e a fidelidade asserida em DoD-40; nenhum token novo, porque `#1F2633` e `#6B7081` já são `login-panel`/`login-text` da Onda 2 (decisão 19); (2) nova seção "Reconciliação com o plano mestre §3–§4" com três itens numerados — **R1** expõe o rename `caminhoes_cadastro`/`motoristas` → `frota_caminhoes`/`frota_motoristas` campo a campo (a FK `caminhoes.caminhao_cadastro_id` mantém o nome do mestre), **R2** declara que `slug` **é** o `tipo` do mestre com tabela de equivalência dos 6 valores e justifica `campos` sem sufixo `_json` (conjunto fechado, como `paradas`/`dias_atendimento`), **R3** encaminha `GET /modelos-etiqueta/:id/preview` para a divergência numerada nova **D18.c** (preview ao vivo é render dos 12 booleanos no cliente; o layout físico nasce na Onda 6, junto do gateway de impressora); (3) dívidas apontando a onda certa do roadmap §8 — frota × expedição passa da Onda 7 para a **Onda 9 (Carga)** (decisão 13 e dívida 1) e a geração por cadência passa da Onda 4 para a **Onda 5 (Gestão)**, dona de `/gestao/operacoes` (nova dívida 8); (4) DoD-38 e DoD-81 deixam de compartilhar nome de teste — DoD-38 fica com `clique na linha abre o drawer em edicao com os dados da linha` e DoD-81 ganha teste próprio `drawer respeita a largura de 460 e 520 px por tela` (a asserção de largura foi preservada e o arquivo passa de 6 para 7 testes; a cláusula "célula de ações não abre" foi consolidada em DoD-80, que já a testa) — mapa DoD → teste verificado 1:1, 86 nomes distintos. Revisão anterior (`140b533`) fechou as 10 correções do recheck3, todas relidas contra o `.tsx` do protótipo: (1) cabeçalhos de `/admin/perfis` e `/admin/parametros` com trilha "Administração / …" em `text-[11px]`, `h1` `text-[20px]` e subtítulo `text-[12px]` (decisão 29), e a matriz de perfis reescrita com perfil por linha e permissão por coluna (`PerfisAcesso.tsx`); (2) Task 20 com os rótulos literais "Transformação de Desossa (TZ)", "Simulador de Disponibilidade", "Quantidade de TZ livre", "Reservar produto" e "Quantidade a reservar" (`RegraDesdobramento.tsx:424-453`); (3) Task 16 (Fornecedores) em quatro seções com ícone — o protótipo não usa `Tabs` —, com `secoes`/`secao` no `CadastroMasterDetail` e divergência D16.a; (4) Task 17 (Rotas) com `MoveVertical` e "Sequência de Paradas / Bairros", paradas editáveis por `Input` e divergência D17.a para reordenar/remover; (5) `useState` iniciais dos simuladores iguais ao protótipo (`10` e `10`/`3`) e DoD-50 deixando de petrificar total fabricado — passa a conferir o valor devolvido pela API; (6) imports de teste corrigidos para os exports nomeados reais `ProdutosClient` e `UsuariosAdminClient`; (7) Task 21.3 com JSX literal completo do drawer de usuários (`w-[520px]`, `Switch` de status, `Checkbox` de perfis, botão "Aprovar usuário" com SF-01 repassado sem máscara); (8) Task 25.3 e demais rotas BFF com código literal (proibido "repetir 25.1/25.2") — 19 rotas no DoD-70, incluindo `admin/usuarios/[id]/aprovar`; (9) filtro de Auditoria com a opção neutra "Todos os usuários" (`Auditoria.tsx:44`); (10) `--color-warning-surface` creditado ao aviso da pendência **P9** e grafia "ALFA CARNES" onde o protótipo escreve assim (`ModelosEtiqueta.tsx:91`). Novas divergências autorizadas: D16.a, D17.a, D22.a (chips de menu editáveis), D23.a ("Salvar" por cartão). Achados próprios da autorrevisão, corrigidos junto: o helper `mensagemDeErro(res)` usado em toda a onda não existia (só `extrairMensagemErro(body)`) e o `<Toaster />` do `sonner` não estava montado — a Task 12.1 passa a criar o adaptador e a montar o `Toaster` no layout `(admin)`, sem o que os erros sumiriam em silêncio (RA-05); nenhum trecho gerado escreve hex (`resumo-perfis.tsx` e o painel de diff passam a usar tokens, com o token `--color-code-surface` para o `pre` de `Auditoria.tsx:128`). Revisão anterior (`698efe4`) fechara as 6 correções do recheck2: (1) Task 13 realinhada ao schema real de `representantes` (`codigo, nome, tipo_canal, contato, status, observacao`) — campos inventados removidos e a afirmação falsa sobre o backend substituída pelas decisões 44/45 e pelas divergências D13.a/D13.b/D13.c; (2) Task 12 reescrita contra `Caminhoes.tsx`/`Motoristas.tsx`/`Representantes.tsx` — `select` reais de status/canal (sem botão "Filtros" inerte, RA-06), toggle `Power`/`PowerOff` sem exclusão, largura por tela (460 px caminhões/motoristas, 520 px representantes), zebra + clique na linha e contador da decisão 41 (sem "Mostrando N de M"); (3) Task 18 usa `Checkbox` como `ModelosEtiqueta.tsx:198`, com D18.b para o botão "Salvar Modelo"; (4) DoD-10 compara os grupos na ordem canônica do menu nos 11 perfis; (5) DoD-19 chama `seedModelosEtiqueta` e confere os valores literais da decisão 21; (6) listas de arquivos incluem `auth.service.ts` e `(admin)/page.tsx`, e a Task 8.5 pina `app/backend/src/modules/auth/auth.service.ts` (`montarMe`) como único ponto do `getMe`. Antes disso (`760a0f6`), o plano fechara as 14 correções do Monitor: AD-01/AD-02/AD-06 honradas (restam 2 badges Provisório — P1 e P12 — em `/admin/parametros` e P9 em modelos de etiqueta), Tasks 14/15/18 refeitas contra `Caminhoes.tsx`/`Motoristas.tsx`/`ModelosEtiqueta.tsx`, rota de entrada `comercial` unificada em `/comercial/clientes`, código literal nas Tasks 4.4/4.6/5.2/5.3/5.4/20.3, nomes e caminhos únicos, estrutura sem arquivos órfãos. Dívidas da Onda 2 absorvidas no mapa DoD→teste: **26 perdas** (decisão 25 da Onda 2) em DoD-05/DoD-07, conciliação catálogo×matriz (decisão 31 da Onda 2) em DoD-06/DoD-08/DoD-12, linha 41 da matriz (decisão 27 da Onda 2) em DoD-33 a DoD-35 e DoD-65 a DoD-69. P13 fechada por AD-04 (`ESTOQUE_*`). Nova dívida: "representantes permitidos" (linha 38) diferida para a Onda 4 pela decisão 43 |
| 4 | Comercial (Clientes, Pedidos, Preços, Disponibilidade-mapa, Espelho) | 3 | aguardando_inicio | just-in-time | — | — | P2 fechada por AD-06: sem expiração automática; liberação explícita e auditada |
| 5 | Gestão (Painel, Operações UI, Compras, Overbooking, Aprovações, SIF) | 3 | aguardando_inicio | just-in-time | — | — | — |
| 6 | Recebimento & Balança (recebimento §6.10, pesagem + Troca de Peça, etiquetas) | 4, 5 | aguardando_inicio | just-in-time | — | — | Destino das **7 dívidas de NF/Recebimento herdadas da Onda 1** (Portão 2, veredito `22d3f51`), formalizado na decisão 28 e na seção "Dívidas herdadas da Onda 1" do plano da Onda 2 — o plano tático desta onda precisa contê-las no mapa DoD→teste |
| 7 | Desossa (painel aeroporto/TV, pesagem c/ exclusividade, etiquetas) | 6 | aguardando_inicio | just-in-time | — | — | — |
| 8 | Estoque (consulta FIFO, entrada, ajustes) | 7 | aguardando_inicio | just-in-time | — | — | — |
| 9 | Carga (planejamento, conferência, enviar p/ faturamento) | 7 | aguardando_inicio | just-in-time | — | — | — |
| 10 | Faturamento (adapter EISS real + RTC, Notas/XML, Seguro F6b, Liberação c/ checklist) | 8, 9 | aguardando_inicio | just-in-time | — | — | Pendência externa: credenciais homologação EISS |

## Histórico pré-pipeline (fases F1–F6a — concluídas sob o framework anterior)

F1 (PR#1) · F2 (PR#2) · F3 (PR#3) · F4a (PR#4) · F4b (PR#5) · F4c (PR#6, gate F4 emitido 2026-06-07) · F5 (PR#7, 2026-06-08) · F6a (PR#8) · F7 cadastros (migration 0009) · absorção protótipo v2 (`540abea`) · recebimento simplificado (`d4e55c1`).
