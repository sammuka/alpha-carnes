# Execução — Status por Onda

> **Escritor único: Executor.** Vocabulário de status: `aguardando_inicio → planejando → aguardando_portao1 → plano_aprovado → implementando → aguardando_portao2 → mergeada` | `bloqueada`.
> Roadmap e grafo de dependências: [`roadmap-canonico.md`](../governance/roadmap-canonico.md) §8. Rito: [`pipeline-execucao.md`](../governance/pipeline-execucao.md).

| Onda | Escopo resumido | Depende de | Status | Plano tático | PR | SHA merge | Observações |
|---|---|---|---|---|---|---|---|
| 0 | Pipeline de governança (constituição, gates, skills, workflows, estado vivo) | — | mergeada | (plano mestre §6) | [#10](https://github.com/sammuka/alpha-carnes/pull/10) | `0f8491ff2141473b5b7e3cf784b9c878fce35549` | CI completo no head `72e2ac961b59e47ae1737b2d287f2134df53a7e4`; dois Monitores independentes aprovaram o mesmo objeto |
| 1 | Correção estrutural: Operação (D2), overbooking v1.1 (D1), Pedido ao Fornecedor + conferência tripla (D3), terminologia (D5) | 0 | mergeada | [`2026-07-22-onda1-correcao-estrutural.md`](../superpowers/plans/2026-07-22-onda1-correcao-estrutural.md) | [#13](https://github.com/sammuka/alpha-carnes/pull/13) | `231f6843d29296d0b87ff69f1246cb54b2a6bc7e` | Portão 2 aprovado no head `22d3f51` (veredito `3c220de`); CI verde no head do veredito (run `30134001208`). Plano sha256 `2d52f9df…`. AD-07/AD-08. Plano tático integrado antes pelo PR [#12](https://github.com/sammuka/alpha-carnes/pull/12); D9 pelo PR [#11](https://github.com/sammuka/alpha-carnes/pull/11) |
| 2 | Shell + DS fiel ao protótipo (layout, menu 9 grupos, tokens, componentes compartilhados) | 1 | aguardando_portao1 | [`2026-07-25-onda2-shell-ds.md`](../superpowers/plans/2026-07-25-onda2-shell-ds.md) | — | — | Plano tático escrito na branch `feature/onda2-plano-shell-ds`; **revisão 3 após Portão 1 `ajustar`** (veredito `9b34d6c`, Monitor `7389e835`): sha256 `6a47d5c0…` (era `b9839672…`), 9 Tasks, **31 decisões** fixadas e **37 invariantes** no mapa DoD→teste. Achados fechados nesta revisão: (A) o efeito do gate de grupo foi recalculado sobre 39 itens × 11 perfis contra a coluna "Perfis RBAC" da matriz — a decisão 25 passou de 4 casos para a **tabela completa de 26 perdas** (perfil, rota, papel na matriz, grupo invisível, motivo, destino Onda 3), com a decisão 30 **corrigindo** as duas perdas restauráveis sem efeito colateral (`faturamento` recupera `Relatórios & SIF`, matriz linha 13, via `FATURAMENTO_GERENCIAR` no gate de `GESTÃO` + estreitamento de `/gestao/dashboard`; `compras` recupera `Pendências de Overbooking`, linha 11, via `COMPRAS_PROGRAMADAS_GERENCIAR` no item) e a decisão 31 pinando os itens visíveis que a matriz não atribui (11 em `compras`, 3 em `diretoria`); `menu-rbac.test.ts` agora transcreve a matriz e afere perdas e extras nos 11 perfis, mais o invariante de que toda perda vem do gate de grupo e nunca do filtro de item. (B) O caso de colapso do `NavGroup` deixou de usar `not.toBeVisible()` (falso positivo: `max-height` inline é ignorada pelo jest-dom e o Tailwind não compila no jsdom) e passa a aferir o mecanismo — `aria-expanded`, `aria-controls`, `data-state` e a `max-height` inline —, com o componente expondo `id`/`data-state`; o caso Playwright equivalente, sujeito ao mesmo falso positivo, foi reescrito para `data-state` + `max-height` computada. (C) Contagem do smoke corrigida para 14 arquivos novos; microcopy do Painel Geral alinhada ao protótipo (`<h1>` de `/gestao/dashboard` + specs Playwright existentes) e o CTA `Acessar Sistema` propagado para `e2e/jornada-operacional.spec.ts`. As dívidas herdadas da Onda 1 seguem redirecionadas para a Onda 6 (decisão 28 + seção "Dívidas herdadas da Onda 1" do plano) — o plano da Onda 6 precisa absorver as sete linhas no seu mapa DoD→teste, e a Onda 3 precisa absorver as 26 linhas da decisão 25. Revisão 2 (veredito `4cbf6e9`, Monitor `3a1dbb62`, sha256 `b9839672…`, 29 decisões e 34 invariantes) havia fechado os 10 achados anteriores: critério sintático para o hex de seletor de atributo (`chart.tsx`, decisão 23), trechos das Tasks 3/5 recompilados sob `noUncheckedIndexedAccess`, teste de `AdminHeader` desambiguado, comando real de subida do backend (serviço `backend` do compose — não existe `start:dev`), rota de entrada `/` + destino pós-login pelo menu do perfil (decisão 26), primeira declaração dos efeitos do gate de grupo (decisão 25), RBAC de UI de `/cadastros/rotas` e `/desossa/dashboard` com decisão numerada (24), `/admin/auditoria` (matriz linha 41) diferido para a Onda 3 com teste de guarda (27) e CTA do login no token de ação (29). Aguarda novo veredito do Portão 1. Implementação irá na branch `feature/onda2-shell-ds` |
| 3 | Cadastros & Regras completos + Admin | 2 | aguardando_inicio | just-in-time | — | — | P13 fechada por AD-04: 11 perfis com recorte `ESTOQUE_*`. O plano tático precisa absorver no mapa DoD→teste as **26 perdas de item** declaradas na decisão 25 do plano da Onda 2 e a conciliação catálogo × matriz da decisão 31, além da linha 41 da matriz diferida pela decisão 27 |
| 4 | Comercial (Clientes, Pedidos, Preços, Disponibilidade-mapa, Espelho) | 3 | aguardando_inicio | just-in-time | — | — | P2 fechada por AD-06: sem expiração automática; liberação explícita e auditada |
| 5 | Gestão (Painel, Operações UI, Compras, Overbooking, Aprovações, SIF) | 3 | aguardando_inicio | just-in-time | — | — | — |
| 6 | Recebimento & Balança (recebimento §6.10, pesagem + Troca de Peça, etiquetas) | 4, 5 | aguardando_inicio | just-in-time | — | — | Destino das **7 dívidas de NF/Recebimento herdadas da Onda 1** (Portão 2, veredito `22d3f51`), formalizado na decisão 28 e na seção "Dívidas herdadas da Onda 1" do plano da Onda 2 — o plano tático desta onda precisa contê-las no mapa DoD→teste |
| 7 | Desossa (painel aeroporto/TV, pesagem c/ exclusividade, etiquetas) | 6 | aguardando_inicio | just-in-time | — | — | — |
| 8 | Estoque (consulta FIFO, entrada, ajustes) | 7 | aguardando_inicio | just-in-time | — | — | — |
| 9 | Carga (planejamento, conferência, enviar p/ faturamento) | 7 | aguardando_inicio | just-in-time | — | — | — |
| 10 | Faturamento (adapter EISS real + RTC, Notas/XML, Seguro F6b, Liberação c/ checklist) | 8, 9 | aguardando_inicio | just-in-time | — | — | Pendência externa: credenciais homologação EISS |

## Histórico pré-pipeline (fases F1–F6a — concluídas sob o framework anterior)

F1 (PR#1) · F2 (PR#2) · F3 (PR#3) · F4a (PR#4) · F4b (PR#5) · F4c (PR#6, gate F4 emitido 2026-06-07) · F5 (PR#7, 2026-06-08) · F6a (PR#8) · F7 cadastros (migration 0009) · absorção protótipo v2 (`540abea`) · recebimento simplificado (`d4e55c1`).
