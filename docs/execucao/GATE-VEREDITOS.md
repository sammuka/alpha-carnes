# Vereditos de Gate — AlphaCarnes

> **Escritor único: Monitor. Append-only** — nunca editar/remover linhas anteriores.
> Formato: `| data-hora | onda | portão (1=plano, 2=PR, A=adversarial) | veredito (aprovado/ajustar/bloqueado) | evidência (link/comando) | feedback resumido |`
> Critérios: [`pipeline-execucao.md`](../governance/pipeline-execucao.md) §3–§4; princípios: [`constituicao.md`](../governance/constituicao.md).

| Data/hora | Onda | Portão | Veredito | Evidência | Feedback |
|---|---|---|---|---|---|
| 2026-07-23T09:56:36-03:00 | 0 | 2 | aprovado | PR [#10](https://github.com/sammuka/alpha-carnes/pull/10), head `72e2ac961b59e47ae1737b2d287f2134df53a7e4`, run `30008201248` | Monitor independente: 41/41 entradas, AD-03–AD-06, lock, plano literal, coverage ACMR/base SHA, gitleaks e Vercel exclusiva da landing aprovados sem ressalvas. |
| 2026-07-23T09:57:56-03:00 | 0 | A | aprovado | PR [#10](https://github.com/sammuka/alpha-carnes/pull/10), head `72e2ac961b59e47ae1737b2d287f2134df53a7e4`, squash `0f8491ff2141473b5b7e3cf784b9c878fce35549` | Segundo Monitor independente: testes adicionais de ownership/concorrência, pins, migrations, RBAC e limite de deploy confirmados; nenhum bloqueio. |
| 2026-07-23T15:14:02Z | 1 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-22-onda1-correcao-estrutural.md`; sha256=`84b2bc0466b740f34b28994358a366d26e4c786157b3392387dda3e8a42eca33`; planoOID=`7e5c5531c7d22ae5cb645c263b56361d8fe2cb03`; HEAD=`8b2958ef992e1243015cd7f0507310dbac9706e0`; run=`gate1-onda1-20260723-b1f8293`; comandos=grep/sed/sha256sum sobre plano+schemas (recebimentos, pesagem, pedidos), protótipo `feature/completude-v1.1`, EXECUCAO-STATUS/roadmap | 4 ajustes obrigatórios: (1) ordem expand/backfill viola: `0013` grava status novos (`pesagem_em_andamento`, `em_elaboracao_reserva_ativa`, `aguardando_confirmacao_overbooking`) mas L211 mantém CHECKs legados até `0014` → CHECK abortará o backfill; ampliar/soltar os CHECKs em `0012` antes do backfill. (2) SQL da conferência (L1542-1547) lê `pecas.item_comercial_id` e `SUM(peso_liquido)`, inexistentes — schema real tem `item_comercial_base_id` e `peso_original`; corrigir o SQL ou definir mapeamento no expand. (3) Escopo D9 (CLAUDE.md/AGENTS.md), listado na linha da Onda 1 em EXECUCAO-STATUS e no roadmap §8, não consta do Goal/Tasks do plano — incluir ou reescopar formalmente. (4) Fidelidade Princípio I: microcopy do overbooking diverge — plano L815/L866 diz "o gestor deverá tratar a falta" vs protótipo PedidoVenda.tsx:229 "a gestão deverá tratar a falta"; usar o texto validado. Sem ressalvas: reprovado para ajuste. |
| 2026-07-23T15:51:23Z | 1 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-22-onda1-correcao-estrutural.md`; sha256=`e0f602d5295fe0b0275cff56d5f39b3fc0afd0f3296abd573c923a2c373e4e37`; HEAD=`16e6e702feb23c6914e47ab1eacb041eda08171d`; run=`gate1-onda1-recheck-20260723-16e6e70`; comandos=sha256sum+git rev-parse/status; grep sobre plano; leitura de schemas (permissoes, pesagem, pedidos, recebimentos, compras-programadas, disponibilidades, eventos, decimal), migration 0011+journal, frontend pesagem-destinacao, protótipo `feature/completude-v1.1`, EXECUCAO-STATUS/roadmap/DECISOES | Reauditoria independente: 4 achados anteriores (CHECK superset em `0012`, SQL da conferência com `item_comercial_base_id`/`peso_original`, D9 fora do escopo, microcopy) foram CORRIGIDOS neste objeto — L369-390 ampliam os três CHECKs reais ao superset legado∪final antes do `0013`; L1570-1679 usam o schema real de `pecas`; D9 formalmente fora do escopo (Goal L8) e concluída pelo PR #11 squash `8b2958e`, sem gap status/roadmap/plano; microcopy L720/L815 idêntica a PedidoVenda.tsx:229 "a gestão deverá tratar a falta". AD-05 challenge 409 read-only sem mutação e não bloqueante, AD-06 sem TTL, 11 perfis, eventos dentro do objeto `as const`, Docker 4000/4001/15433, EISS não antecipado — todos OK. 1 ajuste obrigatório NOVO (Autossuficiência): Task 2 L669-679 faz `MAPA_PERFIL_PERMISSOES.<perfil>.push('OPERACOES_GERENCIAR','PEDIDO_OVERBOOKING_CONFIRMAR','OVERBOOKING_RESOLVER','PEDIDO_FORNECEDOR_GERENCIAR','CONFERENCIA_CONCLUIR','PEDIDO_FINALIZAR')` mas nenhuma Task adiciona essas 6 chaves ao objeto canônico `PERMISSOES` (permissoes.ts L1-70) — `.<perfil>` é `Permissao[]` e `Permissao` é a união derivada de `PERMISSOES` (L72), então o `.push` de literais fora da união FALHA em TS-strict (TS2345, Tech Stack) e sem a chave o seed/DoD-RBAC (L135) não concede a permissão em runtime (403 permanente). Corrigir: estender `PERMISSOES` com as 6 chaves (torna `DESCRICOES_PERMISSOES` exaustivo e o `.push` válido) antes de usá-las no controller/seed. Sem ressalvas: reprovado para ajuste. |
| 2026-07-23T16:15:32Z | 1 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-22-onda1-correcao-estrutural.md`; sha256=`ff45f37e0d5897ef0ae604df491563b31ed34bea82a391581044e7c9ae85c6bc`; HEAD=`9e6ab4181dab97c3cd0245a4642e3cb28b3fc5e3`; run=`gate1-onda1-recheck2-20260723-9e6ab41`; comandos=git rev-parse/status (árvore limpa)+sha256sum (bate); grep/leitura integral do plano; leitura de schemas reais (recebimentos, pedidos, compras-programadas, disponibilidades, permissoes); protótipo `feature/completude-v1.1`; EXECUCAO-STATUS/roadmap/DECISOES/SKILL | Terceira instância independente. Pins OK (HEAD `9e6ab41`, sha256 bate, Onda 1 `aguardando_portao1`, Onda 0 `mergeada`). Achado do recheck anterior CORRIGIDO: Task 2 L670-675 declara as 6 chaves em `PERMISSOES` e L683-688 em `DESCRICOES_PERMISSOES` (Record exaustivo) ANTES do `.push` (L694-704) — `.push`/decorators/seed/11 perfis coerentes; nenhum literal de permissão sem declaração canônica. Também OK: microcopy "a gestão" (L927/960/1011=PedidoVenda.tsx:229), conferência com `item_comercial_base_id`/`peso_original` (L1690-1695), D9 fora do escopo, CHECK superset de STATUS em 0012 (L379-396), overbooking 409 read-only não bloqueante, unicidade com operação (AD-03), sem TTL (AD-06), Docker 4000/4001/15433, Vercel fora do app, EISS não antecipado. 2 ajustes obrigatórios NOVOS (Consistência/executabilidade): (1) Task 4 L1656-1662 `tx.insert(recebimentos).values({...usuarioCriacaoId: usuarioId})` referencia coluna inexistente — o schema real `recebimentos.schema.ts` L27-51 NÃO tem `usuarioCriacaoId` e possui `responsavelRecebimentoId` NOT NULL sem default (L44) que o INSERT não preenche; grep de "responsavel"/"rename" no plano = vazio, logo não há renomeação prevista → falha TS-strict + NOT NULL em runtime. Corrigir: usar `responsavelRecebimentoId: usuarioId` (não `usuarioCriacaoId`). Contraste: `pedidos_venda` (L977) tem `usuarioCriacaoId` real e está correto. (2) Task 5 L1801-1807 `insert(divergenciasRecebimento).values({...tipo: classificarTipoV11(item)})` grava `falta|excesso|produto_nao_previsto|peso_divergente|outro` (L1824-1829), mas o CHECK real `chk_diverg_receb_tipo` (recebimentos.schema.ts L114-116) só aceita `quantidade_menor|quantidade_maior|item_divergente|qualidade_divergente|peso_incompativel|item_ausente|item_excedente|inconsistencia_nf_fisico` — nenhum valor novo pertence ao conjunto. O plano amplia os três CHECKs de STATUS em 0012/0014 mas NÃO trata `chk_diverg_receb_tipo` (só troca `tipo` para `notNull`/adiciona colunas L2032-2035) → toda divergência abortará no INSERT. Corrigir: ampliar/trocar `chk_diverg_receb_tipo` para os valores de `TipoDivergenciaV11` no expand 0012 (superset) e/ou contract 0014, pelo idioma nomeado de `0011`. Sem ressalvas: reprovado para ajuste. |
| 2026-07-23T16:54:16Z | 1 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-22-onda1-correcao-estrutural.md`; sha256=`8fef596aa106c27f06f812a8d5d59a7ba1f972cb57ee57a7bc842d7b3674a501`; HEAD=`3c0d71c75bfa4ed9aa1cb286a8d88faf3fecd35b`; run=`gate1-onda1-recheck3-20260723-3c0d71c`; comandos=git rev-parse/status (árvore limpa)+sha256sum (bate); leitura integral do plano; grep dirigido (calcularQuadro/situacao/recebimentoItemId/requerBalanca); leitura de schemas reais (recebimentos, pesagem, pedidos, permissoes, eventos, decimal) e do serviço `ocorrencia-fornecedor.service.ts`; find de `modules/operacao/recebimento`; protótipo `feature/completude-v1.1`; EXECUCAO-STATUS/roadmap/DECISOES/constituição/SKILL | Quarta instância independente. Pins OK (HEAD `3c0d71c`, sha256 bate, Onda 1 `aguardando_portao1`, Onda 0 `mergeada`). Os DOIS achados do recheck2 foram CORRIGIDOS neste objeto: (a) Task 4 L1678 usa `responsavelRecebimentoId: usuarioId` (NOT NULL real de `recebimentos.schema.ts`), não mais `usuarioCriacaoId`; (b) o expand 0012 L404-409 amplia `chk_diverg_receb_tipo` ao superset legado∪v1.1 (13 valores) e o contract 0014 L2159 o fecha nos 5 finais (`falta|excesso|produto_nao_previsto|peso_divergente|outro`) pelo idioma nomeado de 0011, com backfill 0013 L2073-2075 remapeando 8→5 preservando origem em `descricao`. Também OK: microcopy "a gestão deverá tratar a falta" (L943/977/1028 = PedidoVenda.tsx:229); catálogo RBAC completo (6 chaves em `PERMISSOES` + `DESCRICOES_PERMISSOES` exaustivo antes do `.push`, decorators/seed/grant-deny/11 perfis); `pecas` com `item_comercial_base_id`/`peso_original` (L1703-1712); expand→backfill→contract dos 3 CHECKs de STATUS; overbooking AD-05 409 read-only sem mutação e não bloqueante; unicidade com operação (AD-03); sem TTL (AD-06); ocorrência via `abrirNaTx` sem emit + `emitirAbertura` pós-commit (bate com o serviço real); D9 fora do escopo (concluída no PR #11 `8b2958e`); Docker 4000/4001/15433; Vercel fora do app; EISS não antecipado. 1 ajuste obrigatório NOVO (Autossuficiência + RA-01/Princípio VIII): Task 5 introduz o método NOVO `calcularQuadro(this.db, id)` (controller L1778; inexistente no código real — `modules/operacao/recebimento` não tem `conferencia.service`, e o único `concluirConferencia` é o de `expedicao`) porém NÃO define seu corpo. O SELECT do quadro (L1726-1751) projeta só `item_comercial_id, qtd_pedido, qtd_nf, qtd_apurada, peso_nf, peso_apurado, previsto_no_pedido` (snake_case) e NÃO projeta `situacao` nem `recebimento_item_id`, mas o consumidor `concluirConferencia` L1824 filtra `q.situacao === 'divergente'` e L1828 lê `item.recebimentoItemId`, e a interface `QuadroItem` (L1877-1886) exige campos camelCase (`recebimentoItemId`,`qtdNf`,`pesoNf`,`situacao`). Faltam no plano: (i) o mapeamento linhas SQL→`QuadroItem` (snake→camel, projeção de `recebimento_item_id`) e (ii) — crítico — o critério de negócio que classifica `situacao: 'conforme'|'divergente'` (a regra que dispara a ocorrência administrativa ao fornecedor, RA-01/Princípio III; `classificarTipoV11` só roda DEPOIS do filtro e não a define, e o peso usa igualdade exata sem tolerância declarada). Deixar `calcularQuadro`/`situacao` indefinidos força o Worker a inventar regra de negócio (viola Autossuficiência e Princípio VIII). Corrigir: definir no plano o corpo de `calcularQuadro` com a projeção completa (incl. `recebimento_item_id`), o mapeamento para `QuadroItem` e o critério explícito de `conforme|divergente` (com tolerância de peso, se houver). Sem ressalvas: reprovado para ajuste. |
| 2026-07-23T17:26:52Z | 1 | 1 | aprovado | plano=`docs/superpowers/plans/2026-07-22-onda1-correcao-estrutural.md`; sha256=`2d52f9dffe31c482c38ce567022201507022e97b536606c2ad37d2014595beee`; HEAD=`0b466817163b0d893ed0bbfaab781248defd8f5c`; run=`gate1-onda1-recheck4-20260723-0b46681`; comandos=git rev-parse/status (árvore limpa)+sha256sum (bate); leitura integral do plano; leitura de schemas/serviços reais (recebimentos, pedidos-fornecedor, notas-fiscais, conclusoes-conferencia, permissoes, eventos, ocorrencia-fornecedor.service+dto); grep TBD/microcopy; protótipo `feature/completude-v1.1` (PedidoVenda.tsx:229, RecebimentoCarga.tsx:370-374, PesagemDestinacao.tsx); EXECUCAO-STATUS/roadmap/DECISOES/constituição/SKILL | Quinta instância independente. Pins conferem (HEAD `0b46681`, sha256 `2d52f9d`, Onda 1 `aguardando_portao1`, Onda 0 `mergeada`, árvore limpa). O achado do recheck3 (`calcularQuadro`/`situacao` indefinidos) foi CORRIGIDO neste objeto (commit `0b46681` "specify conference calculation contract"): (1) `calcularQuadro` L1701-1792 tem corpo completo — `db: NodePgDatabase<typeof schema>`, query crua por `db.execute(sql...)` lendo `.rows` (idioma real de `disponibilidade.service.ts`); o SELECT projeta `ri.id AS recebimento_item_id`, qtd por modalidade (`requer_balanca ? COUNT(pecas) : ri.quantidade_recebida`), peso só do pesável, `previsto_no_pedido`; UNION garante item só-pesado no quadro (RA-05/06); GROUP BY sem duplicação; aliases `item_comercial_base_id AS item_comercial_id`/`SUM(peso_original)` batem com `pesagem.schema.ts` e `pfi.quantidade_prevista/item_comercial_id` com `pedidos_fornecedor_itens`. (2) mapa `QuadroRow`(snake) → `QuadroItem`(camel) campo a campo, `recebimentoItemId` nullable coerente com a FK nullable do expand. (3) `situacao` sai SÓ de `classificarSituacao` (L1955-1968): não previsto OU `compararQtd(qtdApurada,qtdNf)!==0` OU (ambos pesos presentes e `compararQtd` peso !==0); Decimal exato, sem tolerância — aterrado em RecebimentoCarga.tsx:374 (`difQtd !== 0`) e spec v1.1 §6.10.4, sem AD autorizando folga (Princípio VIII); caixaria `pesoApurado=null` não gera falso positivo. FKs/CHECKs 0012→0013→0014 fechados: expand nullable + 4 CHECKs ao superset; backfill 0013 remapeia 8→5 tipos e preenche `item_comercial_id` com guarda `RAISE EXCEPTION` contra NULL; contract 0014 aperta os 4 CHECKs aos finais, `item_comercial_id` NOT NULL e `recebimento_item_id` nullable. Conclusão transacional com `onConflictDoNothing` (409 na 2ª), `abrirNaTx` sem emit + `emitirAbertura` pós-commit por ocorrência (bate com o serviço/DTO/evento reais). DoD→teste 1:1 (L138 cobre `calcularQuadro`). Reconfirmados sem regressão: RBAC 6 chaves em `PERMISSOES`+`DESCRICOES` exaustivo antes do `.push`/11 perfis (AD-04); `responsavelRecebimentoId`; CHECKs de status e tipo; schema `pecas`; microcopy literal; D9 fora (PR #11 `8b2958e`); AD-03/05/06; Docker 4000/4001/15433; Vercel fora do app; EISS não antecipado; fidelidade ao protótipo; zero TBD/escopo futuro. Aprovado sem ressalvas: Executor pode marcar `plano_aprovado`. |
| 2026-07-24T18:30:46-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `26a2d9b`, run `30117112512`; `gh pr checks 13` + annotation do job lint | CI vermelho imediato (jobs 0 steps): "The job was not started because recent account payments have failed or your spending limit needs to be increased." — billing Actions em PRIVATE. Gate documental NÃO avançado. Remediar: lease PUBLIC→CI→PRIVATE via `.codex/scripts/visibility-ci.ps1` (mesmo rito do PR #12) e reexecutar Portão 2 com checks verdes. |
| 2026-07-24T18:41:22-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `c38693c`, run `30117370072` (lease PUBLIC→PRIVATE); lint fix `c38693c` | CI quase verde (lint/type-check/test-backend/test-frontend/build/audit/secret-scan PASS). Falha em `coverage` ACMR: 14 services tocados com branches <80% (global 80.12% OK). Ex.: pedidos 65.7%, disponibilidade 60%, faturamento 70%, recebimento 77.2%, corte 75.9%. Portão 2 documental não avançado até ACMR verde. |
| 2026-07-24T17:45:55-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `21a5d179dd0f8c93566860e26fe90fae0abdbf51`, run `30124198575`, plano sha256 `2d52f9df…`; comandos=`gh pr checks 13` (10/10 pass) + `gh pr view 13 --json mergeable` (CLEAN/MERGEABLE) + `gh repo view --json visibility` (PRIVATE) + `gh run view 30124198575 --log --job 89585617418` (ACMR: 22 services ≥80% linhas/branches) + `sha256sum` do plano + `git diff --name-status origin/develop...HEAD` (132 arquivos) + leitura integral do plano (2540 L) e dos 3 SQL de migration + `find app/frontend/src/app/api -type d` + greps dirigidos (403/FORBIDDEN em todo `test/`, `marcas?`, `data_operacao`, `setInterval/@Cron`, `success`, hex no diff do frontend) + protótipo `feature/completude-v1.1` (PedidoVenda.tsx:229, PesagemDestinacao.tsx:657) | CI 100% verde e ACMR real (22/22 services tocados ≥80% linhas **e** branches) — o achado do Portão 2 anterior está resolvido. Backend confere com o plano: expand→backfill→contract literal (0012 sem `SET NOT NULL`/`DROP COLUMN`, 4 CHECKs no superset; 0013 remapeia 8→5 tipos com `[origem_legado=…]` + guarda `RAISE EXCEPTION`; 0014 dropa `data_operacao` das 6 tabelas + os 7 `nfe_*`, aperta os 4 CHECKs e fecha as FKs), challenge 409 read-only antes de qualquer escrita (`pedidos.service.ts` L131-148, `garantirOperacao` só após a decisão), `calcularQuadro`/`classificarSituacao` como única fonte da `situacao` com `compararQtd` exato, microcopy idêntica ao protótipo, 6 chaves em `PERMISSOES`+`DESCRICOES` exaustivo, `@RequirePermissoes` em todas as rotas novas, eventos pós-commit, zero `setInterval`/`success:true`/hex novo/rótulo banido. **4 ajustes obrigatórios:** (1) **Princípio II (completude E2E) — 12 rotas BFF da "Estrutura de arquivos" (plano L104-111) não existem**: `api/comercial/pedidos/confirmar-overbooking`, `[id]/itens`, `[id]/itens/confirmar-overbooking`, `[id]/finalizar`, `api/comercial/overbooking/{route,[id],[id]/decisao}`, `api/operacao/pedidos-fornecedor/{route,[id]}`, `api/operacao/recebimentos/[id]/{nf,concluir-pesagem,conferencia,conferencia/concluir}`; `find app/frontend/src/app/api -type d` não lista nenhuma e não há catch-all/proxy. O único BFF tocado é `comercial/pedidos/route.ts` (repassa o 409) → a UI recebe o challenge de overbooking mas **não tem caminho para confirmá-lo**, e a fila de pendências, o Pedido ao Fornecedor e a conferência tripla ficam inalcançáveis pelo frontend. `lib/comercial.ts` e `lib/operacao.ts` (também listados no plano) não foram tocados — sem tipos dos novos contratos. Criar as rotas + tipos ou reescopar formalmente com AD-xx. (2) **DoD→teste "RBAC das permissões novas — um caso 403 em cada suíte de endpoint" (mapa L140) descumprido para 5 de 6 chaves**: `grep -rn '403\|FORBIDDEN' app/backend/test` acha 403 novo só em `operacoes.e2e-spec.ts:56` (`OPERACOES_GERENCIAR`). Sem caso 403 para `PEDIDO_OVERBOOKING_CONFIRMAR`, `OVERBOOKING_RESOLVER`, `PEDIDO_FORNECEDOR_GERENCIAR`, `CONFERENCIA_CONCLUIR` e `PEDIDO_FINALIZAR` — remover o decorator dessas rotas não quebraria nenhum teste (`seed.spec.ts` prova concessão no catálogo, não o guard HTTP). (3) **DoD→teste "`data_operacao` e cache `nfe_*` não existem após contract — consulta `information_schema.columns`" (mapa L129) inexistente**: `onda1-migrations.e2e-spec.ts` só usa `expectColuna` para provar *presença* na janela expand (L31) e o teste do 0014 (L89-97) verifica apenas `chk_diverg_receb_tipo`; não há helper de ausência em `test/helpers/onda1-migrations.ts`. Os `nfe_*` são nullable, logo não há nem cobertura indireta: se o 0014 deixasse de dropá-los, a suíte seguiria verde. (4) **Escopo fora do plano — governança alterada dentro do PR da onda**: o commit `5668024` ("test(onda1): progresso Task 9") adiciona `docs/governance/pipeline-execucao.md` §9 "Paralelismo de Workers" (+26 L, renumerando §9→§10) e a seção "Paralelismo de execução" em `docs/governance/roadmap-canonico.md` (+5 L). Nenhum dos dois consta da "Estrutura de arquivos" nem do Goal (D1/D2/D3/D5); o plano só autoriza doc em `ROLLBACK.md` (Task 7) e a reescopagem de D9 (L8). Agrava: o PR cria a regra de paralelismo e a aplica retroativamente no mesmo objeto (fatias `b20e890`/`0956c7c`/`ebbea99`). Extrair para PR de governança próprio ou registrar autorização. **Dívida registrada (não reprova):** (a) o bloco `DO $$ … RAISE EXCEPTION 'duplicidade ativa em pedidos_venda_itens…'` do plano (Task 3 L1215-1228) não foi anexado ao `0012` antes do `CREATE UNIQUE INDEX uq_pedido_venda_item_comercial_ativo` (0012 L176; zero blocos `DO $$` no arquivo) — o índice ainda aborta alto em legado duplicado e nada é escolhido silenciosamente, mas o diagnóstico nomeado exigido não existe (contraste: a guarda equivalente do `0013` L34-42 foi implementada); (b) teste nomeado "journal aplica os três arquivos em ordem" (mapa L122) ausente — substância coberta por `migrarAte('0014_onda1_contract')`; (c) `EXECUCAO-STATUS.md` alterado em commit próprio do Executor (`d422cc2`), coerente com a skill §9 apesar de o plano L4 vedá-lo ao Worker; (d) `pushPermissoes()` no lugar do `.push` literal do plano — equivalente e com falha explícita se o perfil faltar. Merge bloqueado até (1)-(4). |
| 2026-07-24T18:07:40-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `6711c3c8e0a8c90ed1a50b5e44db117e8598b709`, run `30125705409`, plano sha256 `2d52f9df…`; comandos=`gh pr checks 13` (10/10 pass: 8 obrigatórios + Vercel/Vercel Preview Comments) + `gh run view 30125705409 --json headSha,conclusion` (`success`, headSha == HEAD) + `gh pr view 13 --json state,mergeable` (OPEN/MERGEABLE) + `gh repo view --json visibility` (PRIVATE) + `sha256sum` do plano (bate) + `git diff --name-status origin/develop...HEAD` (148 arquivos) + `git show --stat` de `6711c3c`/`e10667e`/`0b65ed8`/`1130e1c` + `find app/frontend/src/app/api -name route.ts` + leitura das 13 rotas BFF, `lib/api.ts`, `lib/{comercial,operacao}.ts` + grep `@Controller/@RequirePermissoes` nos 5 controllers + leitura dos 6 casos 403 + leitura de `conferencia.service.ts` (SQL de `qtd_nf`), `recebimento.service.ts` (`iniciar`/`atualizarNfe`/`listar`/`detalhar`), `recebimento.dto.ts`, `0014` (`DROP COLUMN`) + greps RA-04/05/06, hex, rótulo banido e segredos no diff | Sexta instância independente; CI 100% verde no HEAD auditado. **3 dos 4 achados anteriores RESOLVIDOS:** (2) há caso 403 real para as 6 chaves novas — `operacoes.e2e-spec.ts:56`, `pedidos-v11.e2e-spec.ts:62` (`PEDIDO_OVERBOOKING_CONFIRMAR`) e `:76` (`PEDIDO_FINALIZAR`), `overbooking-lifecycle.e2e-spec.ts:35` (`OVERBOOKING_RESOLVER`), `pedido-fornecedor.e2e-spec.ts:70`, `conferencia-tripla.e2e-spec.ts:109` — todos batem o endpoint real com perfil sem a chave e falhariam se o decorator saísse; (3) `expectColunaAusente` (`test/helpers/onda1-migrations.ts:198-210`) + `onda1-migrations.e2e-spec.ts:100-127` provam por `information_schema` a ausência de `data_operacao` nas 6 tabelas e dos 7 `nfe_*` pós-`0014`; (4) AD-07 (`DECISOES.md`) autoriza formalmente a governança alterada no PR. Do achado (1), 11 das 12 rotas BFF estão criadas e batem com os controllers reais (`comercial/pedidos/{confirmar-overbooking,[id]/itens,[id]/itens/confirmar-overbooking,[id]/finalizar}`, `comercial/overbooking/{,[id],[id]/decisao}`, `operacao/pedidos-fornecedor/{,[id]}`, `operacao/recebimentos/[id]/{concluir-pesagem,conferencia,conferencia/concluir}`), com `apiFetch` preservando status/body do challenge 409 e `lib/{comercial,operacao}.ts` tipando os contratos novos. Reconfirmados sem regressão: RA-02 (`db.transaction` + `auditoria.registrar` no mesmo escopo em overbooking/conferência/pedido-fornecedor/operações), RA-04 (emissões fora do `transaction`, ex. `operacoes.service.ts:148-152`), zero `setInterval`/`@Cron`/`success: true`/hex novo/rótulo banido/segredo. **2 ajustes obrigatórios:** (1) **Resíduo do achado 1 — Princípio II: a perna "NF" da conferência tripla continua inalcançável pela UI.** O BFF novo `app/api/operacao/recebimentos/[id]/nf/route.ts` é alias byte-equivalente do pré-existente `[id]/nfe/route.ts` (mesmo `PATCH /operacao/recebimentos/:id/nfe`), não do endpoint que "substitui o cache" (plano L1635). O único writer de `notas_fiscais_fornecedor(_itens)` é `POST /operacao/pedidos-fornecedor/:id/nf` (`pedido-fornecedor.controller.ts:62`, `service.registrarNf` L166-216) e ele não tem BFF; como `qtd_nf`/`peso_nf` do quadro saem exclusivamente dessas tabelas (`conferencia.service.ts` L119-127 e L153-158), o frontend renderiza o quadro mas nunca alimenta a NF. Agrava: rota duplicada sem consumidor (DRY). Criar o BFF do endpoint real (resolvendo o `pedidoFornecedorId` do recebimento) ou reescopar com AD-xx. (2) **RA-05/06 — descarte silencioso de dados de NF introduzido por este PR.** `iniciarRecebimentoSchema` (`recebimento.dto.ts` L8-29) e `atualizarNfeSchema` (L33-52) continuam aceitando `nfeSerie`, `nfeChave`, `nfeDataEmissao`, `nfePesoBruto`, `nfePesoLiquido` e `nfeVolumes`, mas depois do `0014` dropar as 7 colunas `iniciar` (`recebimento.service.ts:316`) e `atualizarNfe` (L398-406) só persistem `nfeNumero` → `notaFiscalFornecedor` e devolvem 201/200 sem erro nem aviso; `detalhar` (L241-277) não faz o join com `notas_fiscais_fornecedor` (só `listar` o faz, L123), então nem o número volta. Consequência verificada: `pesagem-destinacao-client.tsx` L463-468 e `recebimento-carga-client.tsx` L297-302/793-801 leem `detalhe.nfeNumero`/`nfeSerie`/`nfePesoBruto` — hoje sempre `undefined` → o campo "NF" some das duas telas (Princípio I) e o que o operador digita em "Chave NF-e"/"Peso bruto NF" (L1049/L1058, enviado em L374-380 e L435-440) é descartado em silêncio. `lib/operacao.ts` (`RecebimentoDetalhe`) ainda declara esses campos, então o type-check fica verde sobre um contrato que a API não cumpre, e nenhum teste pega: `recebimento.e2e-spec.ts:470-479` envia `nfePesoBruto: 1500.5` e afere só 200/`notaFiscalFornecedor`, e o unit `atualizarNfe → persiste campos NF e audita` (`recebimento-simplificado-branches.spec.ts:149-157`) envia `nfeSerie`/`nfePesoBruto` sem assertir nada sobre eles. Contradiz o plano L2296 ("NF vem de `notas_fiscais_fornecedor`"). Corrigir: rotear esses campos para `registrarNf`/`notas_fiscais_fornecedor` e expor a NF em `detalhar` pela junção, ou rejeitar explicitamente os campos e ajustar telas/tipos — nunca aceitar e descartar. **Dívida registrada (não reprova, mantida do veredito anterior):** (a) bloco `DO $$ … RAISE EXCEPTION 'duplicidade ativa em pedidos_venda_itens…'` do plano (Task 3 L1215-1228) ausente no `0012`; (b) teste nomeado "journal aplica os três arquivos em ordem" ausente (substância coberta por `migrarAte('0014_onda1_contract')`); (c) `EXECUCAO-STATUS.md` alterado em commit do Executor; (d) `pushPermissoes()` no lugar do `.push` literal. Merge bloqueado até (1)-(2). |
| 2026-07-24T18:41:03-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `2445aeb295f86adf352bc89febbb8bf88f365725`, run `30127708371`, plano sha256 `2d52f9df…`; comandos=`gh pr checks 13` (10/10 pass: 8 obrigatórios + Vercel/Vercel Preview Comments) + `gh run view 30127708371 --json headSha,conclusion` (`success`, headSha == HEAD) + `gh pr view 13 --json state,mergeable` (OPEN/MERGEABLE) + `sha256sum` do plano (bate) + `git status --porcelain` (limpa) + `git show --stat 77d3cf6 9f899c6 2445aeb` + `git diff 19c69da..2445aeb` de `recebimento.service.ts`/`pedido-fornecedor.service.ts`/testes + leitura integral de `nota-fiscal-fornecedor.persistence.ts` (217 L), `recebimento.dto.ts`, `conferencia.service.ts` (SQL do quadro + guarda `legado_sem_itens_nf` L248), `pedido-fornecedor.dto.ts` (`registrarNfSchema`), das 2 rotas BFF novas e de `[id]/nfe/route.ts` + `rg` de `nfePesoLiquido/nfeBruto/nfeVolumes` em todo o frontend + leitura de `recebimento-carga-client.tsx` (L296-309, 376-382, 436-442, 801-809, 1057-1066) + `pedido-fornecedor.e2e-spec.ts:147-172` + `gh run view --log --job 89596344304` (ACMR: 22 services) + `scripts/check-coverage{,-lib}.mjs` (glob `**/*.service.ts`) + `0013_onda1_backfill.sql:74` + plano L39/L104-111/L1795-1804/L1839-1872/L2108 + greps `success:true`/`setInterval`/`@Cron`/hex/`marca`/segredo no diff novo (vazios) | Sétima instância independente; CI 100% verde no HEAD auditado (ACMR 22/22 services ≥80% linhas **e** branches; total 98.42%/86.26%). **Achado (1) do veredito anterior RESOLVIDO:** existe BFF do writer real (`app/api/operacao/pedidos-fornecedor/[id]/nf/route.ts`, `POST` → `/operacao/pedidos-fornecedor/:id/nf`) e o `PATCH /operacao/recebimentos/:id/nfe` que a tela já chama (`recebimento-carga-client.tsx:431`) passou a gravar `notas_fiscais_fornecedor(_itens)`, tornando a perna NF alcançável; `registrarNf` foi refatorado para o helper compartilhado (DRY) e `detalhar` expõe `nfeSerie/nfeChave/nfeDataEmissao/nfePesoBruto` (e2e `recebimento.e2e-spec.ts:483-522` afere round-trip + linhas em `notas_fiscais_fornecedor_itens`); eventos `NF_FORNECEDOR_REGISTRADA` emitidos pós-commit (RA-04); auditoria do INSERT no mesmo escopo da tx (RA-02). **3 ajustes obrigatórios (2 deles regressões NOVAS introduzidas por `77d3cf6`):** (1) **Dado fiscal inventado — RA-05/06 + invariante 10 do plano (L39).** `derivarItensNfDoPedido` (`nota-fiscal-fornecedor.persistence.ts` L66-87) grava `quantidadeDeclarada: Number(pfi.quantidade_prevista)` — a quantidade do **pedido**, não a declarada na NF — e é o caminho usado por TODA NF vinda da UI (`persistirNfDeCamposUiNaTx` L207 só deriva porque `iniciar` L372-381 e `atualizarNfe` L444-456 nunca passam `itens`; o painel da tela captura apenas cabeçalho). Consequências verificadas: `qtd_nf` do quadro passa a ser estruturalmente igual a `qtd_pedido` (`conferencia.service.ts` L118-127 vs L152-153), logo a perna **Pedido × NF** da conferência tripla (D3) fica tautológica e uma NF realmente divergente nunca é detectada; `pesoDeclarado` fica `null` → `peso_nf` nulo e a comparação de peso silenciosamente desativada em `classificarSituacao`, de modo que o "Peso bruto NF" digitado nunca chega ao quadro; e `notas_fiscais_fornecedor_itens` — documento auditável referenciado por `conclusoes_conferencia_nfs` e por `divergencias_recebimento.nf_fornecedor_id`, base da ocorrência administrativa contra o fornecedor — passa a conter números que o fornecedor nunca declarou. Contradiz frontalmente o plano L39 ("NF … sem itens **não ganha valores inferidos** … exige carga explícita dos itens da NF") e torna inalcançável a guarda que o próprio PR implementa (`conferencia.service.ts` L248-249 `NF_ITENS_OBRIGATORIOS`), pois sempre haverá itens. Corrigir: persistir só o cabeçalho pelo idioma já existente (`payload_json` sem itens, deixando a guarda bloquear a conclusão até a carga real) ou capturar os itens da NF na UI — nunca inferir do pedido. (2) **Regressão em `registrarNf`: no máximo uma NF ativa por recebimento, com soft delete não auditado (RA-02).** `persistirNfEstruturadaNaTx` L161 chama `softDeleteNfAtiva` (L89-111), que marca `deleted_at` em TODAS as NFs ativas do recebimento **e** em seus itens antes de inserir a nova; como `registrarNf` (`pedido-fornecedor.service.ts` L193-200) agora delega a esse helper, registrar a 2ª nota do mesmo recebimento apaga a 1ª. Isso quebra invariantes do plano aprovado: L1839-1843 seleciona **todas** as NFs ativas do recebimento, L1853 grava o conjunto completo em `conclusoes_conferencia_nfs` ("preserva o conjunto completo", invariante 12/L41) e L1872 tem o ramo `nfs.length === 1 ? nfs[0].id : null`, que só existe porque N>1 NFs por recebimento é estado legítimo — todos passam a ser código morto. Impacto de negócio: carga com duas notas do fornecedor passa a contar só a última em `qtd_nf`/`peso_nf` (o CTE filtra `nf.deleted_at IS NULL`), gerando "falta" espúria e ocorrência administrativa falsa contra o fornecedor. Agrava: o soft delete de registro de negócio ocorre **sem** `auditoria.registrar` (só o INSERT é auditado), contra RA-02/convenções de schema. Nenhum teste pega: `pedido-fornecedor.e2e-spec.ts:147-172` registra 2 NFs em recebimentos **diferentes** (`r1`/`r2`) e conta sem filtrar `deletedAt`, seguindo verde mesmo se uma fosse apagada. Corrigir: restringir o soft delete ao caminho de correção do cabeçalho vindo da UI (ou substituir por atualização do cabeçalho existente), preservar N NFs em `registrarNf` e auditar qualquer soft delete. (3) **Resíduo do achado (2) anterior — `nfePesoLiquido` ainda é aceito e descartado (RA-05/06 + Princípio I).** Ambos os DTOs seguem aceitando o campo (`recebimento.dto.ts` L22 e L46), mas `mapearCamposNfParaRegistrar` L53 faz `pesoTotalDeclarado = dto.nfePesoBruto ?? dto.nfePesoLiquido` e `payload` só recebe `volumes` (L51) → com os dois preenchidos (caso normal da tela, campos lado a lado em `recebimento-carga-client.tsx` L1057-1066) o peso líquido não é gravado em lugar algum; `detalhar` L283-288 não retorna `nfePesoLiquido`, então o card L805 exibe "—" para sempre e o form L303 nunca recarrega o valor, apesar de `lib/operacao.ts:82` declarar o campo. Pior: preenchendo **só** o líquido, ele é gravado em `peso_total_declarado` e devolvido como `nfePesoBruto` (L287) → o valor aparece sob o rótulo errado ("Peso bruto NF"). O plano define o idioma correto — `0013_onda1_backfill.sql:74` preserva `nfe_peso_liquido` e `nfe_volumes` em `payload_json` (plano L2108) —, que o caminho novo ignora; nenhum teste cobre (o e2e L483-522 e o unit `2445aeb` não enviam/aferem `nfePesoLiquido`). Corrigir: gravar em `payload_json` e expor em `detalhar`, ou rejeitar o campo explicitamente e remover da tela/tipo. **Dívida registrada (não reprova):** (a) `nota-fiscal-fornecedor.persistence.ts` — 217 L com regras de negócio de NF (mapeamento, validação, soft delete) — fica **fora** do gate ACMR por arquivo, cujo glob é `app/backend/src/**/*.service.ts` (`scripts/check-coverage-lib.mjs:20`); a extração também elevou `pedido-fornecedor.service.ts` a 100%/100%, e é justamente nesse arquivo que mora o ramo não testado do achado (3) — cobertura total (98.42%/86.26%) segue como única rede; (b) `app/api/operacao/recebimentos/[id]/nf/route.ts` continua byte-equivalente a `[id]/nfe/route.ts` (só um comentário difere) e sem consumidor — a tela chama `/nfe`; a rota é autorizada pelo plano L111, então resta apenas DRY; (c) `lib/operacao.ts:83` declara `nfeVolumes: string \| null` mas o backend devolve `number` (de `payload_json.volumes`) ou omite a chave — divergência de contrato que o type-check não pega; (d) mantidas do veredito anterior: bloco `DO $$` do plano (Task 3 L1215-1228) ausente no `0012`; teste nomeado "journal aplica os três arquivos em ordem" ausente; `EXECUCAO-STATUS.md` em commit do Executor; `pushPermissoes()` no lugar do `.push` literal. Merge bloqueado até (1)-(3). |
| 2026-07-24T19:11:19-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `3dfd2ae9cb7d9b4f85305c87a2c18d5e3160ea5f`, run `30129219436`, plano sha256 `2d52f9df…`; comandos=`gh pr checks 13` (10/10 pass: 8 obrigatórios + Vercel/Vercel Preview Comments) + `gh pr view 13 --json headRefOid,state,mergeable` (OPEN/MERGEABLE, `headRefOid` == HEAD do worktree) + `sha256sum` do plano (bate) + `git status --porcelain` (limpa antes e depois) + `git diff --name-status 1a7a33c..3dfd2ae` (7 arquivos, só backend; 0 em `scripts/`, `drizzle/`, `app/frontend/`) + leitura integral de `nota-fiscal-fornecedor.persistence.ts` (301 L) + `conferencia.service.ts` L113-254 + `recebimento.service.ts` L340-481 + `grep` de todos os writers de `notasFiscaisFornecedorItens` no `src` (um só: `persistirNfEstruturadaNaTx`) + `pedido-fornecedor.controller.ts` (só `@Post(':id/nf')`, sem `@Delete`/`@Patch` de NF) + `recebimento-carga-client.tsx` L296-304/366-455/1034-1066/1122-1156 + `lib/operacao.ts` L81-83/149-151 + `conferencia-tripla.e2e-spec.ts` L60-134 + plano L39/L1795-1804 + greps `success:true`/`setInterval`/`@Cron`/hex/`marca`/segredo no diff novo (vazios) + **e2e temporário do Monitor** (3 casos A/B/C via `npx jest --runInBand` contra Postgres 18 local, saída em log; arquivo removido após a coleta, árvore limpa) | Oitava instância independente; CI 100% verde no HEAD auditado. **Os 3 achados do veredito anterior FECHARAM:** (1) `derivarItensNfDoPedido` eliminado — o caminho da UI sem itens grava só cabeçalho (`persistirNfCabecalhoUiNaTx` L198-270), `persistirNfEstruturadaNaTx` L159-161 segue exigindo itens e a guarda `NF_ITENS_OBRIGATORIOS` passou a valer para QUALQUER NF ativa com 0 itens (`conferencia.service.ts` L248); e2e `recebimento.e2e-spec.ts:484-524` afere `itensNf` vazio + marcadores no payload; nenhum número do pedido chega a `notas_fiscais_fornecedor_itens`. (2) `softDeleteNfAtiva` removido e `registrarNf` sempre INSERT; `pedido-fornecedor.e2e-spec.ts:116-176` agora registra as 2 NFs no MESMO recebimento e afere `nfs.length === 2` com `deletedAt === null` em todas — falharia se o soft delete voltasse; o UPDATE de cabeçalho é auditado no mesmo escopo da tx (L239-247, RA-02). (3) `pesoTotalDeclarado` só recebe `nfePesoBruto` (L65), `pesoLiquido` vai para `payload_json` (L47) e `detalhar` o expõe (`recebimento.service.ts` L288-290); e2e L527-556 afere round-trip 1800.5 com `nfePesoBruto` e `peso_total_declarado` nulos; 2 unit tests cobrem os dois ramos; a tela consome em L303/L805. **3 regressões NOVAS no caminho de cabeçalho criado pela própria correção — todas reproduzidas em runtime:** (A) **Impasse: recebimento nascido na tela nunca conclui a conferência.** `criarLote()` exige `nfeNumero` (client L371, botões L1122/L1129) e o envia no `POST /operacao/recebimentos` → `iniciar` L376-384 chama `persistirNfDeCamposUiNaTx` sem itens → 100% das cargas criadas pela tela ganham uma NF ativa com 0 itens; a guarda L241-254 itera TODAS as NFs ativas, logo uma só sem itens trava. Não há saída: o único writer de `notas_fiscais_fornecedor_itens` em todo o `src` é `persistirNfEstruturadaNaTx`, que sempre INSERTa uma NF nova (nunca preenche a existente), e nenhum endpoint apaga/cancela NF. Reproduzido: lote com `nfeNumero:'900100'` + carga explícita dos itens reais via `POST /pedidos-fornecedor/:id/nf` (mesmo número) + `/itens` + `/concluir` → `POST /conferencia/concluir` = **409 `{"code":"NF_ITENS_OBRIGATORIOS"}`** com **2 NFs ativas**. Mata a perna obrigatória Pedido×NF×Pesagem (D3) — razão de ser da onda — e tudo a jusante (ocorrência administrativa, impacto na disponibilidade). CI fica verde porque o único happy path de conclusão (`conferencia-tripla.e2e-spec.ts` L78-134) cria o recebimento com `{ pedidoFornecedorId }` puro, sem campos de NF, contornando a tela; e o novo teste L557-586 afere justamente o 409, congelando o impasse como comportamento desejado. Divergência do plano: L39 reserva o estado "sem itens" às NFs LEGADAS migradas pelo `0013` (`payload_json.migracao='legado_sem_itens_nf'`), e o código estampa esse mesmo marcador em NF digitada hoje (L217-220), promovendo exceção de migração a estado normal. Corrigir: a carga de itens deve consumir/completar o cabeçalho existente (upsert por `recebimento_id` + `numero`) ou a tela deve capturar os itens no mesmo ato; e o marcador `legado_sem_itens_nf` deve voltar a ser exclusivo do backfill. (B) **NF fantasma na correção de número (dupla contagem).** `buscarNfCabecalhoAtivaPorNumero` L113-132 casa por `numero`, então corrigir um dígito não encontra a anterior e INSERTa outra, deixando as duas ativas. Reproduzido: lote `'900201'` + `PATCH /nfe` `'900202'` → **2 NFs ativas**; `detalhar` mostra só a mais recente (`buscarNfAtivaDoRecebimento` L134-145 usa `limit 1`), então a fantasma é invisível na tela mas entra em `conclusoesConferenciaNfs` (L275-278) e, havendo itens, soma em `qtd_nf`/`peso_nf` no CTE (L118-127) → excesso espúrio e ocorrência falsa contra o fornecedor. É o resíduo do achado (2): o soft delete destrutivo saiu, mas nenhum caminho de correção/substituição de cabeçalho entrou. Sem teste. (C) **PATCH parcial do cabeçalho apaga dado fiscal em silêncio.** Em `persistirNfCabecalhoUiNaTx`, `pesoTotalDeclarado` vira `null` explícito quando `nfePesoBruto` é `undefined` (L226-228) e `payloadJson` é substituído por inteiro (L217-229), enquanto `serie`/`chave`/`dataEmissao` seguem semântica de patch (Drizzle descarta `undefined` no `.set`). Reproduzido: lote com bruto 1234.5 / líquido 1200 / volumes 7 e depois `PATCH /nfe {nfeNumero, nfeSerie}` → linha fica `peso=null, payload={migracao,cabecalho_sem_itens}`: bruto, líquido e volumes perdidos sem erro nem rastro de intenção. Alcançável pela folha de correção da tela (L1150-1156, que só expõe o número) sempre que `formNfe` não estiver hidratado. Corrigir: semântica única de patch para todos os campos + merge do `payload_json`, ou rejeitar update parcial. **Dívidas mantidas (não reprovam):** (a) `nota-fiscal-fornecedor.persistence.ts` cresceu a 301 L de regra de negócio e segue FORA do gate ACMR por arquivo (glob `**/*.service.ts`, `scripts/check-coverage-lib.mjs:20` inalterado) — exatamente onde moram (A)/(B)/(C); (b) `app/api/operacao/recebimentos/[id]/nf/route.ts` continua duplicando `[id]/nfe/route.ts` sem consumidor; (c) `lib/operacao.ts:83` declara `nfeVolumes: string \| null` mas o backend devolve `number` ou omite a chave (idem `nfePesoLiquido`, agora `string` ou ausente); (d) mantidas: bloco `DO $$` do plano (L1215-1228) ausente no `0012`; teste "journal aplica os três arquivos em ordem" ausente; `EXECUCAO-STATUS.md` em commit do Executor; `pushPermissoes()` no lugar do `.push` literal. Merge bloqueado até (A)-(C). |
| 2026-07-24T20:10:31-03:00 | 1 | 2 | ajustar | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `10ccb2b64a00141154d7c2d51ccd7cd25c36edfb`, run `30132036318`, plano sha256 `2d52f9df…`; comandos=`gh pr checks 13` (10/10 pass: 8 obrigatórios + Vercel/Vercel Preview Comments) + `gh run view 30132036318 --json headSha,conclusion,status` (`success`, `headSha` == HEAD) + `gh pr view 13 --json headRefOid,state,mergeable` (OPEN/MERGEABLE) + `gh repo view --json visibility,isPrivate` (PRIVATE) + `sha256sum` do plano (bate) + `git status --porcelain` (limpa antes e depois) + `git diff --name-status 3dfd2ae..HEAD` (6 arquivos: `ci.yml`, persistence, 3 testes, este arquivo) + `git log/show --stat 09d72ad b597508` + leitura integral de `nota-fiscal-fornecedor.persistence.ts` (437 L), `conferencia.service.ts` L108-254 (CTE do quadro + guarda), `recebimento.service.ts` L340-479 (`iniciar`/`atualizarNfe`), `nota-fiscal-fornecedor.persistence.spec.ts` (6 casos) e `recebimento.e2e-spec.ts` L484-727 + `rg` de consumidores de `pedidos-fornecedor`/`conferencia` no `app/frontend/src` + `npm audit` local (42: 4 moderate + 38 high) e `npm audit --omit=dev` (0) + `npm audit --json` (advisory GHSA-mh99 range `<=5.0.7`; instaladas 1.1.16/2.1.2/5.0.7; `fixAvailable`=eslint@10.8.0 semver-major) + `gh run view --log --job 89609566005` (ACMR: 22 services ≥80%; total 98.38%/86.13%) + greps `success:true`/`setInterval`/`@Cron`/hex/`marca`/segredo no diff novo (vazios) + **e2e temporário do Monitor** (`npx jest --runInBand` contra Postgres 18 local `:15437`; arquivo removido após a coleta, árvore limpa) | Nona instância independente; CI 100% verde no HEAD auditado, repo PRIVATE, HEAD do PR == HEAD do worktree, plano pinado sem drift. **Os 3 achados (A)/(B)/(C) do veredito anterior FECHARAM — verificados no código e nos testes, não no relato:** (A) `persistirNfEstruturadaNaTx` L289-296 passou a procurar cabeçalho órfão (`buscarCabecalhoParaCompletar` L180-188: por `recebimento_id`+`numero` e, na ausência, qualquer NF ativa do recebimento com 0 itens ativos) e, achando, chama `completarCabecalhoComItensNaTx` L219-269, que faz UPDATE do cabeçalho + INSERT dos itens + auditoria `UPDATE` no mesmo escopo da tx (RA-02) e limpa os marcadores via `mesclarPayloadNfCompleta`; o marcador `legado_sem_itens_nf` voltou a ser exclusivo do backfill (`mesclarPayloadNfCabecalho` L64 `delete merged.migracao`; e2e L519 afere `not.toHaveProperty('migracao')`). e2e L557-621 (lote → `PATCH /nfe` cabeçalho → `POST /pedidos-fornecedor/:id/nf` com itens → 1 NF ativa com 1 item, sem `cabecalho_sem_itens` → `conferencia/concluir` **201**) falharia se o impasse voltasse. (B) `persistirNfCabecalhoUiNaTx` L350 passou a casar por recebimento (não por número), então o PATCH faz UPDATE do cabeçalho existente; e2e L623-654 prova 1 NF ativa com o número corrigido após dois PATCHes. (C) `montarPatchCabecalhoUi` L198-202 preserva `pesoTotalDeclarado` quando `nfePesoBruto` vem `undefined` e `mesclarPayloadNfCabecalho` L51-66 faz merge do `payload_json`; e2e L656-695 prova bruto 3200/líquido 3000/volumes 45 sobrevivendo a um PATCH parcial `{numero, serie}` + 2 unit tests dos dois ramos do merge. Mock de `pedido-fornecedor-branches.spec.ts` alinhado ao novo select (2 `chainSelect([])` para o ramo sem órfão) — o `override` de `brace-expansion` de `09d72ad` foi integralmente revertido por `b597508` (nem `package.json` nem `package-lock.json` aparecem no diff líquido `3dfd2ae..HEAD`). **1 ajuste obrigatório (resíduo de (A)+(B) numa terceira posição de partida, reproduzido em runtime):** (1) **Corrigir o número da NF depois que ela já tem itens cria NF fantasma e trava a conferência de novo (RA-05/06 + D3).** `buscarNfCabecalhoAtivaPorRecebimento` L161-178 só devolve NF com **0 itens ativos**; logo, quando a NF do recebimento já foi completada com itens, `persistirNfCabecalhoUiNaTx` não encontra nada e cai no INSERT L392, criando uma segunda NF ativa sem itens — com **200 OK**, sem aviso. Reproduzido: lote `nfeNumero:'910100'` → `POST /pedidos-fornecedor/:id/nf` com itens (201; 1 NF `910100` com 1 item) → `PATCH /operacao/recebimentos/:id/nfe {nfeNumero:'910101'}` = **200** e estado `[{910100, itens:1},{910101, itens:0}]` → `POST /conferencia/concluir` = **409 `{"code":"NF_ITENS_OBRIGATORIOS"}`**. A fantasma é invisível na tela (`buscarNfAtivaDoRecebimento` L206-217 usa `limit 1` e devolve a mais recente, justamente a vazia), não há endpoint que apague/cancele NF, e a única saída — recarregar os itens na fantasma — leva ao outro erro: o CTE `nf_itens` (`conferencia.service.ts` L118-127) agrupa por `recebimento_id`+`item_comercial_id` somando **todas** as NFs ativas, então `qtd_nf` dobra (20 vs `qtd_pedido` 10) → excesso espúrio, divergência e ocorrência administrativa falsa contra o fornecedor. Caminho alcançável: a folha de correção da tela (`recebimento-carga-client.tsx` L1150-1156) envia só o número, e o writer de itens tem BFF exigido pelo plano (L111, `app/api/operacao/pedidos-fornecedor/[id]/nf/route.ts`). Não é regressão nova (em `3dfd2ae` o casamento por número produzia a mesma fantasma), mas é o mesmo defeito que (A)/(B) deveriam ter fechado, agora na única ordem de operações que os e2e novos não exercitam (todos corrigem o número **antes** da carga de itens). Corrigir: o PATCH de cabeçalho deve atualizar a NF ativa mesmo quando ela já tem itens (semântica única de correção) **ou** recusar explicitamente (409 nomeado) o ajuste de número de NF já estruturada — nunca gerar em silêncio um documento fiscal vazio; e cobrir com e2e na ordem inversa (itens → correção de número). **1 formalização obrigatória (mérito técnico aprovado):** (2) **`npm audit --omit=dev` é aceitável como dívida consciente, mas não pode ficar só no `ci.yml`.** Verificado de forma independente: a árvore completa tem 42 vulnerabilidades (4 moderate + 38 high) e **as 38 high são 100% cadeia de desenvolvimento** (`brace-expansion`→`minimatch` em eslint/typescript-eslint/jest/glob/test-exclude/fork-ts-checker + `ejs`→`jake` em ts-jest); `npm audit --omit=dev` → **0 vulnerabilities**, isto é, a árvore que vai a produção está limpa. A justificativa do commit confere: o advisory GHSA-mh99-v99m-4gvg tem range `<=5.0.7` (não há patch nas linhas 1.x/2.x instaladas — 1.1.16 e 2.1.2 — e o `fixAvailable` do npm é `eslint@10.8.0`, semver-major), e o override global para 5.0.8 quebra `minimatch`. **Porém** `quality-gates.md:29` continua dizendo, sem qualificação, "`npm audit` sem vulnerabilidades **high** ou **critical** (monorepo backend/frontend e pacote independente da landing)": o PR estreita um gate transversal sem AD e sem atualizar o texto do gate, criando ainda assimetria com o audit da landing (que segue completo). Precedente vinculante nesta mesma onda: AD-07 exigiu decisão registrada para alteração de governança dentro do PR da onda. Corrigir: registrar AD-08 em `DECISOES.md` (escopo = árvore de produção, motivo = ausência de patch upstream, revisão quando `brace-expansion` ≥5.0.8 alcançar as cadeias 1.x/2.x) e alinhar `quality-gates.md:29`; recomendado manter um `npm audit` completo **não bloqueante** para não perder visibilidade da cadeia dev. **Dívidas registradas (não reprovam):** (a) `nota-fiscal-fornecedor.persistence.ts` chegou a 437 L de regra de negócio de NF e segue **fora** do gate ACMR por arquivo (glob `app/backend/src/**/*.service.ts`, `scripts/check-coverage-lib.mjs:20` inalterado) — é exatamente onde mora o achado (1); (b) **Princípio II:** nenhuma tela consome `POST /api/operacao/pedidos-fornecedor/[id]/nf` nem `/conferencia/concluir` (`rg` em `app/frontend/src` fora de `src/app/api` = 0 chamadas; `/carga/conferencia` é a conferência de **expedição**), logo a perna Pedido×NF×Pesagem só é alcançável via BFF/integração — completude aceita no nível de BFF pelos vereditos de `6711c3c`/`2445aeb`, mas a captura de itens da NF em tela precisa de onda/AD própria; (c) `buscarCabecalhoParaCompletar` usa fallback "qualquer órfão do recebimento", então uma carga de itens com número diferente **renumera** o cabeçalho existente (inclusive um legado migrado pelo `0013`) em vez de criar NF nova — audita o UPDATE, mas silencia a troca de número; (d) `completarCabecalhoComItensNaTx` não trava a linha (`SELECT … FOR UPDATE` ausente): dois `registrarNf` concorrentes podem completar o mesmo órfão e duplicar itens; (e) `app/api/operacao/recebimentos/[id]/nf/route.ts` continua duplicando `[id]/nfe/route.ts` sem consumidor; (f) `lib/operacao.ts:83` declara `nfeVolumes: string \| null` mas o backend devolve `number` ou omite a chave; (g) mantidas: bloco `DO $$` do plano (L1215-1228) ausente no `0012`; teste "journal aplica os três arquivos em ordem" ausente; `EXECUCAO-STATUS.md` em commit do Executor; `pushPermissoes()` no lugar do `.push` literal. Merge bloqueado até (1)-(2). |
| 2026-07-24T20:29:05-03:00 | 1 | 2 | aprovado | PR [#13](https://github.com/sammuka/alpha-carnes/pull/13), head `22d3f51335c18491fac558b3f6b0f6acc8e99b53`, run `30133164601`, plano sha256 `2d52f9df…`; comandos=`gh pr checks 13` (10/10 pass: 8 obrigatórios + Vercel/Vercel Preview Comments) + `gh run view 30133164601 --json headSha,conclusion,status` (`success`, `headSha` == HEAD) + `gh pr view 13 --json headRefOid,state,mergeable` (OPEN/MERGEABLE, `headRefOid` == HEAD do worktree) + `gh repo view --json visibility,isPrivate` (PRIVATE) + `sha256sum` do plano (bate) + `git status --porcelain` (limpa antes e depois) + `git diff --name-status 10ccb2b..HEAD` (5 arquivos: persistence, `recebimento.e2e-spec.ts`, `DECISOES.md`, `quality-gates.md`, este arquivo) + leitura integral de `nota-fiscal-fornecedor.persistence.ts` (458 L) e de `conferencia.service.ts` L100-270 + `rg` de leitores de `cabecalho_sem_itens`/`legado_sem_itens_nf`/`NF_ITENS_OBRIGATORIOS` em todo o repo + `.github/workflows/ci.yml` (job `audit` L126-142) + `gh run view --log --job 89612719129` (ACMR: 22 services ≥80% linhas e branches) + `rg` de `@RequirePermissoes` (44) e dos casos 403 por suíte + greps `success:true`/`setInterval`/`@Cron`/hex/`marca`/segredo no diff novo (só o texto do veredito anterior casa) + **e2e temporário do Monitor** (4 casos M1–M4 via `npx jest --runInBand` contra Postgres 18 local `:15437`; arquivo removido após a coleta, árvore limpa) | Décima instância independente; CI 100% verde no HEAD auditado, repo PRIVATE, `headRefOid` do PR == HEAD do worktree, plano pinado sem drift. **Os 2 achados do veredito anterior FECHARAM — verificados em runtime, não no relato:** (1) **fantasma pós-itens eliminado.** `buscarNfParaAtualizarCabecalhoUi` (persistence L180-200) varre as NFs ativas do recebimento em `createdAt DESC` e **prefere a que tem itens**, caindo na órfã só quando nenhuma tem — o INSERT L414 ficou inalcançável havendo qualquer NF ativa. Reproduzido pelo Monitor na ordem exata do veredito anterior (M1: lote pela tela com `nfeNumero:'930100'` + série + bruto 1500,5 + líquido 1400,25 + volumes 12 → `POST /pedidos-fornecedor/:id/nf` com itens (201) → `PATCH /operacao/recebimentos/:id/nfe {nfeNumero:'930101'}` (200)): estado final **1 NF ativa** `930101` com 1 item, `detalhar` devolvendo número/série/bruto/líquido/volumes íntegros e `POST /conferencia/concluir` = **201** com `quadroJson` `qtdNf=10.000 / qtdPedido=10.000 / qtdApurada=10.000 / situacao=conforme` (sem dupla contagem no CTE). M2 (itens registrados com número `930999` divergente do cabeçalho `930200` e depois `PATCH` para `930201`) também fecha em 1 NF e conferência 201. O e2e do worker (`recebimento.e2e-spec.ts:656-727`, "PATCH nfe corrige numero depois dos itens sem NF fantasma") afere 1 NF ativa + 1 item + 201 e falharia se a fantasma voltasse. (2) **AD-08 registrado e gate alinhado.** `DECISOES.md` traz AD-08 (escopo = árvore de produção do monorepo; motivo = ausência de patch 1.x/2.x para GHSA-mh99 e quebra do minimatch com override 5.0.8; revisão quando houver patch ou major seguro; landing mantém audit completo) e `quality-gates.md:29` foi reescrito com a mesma qualificação e link para a AD; `ci.yml` L138/L141 confere literalmente (`npm audit --omit=dev --audit-level=high` no monorepo, `npm audit --audit-level=high` na landing). **Regressões (A)/(B)/(C) reconfirmadas intactas em runtime:** (A) recebimento nascido na tela com `nfeNumero` conclui a conferência (M1/M2 = 201; marcador `legado_sem_itens_nf` segue exclusivo do backfill — `mesclarPayloadNfCabecalho` L64); (B) correção de número não duplica NF (M1/M2/M3 sempre 1 NF ativa; o `PATCH` faz UPDATE auditado no mesmo escopo da tx, RA-02); (C) PATCH parcial preserva dado fiscal (M3: lote com bruto 3200/líquido 3000/volumes 45 + `PATCH {numero, serie}` → `peso_total_declarado=3200.000`, `payload={volumes:45,pesoLiquido:3000}`, `detalhar` devolve os três). M4 confirma que N NFs com itens por recebimento continuam ativas (6+4) e somam `qtdNf=10` no quadro, sem soft delete. Reconfirmados sem regressão: ACMR 22/22 services ≥80% linhas e branches; `@RequirePermissoes` em todas as rotas novas com 403 real por chave; migrations/BFF/frontend intocados neste incremento; nenhum `success:true`, `setInterval`, `@Cron`, hex fora do DS, rótulo banido ou segredo no diff. **Aprovado — Executor pode fazer squash-merge e atualizar `EXECUCAO-STATUS.md`.** Ressalva de processo: este commit de veredito altera o HEAD do PR; como a diferença é doc-only, o squash deve ocorrer com CI reexecutado no novo HEAD pelo rito de lease PUBLIC→PRIVATE. **Dívidas aceitas conscientemente (com fase de resolução):** (h) **NOVA** — `persistirNfCabecalhoUiNaTx` L376-380 chama `mesclarPayloadNfCabecalho(..., true)` incondicionalmente, então o `PATCH` de cabeçalho carimba `cabecalho_sem_itens: true` no `payload_json` de uma NF **que tem itens** (observado em M1: `{volumes:12, pesoLiquido:1400.25, cabecalho_sem_itens:true}` com `itens:1`; idem M2/M4). Sem impacto funcional — `rg` em todo o repo mostra que o marcador **não tem leitor** (a guarda `NF_ITENS_OBRIGATORIOS` conta linhas de `notas_fiscais_fornecedor_itens` e o CTE do quadro idem) —, mas é atributo falso em documento fiscal auditável e contradiz o próprio `mesclarPayloadNfCompleta` L73. Resolver na Onda 2 (manutenção do módulo NF): `marcarCabecalhoSemItens = (itensAtivos === 0)`; (a) `nota-fiscal-fornecedor.persistence.ts` (458 L de regra de negócio de NF) segue **fora** do gate ACMR por arquivo (glob `app/backend/src/**/*.service.ts`, `scripts/check-coverage-lib.mjs:20`) — resolver junto com (h), estendendo o glob; (b) **Princípio II:** nenhuma tela consome `POST /api/operacao/pedidos-fornecedor/[id]/nf` nem `/conferencia/concluir` — completude aceita no nível de BFF pelos vereditos de `6711c3c`/`2445aeb`; a captura dos itens da NF em tela exige onda/AD própria (Onda 2); (c) `buscarCabecalhoParaCompletar` renumera o cabeçalho órfão quando a carga de itens vem com número diferente — audita o UPDATE, mas silencia a troca (Onda 2); (d) `completarCabecalhoComItensNaTx` sem `SELECT … FOR UPDATE`: dois `registrarNf` concorrentes podem completar o mesmo órfão (Onda 2); (e) `app/api/operacao/recebimentos/[id]/nf/route.ts` duplica `[id]/nfe/route.ts` sem consumidor — remover na Onda 2; (f) `lib/operacao.ts:83` declara `nfeVolumes: string \| null` mas o backend devolve `number` — alinhar contrato na Onda 2; (g) mantidas: bloco `DO $$` do plano (Task 3 L1215-1228) ausente no `0012` (o índice ainda aborta alto, sem escolha silenciosa) e teste nomeado "journal aplica os três arquivos em ordem" ausente (substância coberta por `migrarAte('0014_onda1_contract')`) — resolver na próxima migração estrutural; `EXECUCAO-STATUS.md` em commit do Executor e `pushPermissoes()` no lugar do `.push` literal permanecem equivalências aceitas. |
| 2026-07-25T05:25:35-03:00 | 2 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-25-onda2-shell-ds.md`; sha256=`9fcd8c4c7ac7952c662ef024a177fd02ed76b7c5c1e7d139c1132c3e4b789548` (revalidado, bate com o pin do Planejador); worktree=`.worktrees/onda1-impl` branch `feature/onda2-plano-shell-ds` HEAD `68357cb`, árvore limpa; PR [#15](https://github.com/sammuka/alpha-carnes/pull/15) docs-only (2 arquivos), 10/10 checks SUCCESS (run `30150803857`); comandos=`sha256sum`+`git branch/log/status`; leitura integral do plano (2337 L), constituição, roadmap §8, quality-gates §"Onda 2 — Shell + DS", plano mestre §7 (P1–P15), matriz de rastreabilidade (41 linhas + transversais), `DECISOES.md` (AD-01..AD-08), `EXECUCAO-STATUS.md`; protótipo `feature/completude-v1.1` (`Layout.tsx` ALL_NAV_GROUPS/PROFILES/NavGroup/getBreadcrumb, `Login.tsx`, `PipelineBar.tsx`, `TrocaPeca.tsx` STEP_TITULOS, `──PaletaDeCores──`, `src/imports/01-login.png`+`02-dashboard.png`); app real (`menu-v2.ts`, `breadcrumb-v2.ts`, `app-sidebar.tsx`, `admin-header.tsx`, `(admin)/layout.tsx`, `(auth)/login/*`, `status-pill/kpi-card/alert-item/placeholder-page/button`, `globals.css`, `lib/auth.ts`, `middleware.ts`, jest/tsconfig/playwright configs, `package.json` de raiz/backend/frontend, `permissoes.ts`); `rg` de hex/rgba/CSS/simulador em `app/frontend/src`; verificação de `page.tsx` para as 39 rotas; `tsc --strict --noUncheckedIndexedAccess` sobre repro dos trechos literais das Tasks 3 e 5 | Pré-condições OK: Onda 1 `mergeada` (squash `231f6843`), sha256 confere, PR docs-only verde. Muito do plano está sólido e foi verificado contra a fonte: seção "Referências do protótipo" mapeia cada artefato ao `.tsx` de origem (Princípio I.3); os 9 grupos e 39 itens batem literalmente com `ALL_NAV_GROUPS` (5+6+3+3+3+3+4+8+4 = 39, igual à matriz L78) e o breadcrumb continua derivado de `MENU_V2`; as 14 cores canônicas conferem com `──PaletaDeCores──` e todos os tokens novos têm origem rastreada; `STEP_TITULOS`, as 4 etapas do `PipelineBar` e a microcopy do `Login.tsx` conferem palavra a palavra; catálogo do badge contém exatamente P1/P3/P5–P12/P15 com as referências do mestre §7 e exclui P2/P4/P13/P14 (AD-03..AD-06) — Princípio VIII respeitado; nenhuma pendência aberta vira regra fixa; zero `TBD`/`TODO`/"a definir"/"implementar depois"/"similar à Task"; zero rótulo banido; mapa DoD→teste 1:1 cobre os 5 bullets da DoD da Onda 2; escopo não vaza para Ondas 3–10 (os dois `PlaceholderPage` são justificados pela decisão 6 / RA-05); a tabela de visibilidade por perfil da decisão 11 foi recalculada contra `MAPA_PERFIL_PERMISSOES` e fecha nos 11 perfis, inclusive `gestor`→ADMINISTRAÇÃO só com `/admin/auditoria`. **10 ajustes obrigatórios:** (1) **Invariante de hex impossível de fechar (Task 1, decisões 4 e 22, DoD linha 3).** O plano afirma que "5 arquivos hoje têm hex e todos são tocados nesta onda", mas `rg` mostra 6: falta `src/components/ui/chart.tsx:58`, que traz `#ccc` e `#fff` dentro de string literal (seletores recharts do Shadcn). O teste `nenhum literal hexadecimal de cor em src fora de globals.css` varre `src` inteiro por AST de string literal e falhará nesse arquivo; a decisão 4 proíbe exceção por path e a decisão 22 proíbe editar componentes `ui` fora do escopo — contradição que trava o Worker. Corrigir: incluir `chart.tsx` no escopo com tratamento explícito, ou redefinir o invariante (ex.: ignorar hex fora de contexto de cor) com o critério escrito no plano. (2) **Task 5 quebra `npm run type-check`.** `contadores?.[CHAVES_CONTADOR[index]]` (pipeline-bar.tsx) com `noUncheckedIndexedAccess: true` (`tsconfig.base.json`) gera `TS2538: Type 'undefined' cannot be used as an index type` — reproduzido com `tsc --strict --noUncheckedIndexedAccess`. Reescrever o trecho literal (ex.: `const chave = CHAVES_CONTADOR[index]; const contador = chave ? contadores?.[chave] : undefined`). (3) **Task 3 — `menu-rbac.test.ts` não compila.** `PERMISSOES_POR_PERFIL[perfil]` e `PERMISSOES_POR_PERFIL.conferente` são `string[] \| undefined` sob `noUncheckedIndexedAccess` e são passados a `filtrarMenuPorPermissoes(permissoes: string[])` → `TS2345` (reproduzido); `tsconfig.test.json` herda as flags e o ts-jest falha na compilação, não no assert. (4) **Task 2 — caso de teste ambíguo por construção.** Em `admin-header.test.tsx`, com `pathname='/gestao/dashboard'` e `user.perfil='Gestão'`, `formatMenuGroupTitle('GESTÃO')` devolve exatamente `Gestão`, logo `screen.getByText('Gestão')` casa dois nós (breadcrumb e meta "Perfil") e lança. Usar consulta desambiguada ou fixture de perfil distinta. (5) **Task 8 — comando inexistente.** O bloco de execução do e2e manda `cd app/backend && … && npm run start:dev`, mas `app/backend/package.json` só expõe `build/start:prod/lint/type-check/test/test:cov/db:generate/db:migrate/db:seed`. Substituir pelo caminho real (ex.: serviço `backend` do `docker-compose.yml`) — o Worker não pode improvisar. (6) **Consistência cruzada com o Portão 2 da Onda 1 (veredito `22d3f51`, linha anterior deste arquivo): sete dívidas foram aceitas com "resolver na Onda 2" e nenhuma aparece no plano** — (h) `marcarCabecalhoSemItens = (itensAtivos === 0)`, (a) estender o glob do gate ACMR para além de `**/*.service.ts`, (b) captura dos itens da NF em tela, (c) renumeração silenciosa do cabeçalho órfão, (d) `SELECT … FOR UPDATE` em `completarCabecalhoComItensNaTx`, (e) remoção da rota BFF duplicada `[id]/nf`, (f) contrato `nfeVolumes`. O plano ainda proíbe explicitamente qualquer alteração de backend fora do script de snapshot, o que as torna órfãs. Absorver no escopo ou redirecionar formalmente (AD-xx / nota na linha da onda de destino). (7) **Linhas da matriz atribuídas à Onda 2 sem tratamento:** linha 41 (`/admin/auditoria` — "alinhar filtros/visual ao protótipo **na Onda 2**") não está no plano nem consta como diferida (o roadmap põe Admin na Onda 3 — a reconciliação precisa ser escrita); linha 2 (redirect `/` → `/gestao/dashboard`, classificada Divergente) também não é tratada nem diferida, e hoje não existe `app/frontend/src/app/page.tsx`. (8) **Efeitos do gate de grupo não declarados (Princípio II + RA-05).** Com `permissoesGrupo`, `expedicao` deixa de enxergar `/cadastros/caminhoes` e `/cadastros/motoristas` (matriz linhas 33/34 listam `expedicao`) e `diretoria` deixa de enxergar `/gestao/dashboard` e `/gestao/relatorios` (matriz linhas 8/13 listam `diretoria`); a decisão 11 só declara as divergências de `gestor`, `diretoria`, `conferente` e `logistica`. Some-se a isso o destino fixo pós-login: `login-form-client.tsx` empurra todos para `/gestao/dashboard`, rota que — depois desta onda — fica fora do menu de 7 dos 11 perfis (e `conferente`/`logistica` chegam com a sidebar em estado vazio). A decisão 12 evita a sidebar muda, mas não a rota morta. Declarar item a item e definir o destino pós-login (ou registrar a decisão de mantê-lo). (9) **Mudanças de permissão fora da lista de decisões fixadas:** além das citadas na decisão 14, `menu-v2.ts` troca `/cadastros/rotas` de `EXPEDICAO_GERENCIAR\|CLIENTES_LER` para `ROTAS_LER\|ROTAS_GERENCIAR` e remove `DISPONIBILIDADE_LER` de `/desossa/dashboard`. São mudanças de RBAC de UI e precisam de decisão numerada para serem auditáveis no Portão 2. (10) **Login — cor de ação do protótipo perdida (Princípio I.1).** O plano cria `--color-action-blue` (#2563EB) declarando origem "Login.tsx botão/checkbox", mas mantém `<Button className="h-12 w-full">` na variante default, que é `bg-primary` = `#3B7FD4` com hover `#265389` (`button.tsx:12`), enquanto o protótipo usa `#2563EB` com hover `#1844B8`. Aplicar o token ou declarar a divergência de cor entre as divergências autorizadas da decisão 20. **Sem ressalvas — reprovado para ajuste.** Avaliação dos pontos de foco pedidos: Princípio I (referências por tela) OK; Princípio II OK no recorte declarado, ressalvados (6)-(8); zero TBD/TODO OK; `visibleGroups` por `permissoesGrupo` é coerente e testável (a tabela da decisão 11 confere com o catálogo real) mas incompleto na declaração de efeitos (8); escopo sem vazamento de Ondas 3–10; divergências do login (sem foto de CDN, sem credencial pré-preenchida, sem "Esqueci a senha"/"Lembrar minhas credenciais", sem rodapé de protótipo, chip por variável) são **aceitáveis** e bem fundamentadas em RA-05/on-premises — o único ponto do login a corrigir é (10). |
| 2026-07-25T06:06:12-03:00 | 2 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-25-onda2-shell-ds.md`; sha256=`b98396727fbbd757646e815f3f85bae72d0b719d3d5b1411ba3dc3c3380cf0a9` (revalidado, bate com o pin do Planejador); worktree=`.worktrees/onda1-impl` branch `feature/onda2-plano-shell-ds` HEAD `f507f87` (empty-commit de CI sobre a correção `a3ed93f`; `git diff a3ed93f..HEAD` vazio — conteúdo do plano intacto), árvore limpa antes e depois; PR [#15](https://github.com/sammuka/alpha-carnes/pull/15) docs-only (3 arquivos), 10/10 checks pass (run `30151796603`, `headRefOid` == HEAD do worktree), OPEN/MERGEABLE; comandos=`sha256sum` + `git log/diff/status` + leitura integral do plano (2652 L) + `rg` de hex/rgba/`*.css`/simulador em `app/frontend/src` + `rg -o` do hex de `chart.tsx` (3×`#ccc` + 2×`#fff`, todos em `[stroke='…']`, na ordem exata do inventário pinado) + leitura de `globals.css`, `menu-v2.ts`, `admin-header.tsx`, `nav-group.tsx`, `status-pill.tsx`, `kpi-card.tsx`, `alert-item.tsx`, `lib/auth.ts`, `jest.config.cjs`, `package.json` (raiz/backend/frontend), `docker-compose.yml`, `health.controller.ts`, `main.ts`, `permissoes.ts` + **script `tsx` do Monitor** recalculando a visibilidade antiga × nova dos 39 itens para os 11 perfis a partir de `MAPA_PERFIL_PERMISSOES` real + **teste jsdom temporário do Monitor** no harness do projeto medindo `toBeVisible()` sob `max-height:0` (ambos removidos após a coleta) + protótipo `feature/completude-v1.1` (`Layout.tsx` rótulos e `getBreadcrumb`, `Login.tsx` microcopy/`#2563EB`/`#1844B8`, `PipelineBar.tsx`, `TrocaPeca.tsx` `STEP_TITULOS`) + matriz de rastreabilidade (41 linhas), quality-gates §"Onda 2 — Shell + DS", roadmap §8, `EXECUCAO-STATUS.md`, `DECISOES.md` (AD-01..AD-08) | Segunda instância do Portão 1 da Onda 2 (recheck do veredito `4cbf6e9`). Pré-condições OK: Onda 1 `mergeada` (squash `231f6843`), sha256 do plano bate com o pin, PR docs-only verde. **8 dos 10 achados FECHARAM — verificados na fonte, não no relato:** (1) decisão 23 fixa o critério **sintático e global** (remove `[atributo='#hex']` antes de varrer) e pina o inventário; conferi que `chart.tsx` tem exatamente 5 hex, todos em seletor de atributo, na mesma ordem do array esperado, que o arquivo fica intacto (decisão 22) e que qualquer nova ocorrência falha o teste — a contradição decisão 4 × decisão 22 desapareceu. (2) Task 5 estreita a chave antes de indexar (`const chave = CHAVES_CONTADOR[index]`) — TS2538 eliminado. (3) Task 3 acessa o snapshot e as tabelas fixadas por `permissoesDe`/`gruposEsperadosDe`/`rotaEsperadaDe` com falha explícita — TS2345 eliminado. (4) `admin-header.test.tsx` usa fixture `Administrador` + `within(breadcrumb)`; o `<nav aria-label="Breadcrumb">` já existe no componente e a Task 2 remove o `MetaInline` de "Escopo". (5) Task 8 troca o inexistente `start:dev` pelo serviço `backend` do `docker-compose.yml` (`4001:3001`) com `curl http://localhost:4001/health`; confirmei o serviço, o `HealthController` devolvendo `{"status":"ok"}` e a ausência de `setGlobalPrefix` em `main.ts`. (6) as sete dívidas do Portão 2 da Onda 1 têm destino nomeado, uma a uma, na seção "Dívidas herdadas da Onda 1" + decisão 28, com obrigação processual de entrarem no mapa DoD→teste da Onda 6 — que roadmap §8 e `EXECUCAO-STATUS` definem como "Recebimento & Balança", módulo dono das sete; nenhuma fica órfã e o plano proíbe executá-las aqui. (7) decisão 27 difere a linha 41 da matriz para a Onda 3 (coerente com o roadmap, que aloca Admin à Onda 3) mantendo teste de guarda da visibilidade de `/admin/auditoria` para `administrador`/`gestor`/`diretoria`; decisão 26 trata a linha 2 criando `(admin)/page.tsx` e movendo a decisão de destino para o servidor. (9) decisão 24 registra `/cadastros/rotas` → `ROTAS_LER`/`ROTAS_GERENCIAR` e `/desossa/dashboard` sem `DISPONIBILIDADE_LER`. (10) decisão 29 adiciona a variante `acao` (`bg-action-blue` + `hover:bg-action-blue-strong`), o login passa a usá-la e o teste afere as classes — bate com `Login.tsx:110` do protótipo (`bg-[#2563EB] hover:bg-[#1844B8]`). Reconfirmados sem regressão: as 14 cores canônicas existem em `globals.css` no formato literal `--token: #HEX;` que o teste procura e `--color-sidebar-gradient-start/end` já existem; `globals.css` é a única folha de estilo de `src`; zero resíduo de simulador; `rgba` só em `activity-item.tsx` (tokenizado na Task 1); 9 grupos e 39 itens com rótulos/rotas idênticos a `ALL_NAV_GROUPS`, breadcrumb "Gestão / Painel Geral da Operação", microcopy do login, 4 etapas do `PipelineBar` e `STEP_TITULOS` idênticos ao protótipo; catálogo do badge com P1/P3/P5–P12/P15 e sem P2/P4/P13/P14; tabela de grupos da decisão 11 e tabela de `rotaDeEntrada` da decisão 26 recalculadas contra o catálogo real — batem nos 11 perfis; `StatusPill`/`KpiCard`/`AlertItem` já passam nos testes propostos sem alteração; `getMe` devolve `{sub,nome,perfis,permissoes}` como o plano assume; zero `TBD`/`TODO`/"a definir"/"implementar depois"/"similar à Task"; zero rótulo banido; nenhuma pendência aberta virou regra fixa; escopo sem vazamento das Ondas 3–10. **2 ajustes obrigatórios:** (1) **O achado (8) NÃO fechou: a decisão 25 declara um conjunto fechado ("a introdução de `permissoesGrupo` retira estes itens do menu") que é factualmente incompleto.** Recalculei os 39 itens × 11 perfis com `MAPA_PERFIL_PERMISSOES` real (menu antigo do arquivo × menu novo do plano). Além dos 4 casos declarados (`expedicao`: Caminhões e Motoristas; `diretoria`: Painel Geral e Relatórios) e dos efeitos já cobertos pelas decisões 11/13/14/24, o gate de grupo retira **7 itens que a matriz de rastreabilidade atribui ao próprio perfil**: `faturamento` perde `/gestao/relatorios` (linha 13 lista `faturamento` como perfil **primário**, não consulta), `/comercial/clientes` (linha 3, consulta) e `/recebimento/recebimento-carga` (linha 14, consulta); `recebimento_pesagem` perde `/gestao/aprovacoes` (linha 12, **"registro: recebimento_pesagem"** — a fila onde esse perfil é o registrador); `compras` perde `/recebimento/recebimento-carga` (linha 14, consulta); `comercial` perde `/gestao/compras` (linha 10, consulta) e `/gestao/overbooking` (linha 11, consulta). O caso `faturamento` × `/gestao/relatorios` é a mesma classe de `diretoria` × `/gestao/relatorios`, que o veredito anterior obrigou a declarar — e é mais grave por ser atribuição primária. Agrava: o teste `efeitos declarados do gate de grupo conferem com o catalogo` (mapa DoD linha 32) afere só os 4 casos declarados, então nada no gate captura os 7 restantes. Corrigir: completar a decisão 25 com os 7 itens (motivo + linha da matriz + onda de reconciliação) e estender o teste ao conjunto completo, **ou** ajustar `permissoesGrupo` para preservar as atribuições da matriz. Contagem para conferência (itens visíveis antes → depois): `comercial` 18→5, `recebimento_pesagem` 18→3, `corte` 16→3, `expedicao` 18→3, `faturamento` 19→4, `compras` 15→14, `diretoria` 14→5, `conferente` 10→0, `logistica` 12→0, `gestor` 34→36, `administrador` 37→39. (2) **NOVO (autossuficiência/executabilidade): `nav-group.test.tsx` contradiz a implementação literal da própria Task 2 e ficará vermelho.** O novo `NavGroup` mantém os itens **montados** e colapsa por `style={{ maxHeight: open ? alturaItens : 0 }}` + `overflow-hidden` (plano L952-961), mas o caso `colapsa e expande o grupo ao clicar no cabecalho` afirma `expect(screen.getByRole('link', { name: 'Compras' })).not.toBeVisible()` (L650). O `toBeVisible()` do jest-dom só considera `display`, `visibility`, `opacity`, o atributo `hidden` e `<details>`; `max-height`/`overflow` não entram e o Tailwind não é compilado no jsdom. Reproduzido no harness do projeto (`jest.config.cjs`, jsdom): um `<a>` dentro de `<div className="overflow-hidden" style={{ maxHeight: 0 }}>` **passa** em `toBeVisible()`. Logo o caso permanece vermelho após a implementação correta e o Worker é instruído a parar e reportar (cabeçalho do plano). Corrigir: aferir o estado colapsado pelo contrato observável (`aria-expanded="false"` no cabeçalho mais `toHaveStyle({ maxHeight: '0px' })` ou `hidden`/`aria-hidden` no contêiner), ou desmontar os itens quando fechado, deixando a asserção de visibilidade real para o e2e do Playwright. **Observações (não reprovam):** (a) o mapa DoD linha 26 fala em "12 arquivos novos" de `__tests__`, mas a "Estrutura de arquivos" lista 14 novos + `login.test.tsx` estendido — alinhar o número; (b) Task 8 espera o heading `Dashboard Operacional` em `/gestao/dashboard` enquanto menu e breadcrumb desta onda passam a dizer "Painel Geral da Operação" (o `h1` está em `dashboard-client.tsx:193` e a fidelidade da tela é da Onda 5) — registrar a divergência no README de evidências; (c) a nota na linha da Onda 6 de `EXECUCAO-STATUS.md` com o destino das sete dívidas ainda não existe — ato do Executor, exigido pela decisão 28. **Sem ressalvas — reprovado para ajuste.** |
| 2026-07-25T06:39:08-03:00 | 2 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-25-onda2-shell-ds.md`; sha256=`6a47d5c034f74273e1f5bdbb560f0115be284a1e4668bd1924da3f480373b4db` (revalidado por `sha256sum` na árvore de trabalho — bate com o pin do Planejador); worktree=`.worktrees/onda1-impl` branch `feature/onda2-plano-shell-ds` HEAD `8933864` (empty-commit de CI sobre a correção `7bec485`; `git diff 7bec485..origin/feature/onda2-plano-shell-ds` vazio — conteúdo do plano intacto), árvore limpa antes e depois; PR [#15](https://github.com/sammuka/alpha-carnes/pull/15) docs-only (3 arquivos), 10/10 checks pass (run `30152841639`, `headRefOid` == HEAD do worktree), OPEN/MERGEABLE; comandos=`sha256sum` + `git log/diff/status/fetch/merge --ff-only` + `gh pr view/checks 15` + leitura integral do plano (2960 L) + **script `tsx` do Monitor** reimplementando `MENU_V2`/`filtrarMenuPorPermissoes`/`rotaDeEntrada` do plano e recalculando, a partir do `MAPA_PERFIL_PERMISSOES` real, os grupos visíveis, as perdas, os extras e a rota de entrada dos **11 perfis × 39 itens** contra a matriz transcrita + **3 testes jsdom temporários do Monitor** no harness do projeto (`jest.config.cjs`) medindo a serialização de `max-height` e rodando os casos propostos de `StatusPill`/`KpiCard`/`AlertItem` (todos removidos após a coleta; árvore limpa) + conferência linha a linha da coluna "Perfis RBAC" da matriz (41 linhas) contra `MATRIZ_RASTREABILIDADE` do plano + `rg` de hex/rgba/`*.css`/simulador/rótulo banido em `app/frontend/src` + leitura de `permissoes.ts`, `globals.css`, `status-pill.tsx`, `button.tsx`, `dialog.tsx`, `placeholder-page.tsx`, `login-form-shell.tsx`, `login-form-client.tsx`, `alpha-logo.tsx`, `lib/auth.ts`, `jest.config.cjs`, `tsconfig.base/json/test.json`, `package.json` (backend/frontend), `docker-compose.yml`, `.env.example` + verificação das âncoras de linha citadas (`dashboard-client.tsx:193`, `telas-reais.spec.ts:102`, `telas-migradas.spec.ts:130`, `jornada-operacional.spec.ts:441/442/447`, `app-sidebar.tsx:103`, `chart.tsx:58`) + matriz de rastreabilidade, quality-gates §"Onda 2 — Shell + DS", roadmap §8, `EXECUCAO-STATUS.md`, `DECISOES.md` (AD-01..AD-08) | Terceira instância do Portão 1 da Onda 2 (recheck do veredito `9b34d6c`). Pré-condições OK: Onda 1 `mergeada` (squash `231f6843`), sha256 bate com o pin, PR docs-only verde, HEAD do PR == HEAD do worktree. **O achado (1) do veredito anterior FECHOU, e fechou por recálculo independente, não por relato.** Reimplementei o menu do plano num script `tsx` e recalculei tudo a partir do catálogo real: (i) a coluna "Perfis RBAC" das 39 linhas de rota da matriz está transcrita **fielmente** em `MATRIZ_RASTREABILIDADE` (conferi linha a linha, incluindo os papéis "consulta"/"registro"); (ii) as perdas somam **exatamente 26** e batem, perfil a perfil, com a tabela da decisão 25 (`compras` 1, `comercial` 3, `recebimento_pesagem` 4, `expedicao` 7, `conferente` 1, `faturamento` 3, `logistica` 3, `diretoria` 4; `administrador`/`gestor`/`corte` zero); (iii) **nenhuma** perda vem do filtro de item — todas são efeito do gate de grupo, como o invariante do mapa DoD linha 32 afirma; (iv) a subafirmação "em 15 delas o perfil também não teria a permissão do item" confere (15/26); (v) os extras da decisão 31 batem (`compras` 11, `diretoria` 3, zero nos demais); (vi) os grupos da decisão 11 e as 11 rotas de entrada da decisão 26 batem item a item. Os 7 itens que o veredito anterior cobrou estão resolvidos como o plano declara: `faturamento`×`/gestao/relatorios` e `compras`×`/gestao/overbooking` foram **restaurados** pela decisão 30 (`FATURAMENTO_GERENCIAR` no gate de `GESTÃO` + estreitamento de `/gestao/dashboard` para `COMPRAS_PROGRAMADAS_LER`/`PEDIDOS_LER`, que confirmei não tirar o item de `administrador`/`gestor`/`compras` nem dá-lo a `faturamento`; `COMPRAS_PROGRAMADAS_GERENCIAR` no item de overbooking), e os outros 5 (`faturamento`×clientes/pedidos/recebimento-carga, `recebimento_pesagem`×aprovações, `compras`×recebimento-carga, `comercial`×compras/overbooking/desossa) estão declarados um a um na tabela com papel na matriz, grupo invisível, motivo e destino Onda 3. Contagem final de itens visíveis: `administrador` 39, `gestor` 36, `compras` 15, `comercial` 5, `recebimento_pesagem` 3, `corte` 3, `expedicao` 3, `faturamento` 5, `diretoria` 5, `conferente` 0, `logistica` 0 — coerente com a projeção do veredito anterior mais os dois itens restaurados. As três observações anteriores também fecharam: (a) o mapa DoD linha 26 agora diz **14** arquivos novos e lista os 14, batendo com a "Estrutura de arquivos"; (b) a decisão 5 alinha a microcopy — `<h1>` de `dashboard-client.tsx:193`, item de menu, breadcrumb e os 5 pontos dos specs Playwright existentes, cujas âncoras de linha conferi uma a uma (`telas-reais.spec.ts:102`, `telas-migradas.spec.ts:130`, `jornada-operacional.spec.ts:441/442/447`) — e o e2e da Task 8 já espera `Painel Geral da Operação`; (c) `EXECUCAO-STATUS.md` traz a nota do destino das sete dívidas na linha da Onda 6 e o espelho das 26 perdas na linha da Onda 3. Reconfirmados sem regressão: 6 arquivos com hex em `src` (5 de cor aplicada, todos tocados; `chart.tsx:58` com exatamente 5 ocorrências em `[stroke='#ccc']`×3 + `[stroke='#fff']`×2, na ordem do inventário pinado); `globals.css` é a única folha de estilo; `rgba` só em `activity-item.tsx`; zero `TBD`/`TODO`/"a definir"/"implementar depois"/"similar à Task"; zero rótulo banido (as 4 ocorrências de "marca" são o verbo "marca" em nomes de teste); catálogo do badge com P1/P3/P5–P12/P15 e sem P2/P4/P13/P14; nenhuma pendência aberta virada regra; escopo sem vazamento das Ondas 3–10; `button.tsx` sem `bg-primary` na base do `cva` (a asserção `not.toContain('bg-primary')` do login é válida); `Dialog*`, `PlaceholderPage({title})`, `AlphaLogo({priority})`, `LoginFormShell`, `getMe(): {sub,nome,perfis,permissoes}`, `typescript` e `tsx` como devDependencies, `db:seed`/`e2e:jornada` como âncoras dos scripts novos, serviço `backend` em `4001:3001` e `NEXT_PUBLIC_API_URL` no `.env.example` — todos existem como o plano assume; **rodei os casos propostos de `StatusPill` (6 variantes), `KpiCard` (3) e `AlertItem` (2) contra os componentes reais no harness do projeto: 10/10 PASS sem alteração de componente.** **1 ajuste obrigatório:** (1) **O achado (2) mudou de forma mas não fechou: `nav-group.test.tsx` continua vermelho depois da implementação literal da Task 2 — agora por unidade, não por `toBeVisible()`.** O `not.toBeVisible()` saiu (bem) e o caso passou a aferir o mecanismo real (`aria-expanded`, `aria-controls`, `data-state`, `max-height` inline), mas o componente literal do plano (L1057) escreve `style={{ maxHeight: open ? alturaItens : 0 }}` — **número** — e o teste (L706) exige `expect(painel.style.maxHeight).toBe('0px')`. React serializa `0` como `'0'` (`dangerousStyleValue` só concatena `px` quando `value !== 0`) e o `cssstyle` do jsdom não implementa `max-height` com parser de medida, guardando o valor cru. Reproduzido no harness do projeto: aberto `style.maxHeight === "76px"` (passa), fechado `style.maxHeight === "0"`, `getPropertyValue('max-height') === "0"`, `cssText === "max-height: 0;"` → o caso `colapsa e expande o grupo ao clicar no cabecalho` falha em `Expected "0px" / Received "0"`, e o Worker é instruído a parar e reportar (cabeçalho do plano). **Correção do meu veredito anterior:** a saída que sugeri (`toHaveStyle({ maxHeight: '0px' })`) **também falha** sobre o valor numérico — verifiquei; não usar. O que verifiquei funcionar nos dois estados é emitir a unidade no próprio componente: `style={{ maxHeight: open ? \`${alturaItens}px\` : '0px' }}` (aberto `"76px"`, fechado `"0px"`, ambos PASS), o que mantém as três asserções do plano intactas e não muda o comportamento no browser; alternativa aceitável é o teste afirmar `.toBe('0')`, mas ela amarra o caso a um detalhe de serialização do React. O caso Playwright equivalente (`toHaveCSS('max-height','0px')`) está correto — `getComputedStyle` do Chromium normaliza — e não precisa mudar. **Observação (não reprova):** a matriz diz "recortes p/ demais" na linha 8 e "consulta geral" na linha 9 sem nomear perfis; o plano trata a coluna como lista dos donos do fluxo (decisão 31) e não conta essas duas expressões como atribuição — interpretação coerente e declarada, mas convém uma frase explícita na decisão 25 dizendo que expressões não-nominais da coluna não entram no cálculo, para o Portão 2 não reabrir a conta. **Sem ressalvas — reprovado para ajuste.** |
| 2026-07-25T06:45:38-03:00 | 2 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-25-onda2-shell-ds.md`; sha256=`a153af0d9878803640b21b1142bb9c969054212545979973c69e02ecebc92532` (revalidado por `sha256sum` na árvore de trabalho — bate com o pin do Planejador); worktree=`.worktrees/onda1-impl` branch `feature/onda2-plano-shell-ds` HEAD `fa6a8db` (empty-commit de CI sobre a correção `84e7666`; `git diff 7bec485..HEAD` do plano = **4 inserções / 2 remoções**, restritas ao comentário do teste de colapso e ao literal do `NavGroup` — nenhuma outra região do plano mudou desde a revisão anterior); árvore limpa antes e depois; PR [#15](https://github.com/sammuka/alpha-carnes/pull/15) **não mergeado**; comandos=`sha256sum` + `git branch/log/show/diff/status` + leitura das regiões alteradas (plano L677-738 e L1000-1070) e do checklist do skill + `rg` de proibidos (`TBD`, `TODO`, "a definir", "implementar depois", "similar à Task", "parcial", "mínimo", "MVP", "fase 2") e do rótulo banido em todo o plano + **teste jsdom temporário do Monitor** no harness real do projeto (`app/frontend/jest.config.cjs`, jsdom + ts-jest, binário hoisted da raiz) comparando os dois literais lado a lado (removido após a coleta; árvore limpa) + protótipo `feature/completude-v1.1` (`Layout.tsx` L279-322) + `quality-gates.md` §"Onda 2 — Shell + DS" + roadmap §8 + `EXECUCAO-STATUS.md` (Onda 1 `mergeada` squash `231f6843`, Onda 2 `aguardando_portao1` com a nota da revisão 4) | Quarta instância do Portão 1 da Onda 2 (recheck do veredito `a52a6fa`). Pré-condições OK: sha256 bate com o pin, dependência (Onda 1) mergeada, correção cirúrgica sem drift em nenhuma outra seção. **O achado B FECHOU — verificado em runtime, não no relato.** Rodei no harness real do projeto um caso com os DOIS literais: com a correção da revisão 4 (`style={{ maxHeight: open ? \`${alturaItens}px\` : '0px' }}`) o painel serializa `"76px"` aberto e `"0px"` fechado, e as três asserções do plano (`toBe('76px')` → `toBe('0px')` → `toBe('76px')`, L703/708/713) passam nos três estados, junto de `aria-expanded` e `data-state`; com o literal anterior (numérico) o fechado serializa `"0"` e o caso falha — confirmando que a unidade era a causa e que a correção é suficiente. Sem regressão no restante do mecanismo: o `not.toBeVisible()` continua ausente (zero ocorrências no plano), `aria-controls`/`id`/`data-state` seguem no componente e no helper `painelDe`, o caso Playwright equivalente (`toHaveCSS('max-height','0px')`, L2772) continua correto e agora trivialmente satisfeito, e o comentário do teste (L682-684) documenta por que a unidade é obrigatória. Reconferido também que `alturaItens = items.length * 36 + 4` é idêntico ao `itemsHeight` do protótipo (`Layout.tsx:291`), logo `76px` para 2 itens está certo. Achado A (RBAC/decisão 25) não reaberto: a matriz, as decisões 25/30/31 e o mapa DoD→teste não foram tocados por `84e7666`. Checklist do skill reconferido sem novos problemas: zero `TBD`/`TODO`/"a definir"/"implementar depois"/"similar à Task"; zero "parcial"/"mínimo"/"MVP"/"fase 2" (Princípio II); zero rótulo banido (as ocorrências de "marca" são o verbo em nomes de teste e "marcação"/`marcarCabecalhoSemItens` na tabela de dívidas); seção "Referências do protótipo" íntegra; 9 Tasks numeradas com código literal (134 blocos de código); estrutura de arquivos, 31 decisões e 36 invariantes do mapa DoD→teste inalterados; escopo sem vazamento das Ondas 3–10. **1 ajuste obrigatório (NOVO — Princípio I + autoconsistência do plano):** (1) **A duração da animação de colapso do literal diverge do protótipo e do próprio texto do plano.** O protótipo aplica `transition: "max-height 220ms cubic-bezier(0.4, 0, 0.2, 1)"` no painel de itens (`Layout.tsx:313`) e reserva `duration-200` ao chevron (`:303`) — são valores deliberadamente distintos. O plano cita **220ms** três vezes como o comportamento a portar (L142 na tabela de Referências do protótipo, L678 no comentário do teste e L1000 no enunciado da Task 2), mas o literal do `NavGroup` (L1058) escreve `transition-[max-height] duration-200 ease-[cubic-bezier(0.4,0,0.2,1)]` = **200ms**, e o teste (L723) pina esse valor com `expect(painel.className).toContain('duration-200')`. Consequências: (i) o Worker implementará 200ms, divergindo do protótipo sem que a divergência esteja declarada em nenhuma das 31 decisões nem na lista de divergências autorizadas do template de PR (Princípio I é não-negociável e a fidelidade do easing/timing é o que a própria Referência manda portar); (ii) se alguém corrigir o componente para 220ms seguindo a prosa, o teste fica vermelho — a mesma classe de contradição literal×teste do achado B, apenas na direção inversa. Corrigir no plano (uma linha no componente e uma no teste): usar `duration-[220ms]` no `className` do painel — mantendo `duration-200` no chevron, como o protótipo — e alinhar a asserção para `toContain('duration-[220ms]')`; alternativa aceitável é declarar explicitamente a divergência de 20ms como decisão numerada, com o motivo. O `ease-[cubic-bezier(0.4,0,0.2,1)]`, o `overflow-hidden`, a altura por item (36px + 4px) e o `-rotate-90` do chevron já conferem com o protótipo e não mudam. **Observação (não reprova, mantida do veredito anterior):** a decisão 25 continua sem a frase explícita de que expressões não-nominais da coluna "Perfis RBAC" da matriz ("recortes p/ demais", linha 8; "consulta geral", linha 9) não entram no cálculo das 26 perdas — a interpretação é coerente e está declarada na decisão 31, mas convém fixá-la para o Portão 2 não reabrir a conta. **Sem ressalvas — reprovado para ajuste.** |
| 2026-07-25T06:53:10-03:00 | 2 | 1 | aprovado | plano=`docs/superpowers/plans/2026-07-25-onda2-shell-ds.md`; sha256=`9a9f8b36a67cf5e3fe9c0cde9d3e636174973d153ef175ae2fab9229d14f02bd` (**confere com o pin**); worktree=`F:/Projetos/AlphaCarnes/.worktrees/onda1-impl`, branch `feature/onda2-plano-shell-ds`, HEAD=`dbd2f9e9e71e5fc6c70aefd4182875459a6b87a0`, árvore limpa; run=`gate1-onda2-rev5-20260725-dbd2f9e`; comandos=`sha256sum` do plano; `git show dbd2f9e` (diff da correção); leitura integral das 2961 linhas do plano; `rg` por `duration-`/`220`/`transitionDuration`/`maxHeight`, por proibidos (`TBD`/`TODO`/`a definir`/`implementar depois`/`similar à Task`/`fase 2`/`posteriormente`) e pelo rótulo banido; conferência no protótipo `F:\Projetos\alpha-carnes-prototipo` (`feature/completude-v1.1`, `8d32aa4c`) de `src/app/components/Layout.tsx:280-324` e `src/app/pages/Login.tsx`; conferência cruzada com `docs/superpowers/plans/2026-07-22-matriz-rastreabilidade-v1.1.md` (linhas 3, 11, 13, 24, 28, 29, 41), `docs/governance/quality-gates.md` §"Onda 2 — Shell + DS", `roadmap-canonico.md` §8, `DECISOES.md` (AD-01..AD-08) e `EXECUCAO-STATUS.md` | Terceiro Monitor independente (rev5). **Achado único do veredito `a3691a9` (duração do colapso) FECHADO, com fidelidade verificada no protótipo:** `Layout.tsx:313` aplica `transition: "max-height 220ms cubic-bezier(0.4, 0, 0.2, 1)"` no painel e `:303` reserva `duration-200` ao chevron — o plano agora escreve exatamente isso (`transition-[max-height] duration-[220ms] ease-[cubic-bezier(0.4,0,0.2,1)]` em L1058; chevron `transition-transform duration-200` em L1051) e o teste pina `toContain('duration-[220ms]')` (L723). Prosa coerente nas três citações de 220ms (L142, L678, L1000) e **zero** referência residual a 200ms para o painel (`rg duration-` devolve só L723/L1051/L1058). **Correção cirúrgica sem regressão:** `git show dbd2f9e` altera 4 linhas em 2 arquivos (2 no plano + a frase da decisão 25 + a linha da Onda 2 em EXECUCAO-STATUS) e nada mais; o mecanismo do colapso fechado na rev4 segue íntegro — `maxHeight` com unidade (`'0px'`/`` `${alturaItens}px` ``, L1059), `aria-expanded`/`aria-controls`/`id`/`data-state`, helper `painelDe`, asserções `toBe('76px')→toBe('0px')→toBe('76px')` (L703/708/713), zero `not.toBeVisible()`. O caso Playwright (L2756-2773) não depende do timing: `toHaveCSS('max-height','0px')` tem auto-retry, logo os 220ms não o tornam flaky. Reconferido contra o protótipo que `alturaItens = items.length * 36 + 4` é idêntico a `itemsHeight` (`Layout.tsx:291`), que o chevron fecha com `-rotate-90` (`:304`) e que item 34px/13px, sidebar 256px gradiente `#1E3A5F→#1B4E9B` e header 56px (`:322`, `:334`, `:364`, `:442`) batem com o plano. **Observação do veredito anterior também FECHADA:** a decisão 25 (L74) passa a declarar que expressões não-nominais da coluna "Perfis RBAC" ("recortes p/ demais", "consulta geral") não entram no cálculo — e a frase é **verdadeira e completa**: `rg` na matriz encontra exatamente essas duas ocorrências (linhas 8 e 9) e a transcrição do teste já as omitia (`'/gestao/dashboard': ['gestor','diretoria','administrador']`, L1251; `'/gestao/operacoes': ['gestor','compras','administrador']`, L1252), de modo que prosa e fixture agora dizem a mesma coisa. Contagem reconferida nos dois lados: a tabela da decisão 25 soma 26 linhas (1 `compras` + 3 `comercial` + 4 `recebimento_pesagem` + 7 `expedicao` + 1 `conferente` + 3 `faturamento` + 3 `logistica` + 4 `diretoria`) e `PERDAS_DECLARADAS` (L1288-1318) soma os mesmos 26, com caso dedicado `a decisao 25 declara exatamente 26 perdas` (L1483) e invariante de que toda perda vem do gate de grupo (L1491). Transcrição da matriz auditada por amostragem contra o arquivo-fonte (linhas 3, 13, 24, 28, 41) — fiel, inclusive nos papéis secundários "consulta". **Checklist completo do skill, sem novos achados:** Princípio I — seção "Referências do protótipo" mapeia cada tela/artefato ao `.tsx` de origem e os 33 tokens novos têm hex rastreado à paleta canônica (CTA `#2563EB`/hover `#1844B8` conferidos em `Login.tsx:110`, microcopy e placeholder em `:65`, `:66`, `:76`); Princípio II — nenhum item "parcial/mínimo/MVP/fase 2"; os diferimentos são reconciliações formais numeradas com onda de destino (25→Onda 3, 27→Onda 3, 28→Onda 6); Princípio VIII — pendências abertas só via `BadgeProvisorio`, catálogo tipado sem P2/P4/P13/P14, nenhuma pendência fixada como regra; Princípio IX — zero rótulo banido (as ocorrências de "marca" são o verbo em nomes de teste); proibidos textuais — zero `TBD`/`TODO` em caixa alta, `a definir`, `implementar depois`, `similar à Task`; autossuficiência — Goal/Architecture/Tech Stack, 31 decisões fixadas, estrutura de arquivos, mapa DoD→teste com 36 invariantes 1:1 cobrindo os 5 bullets de `quality-gates.md` §"Onda 2", 9 Tasks com código literal e comandos com saída esperada (inclusive o serviço `backend` do compose e as variáveis do `webServer` do Playwright); escopo — bate com o roadmap §8 (dependência Onda 1 `mergeada` em EXECUCAO-STATUS), sem vazamento das Ondas 3–10; decisões AD-01..AD-08 respeitadas, nenhuma reaberta. **Aprovado sem ressalvas.** O Executor pode atualizar `EXECUCAO-STATUS.md` para `plano_aprovado` e disparar `/disparar-onda onda2`. PR #15 **não** mergeado por este Monitor. |
| 2026-07-25T08:12:44-03:00 | 2 | 2 | ajustar | PR [#16](https://github.com/sammuka/alpha-carnes/pull/16), head auditado `e160321c4e1f4463d7eb14a9310faab370785473`, run `30155114508`, plano sha256 `9a9f8b36a67cf5e3fe9c0cde9d3e636174973d153ef175ae2fab9229d14f02bd`; comandos=`gh pr checks 16` (10/10 pass: lint, type-check, test-backend, coverage, test-frontend, build, audit, secret-scan + Vercel/Vercel Preview Comments) + `gh run view 30155114508 --json headSha,conclusion` (`success`, `headSha` == head auditado) + `gh pr view 16 --json headRefOid,state,mergeable` (OPEN/MERGEABLE, `headRefOid` == `e160321`) + `gh repo view --json visibility,isPrivate` (PRIVATE) + `sha256sum` do plano (bate com o pin) + `git status --porcelain` (limpa antes e depois) + `git diff --name-status origin/develop...e160321` (52 arquivos) + `git diff e160321..HEAD` (só `EXECUCAO-STATUS.md`, commit do Executor `42374be`) + **execução local da suíte Jest do frontend** (`npm test` → 24 suítes / 152 casos PASS) + **teste de snapshot RBAC do backend** (`npx jest test/unit/perfil-permissoes-snapshot.spec.ts` → 2 PASS) + **3 mutações de verificação do Monitor** (rótulo `Relatórios & SIF`, `FATURAMENTO_GERENCIAR` fora do gate de `GESTÃO`, etapa do `PipelineBar`, hex `#AABBCC` em `nav-item.tsx`) com reversão e árvore limpa + `rg` de hex/`rgba`/`SIMULAR PERFIL`/`PROFILE_ORDER`/rótulo banido em `app/frontend/src` + `git diff … \| rg` de `setInterval`/`setTimeout`/`@Cron`/`success: true`/segredos + leitura integral de `menu-v2.ts`, `breadcrumb-v2.ts`, `perfis.ts`, `nav-group.tsx`, `nav-item.tsx`, `app-sidebar.tsx`, `admin-header.tsx`, `(admin)/layout.tsx`, `(admin)/page.tsx`, `badge-provisorio.tsx`, `pipeline-bar.tsx`, `troca-peca-modal.tsx`, `(auth)/login/{page,login-form-client}.tsx`, `globals.css` e dos 8 arquivos de teste centrais + protótipo `F:\Projetos\alpha-carnes-prototipo` `feature/completude-v1.1` (`8d32aa4c`): `Layout.tsx` (`ALL_NAV_GROUPS` L133-217, `NavGroup` L275-340, `<aside>`/`<header>` L361-470, `getBreadcrumb` L222-262), `Login.tsx`, `PipelineBar.tsx`, `TrocaPeca.tsx` (`STEP_TITULOS` L87-94) + `sha256sum` das evidências × `alpha-carnes-prototipo/src/imports/*.png` + leitura de `src/imports/index.html` + `docs/superpowers/plans/2026-07-22-matriz-rastreabilidade-v1.1.md` (linhas 3, 13, 14, 17, 24, 29, 33, 41), `quality-gates.md` §"Onda 2 — Shell + DS", `EXECUCAO-STATUS.md` | Primeiro Monitor do Portão 2 da Onda 2. Pré-condição OK: Portão 1 `aprovado` para a Onda 2 (veredito `061a2d5`, linha anterior deste arquivo) com o mesmo sha256 do plano; CI 100% verde no head auditado; repo PRIVATE; `headRefOid` do PR == head auditado; o commit local `42374be` do Executor é doc-only e ficou fora do diff auditado, como instruído. **A maior parte do objeto foi verificada executando e confere:** (i) **Princípio I** — `MENU_V2` reproduz `ALL_NAV_GROUPS` do protótipo item a item: 9 grupos na ordem canônica e 39 rotas/rótulos idênticos (conferi as 39 linhas contra `Layout.tsx` L133-217, incluindo `Painel Geral da Operação`, `Operações`, `Pendências de Overbooking`, `Aprovações & Ocorrências`, `Relatórios & SIF`); `NavGroup` porta o mecanismo real (`alturaItens = items.length * 36 + 4` = `itemsHeight` L291, `max-height` com unidade, `transition-[max-height] duration-[220ms] ease-[cubic-bezier(0.4,0,0.2,1)]` = L313, chevron `duration-200` + `-rotate-90` = L303/304, `defaultOpen` equivalente a `isActiveGroup \|\| visibleGroups.length <= 3`); sidebar 256px com gradiente `#1E3A5F→#1B4E9B` por token e item 34px/13px; `PipelineBar` é porte 1:1 (4 etapas, contadores, `CheckCircle2`, separador `ChevronRight`, `bg-white/20` no contador da etapa atual); `TrocaPecaModal` reproduz o chrome (`ArrowLeftRight` + "Trocar Peça", "Passo N de 6 · <título>" com os 6 `STEP_TITULOS` literais, barra de 6 segmentos, `Voltar`/`Avançar`/`Confirmar Troca`, painel de sucesso com nova etiqueta/histórico/`Concluir`) sem portar `PEDIDOS_TROCA`/`PECAS_DISPONIVEIS` nem regra de negócio; login fiel (45%/55%, painel `#1F2633` com gradiente, "Sistema Integrado", headline e subtítulo literais, "Bem-vindo de volta", campos h-12, CTA "Acessar Sistema" na variante `acao` = `#2563EB`/hover `#1844B8` de `Login.tsx:110`) com as 5 divergências da decisão 20 aplicadas exatamente como autorizadas — screenshot `01-login.png` confirma. (ii) **Tokens/hex** — `globals.css` ganhou 33 tokens com origem rastreada e continua a única folha de estilo de `src`; `rg` independente encontra hex em **um** arquivo (`chart.tsx:58`, 3×`#ccc` + 2×`#fff`, todos em `[stroke='…']`, exatamente o inventário pinado da decisão 23) e **zero** `rgba(` fora de `globals.css`; injetei `text-[#AABBCC]` em `nav-item.tsx` e o caso `nenhum literal hexadecimal de cor em src fora de globals.css` ficou vermelho, provando que o gate morde. (iii) **RBAC real** — zero resíduo de `SIMULAR PERFIL`/`PROFILE_ORDER`/`activeProfile`; a fixture do frontend é o snapshot gerado do backend (`perfil-permissoes.snapshot.json`) guardado nos dois lados, e `permissoes.ts` está **intacto** (`git diff` vazio); `menu-rbac.test.ts` transcreve as 39 linhas da matriz (auditei por amostragem as linhas 3, 13, 14, 17, 24, 29, 33 e 41 contra o arquivo-fonte — fiéis, inclusive nos papéis "consulta"/"registro") e afere grupos, rota de entrada, as 26 perdas da decisão 25, os extras da decisão 31, o invariante de que toda perda vem do gate de grupo e a guarda da linha 41; removi `FATURAMENTO_GERENCIAR` do gate de `GESTÃO` e os casos ficaram vermelhos. (iv) **RA-01..06** — nenhuma regra de negócio nova no frontend (só navegação); nenhum endpoint, migration, permissão ou perfil novo; a única mudança de backend é a autorizada pelo plano (script `rbac:snapshot` + JSON + teste); zero `setInterval`/`setTimeout`/`@Cron` e zero `success: true` no diff; rota morta eliminada (`/` resolve `rotaDeEntrada` no servidor, os 2 itens novos do menu ganharam `page.tsx`, e "nenhum grupo liberado" vira aviso explícito em vez de 404/tela branca); nenhum dado inventado na identidade (chip "Escopo" removido, `perfis` formatado pelos rótulos canônicos). (v) **Segurança** — sem segredos no diff (o único literal é o placeholder `change-me-admin-password` do e2e, com `secret-scan` verde), sem migration, sem `DELETE`/`DROP`. (vi) Suíte completa reexecutada localmente: 24/24 suítes e 152/152 casos PASS; os 36 invariantes do mapa DoD→teste têm teste nomeado existente e aferindo o comportamento, não só o título. **2 ajustes obrigatórios:** (1) **DoD "screenshot de shell comparado ao protótipo" (`quality-gates.md:259`, invariante 27 do plano) não está cumprido: o par de referência não é o protótipo.** `docs/evidencias/onda2-shell/referencia-prototipo/01-login-prototipo.png` e `02-shell-prototipo.png` são cópias byte-idênticas (`sha256` `bdffafca…` e `21a0d4c5…`) de `alpha-carnes-prototipo/src/imports/01-login.png` e `02-dashboard.png` — que **não são artefatos de design do protótipo**: `src/imports/index.html` (o gerador que acompanha esses PNGs no mesmo diretório e commit `361f4228` "Add files from Figma Make") se declara **"AlphaCarnes - Jornada E2E Visual … Relatório gerado pela automação Playwright"**, com `Run ID 20260610191240`, UUIDs de banco e URLs `http://localhost:3100/login` e `http://localhost:3100/` — ou seja, são screenshots antigos **da própria aplicação Next.js**, versionados por engano dentro do protótipo. A prova visual está no próprio arquivo: `02-shell-prototipo.png` mostra um menu de **4 grupos** (`CADASTROS` com "Itens de Compra"/"Itens Comerciais", `COMERCIAL`, `OPERAÇÃO`, `ADMINISTRAÇÃO`) e o `<h1>` **"Dashboard Operacional"** — estrutura que não existe em `Layout.tsx` (9 grupos/39 itens) e microcopy que esta própria onda remove (decisão 5). Consequências: o único controle de fidelidade por imagem da onda compara app(hoje) × app(jun/2026) e não prova nada sobre o protótipo; o `README.md` das evidências afirma no título e na tabela que o par é "app × protótipo" e nomeia o diretório `referencia-prototipo/`, o que é **procedência falsa em artefato auditável** (RA-06) e fica como baseline herdado pelas próximas ondas — quem abrir a pasta verá um shell de 4 grupos como "o protótipo" e poderá regredir a tela. Não é desvio do Worker: o plano L150 pina nominalmente esses dois arquivos; o erro nasceu no plano e o Portão 1 não o pegou. Corrigir: capturar a referência do protótipo de verdade (subir o Vite de `alpha-carnes-prototipo` em `feature/completude-v1.1` e fotografar `/gestao/dashboard` e `/login`, mesmo viewport 1280×800) **ou**, se a captura não for viável no ambiente, remover o par falso, apontar a comparação para os arquivos-fonte (`Layout.tsx`, `Login.tsx`, `──PaletaDeCores──`) e dizer isso no README — nunca manter um screenshot da própria aplicação rotulado como protótipo. Registrar também, em `docs_v2`/`DECISOES.md` ou no README, que `alpha-carnes-prototipo/src/imports/0*.png` é galeria de evidência da app e **não** fonte de verdade de UI, para não contaminar as Ondas 3–10. (2) **Princípio I — o breadcrumb de `/cadastros/fornecedores` diverge do protótipo e a divergência não está declarada.** A decisão 7 mantém o breadcrumb derivado de `MENU_V2` ("corrigir rótulo/rota no menu corrige o breadcrumb por construção"), o que pressupõe rótulo de menu == item do breadcrumb nas 39 rotas. Comparei as 39 linhas de `getBreadcrumb` (`Layout.tsx:222-262`) com os rótulos de `MENU_V2`: **38 batem e 1 não** — o protótipo mostra `Cadastros & Regras / Fornecedores` (`:253`) enquanto o menu diz `Fornecedores / Frigoríficos` (`:283` de `ALL_NAV_GROUPS`), logo o app renderiza `Cadastros & Regras / Fornecedores / Frigoríficos`. Nenhum teste pega (`admin-header.test.tsx` afere outra rota) e a divergência não consta das divergências autorizadas do plano nem do README de evidências. Corrigir: declarar formalmente a divergência (decisão numerada + linha no README/PR, mantendo a derivação única da decisão 7) ou reconciliar rótulo × breadcrumb — não deixar silenciosa numa onda cuja razão de ser é o Princípio I. **Observações e dívidas registradas (não reprovam):** (a) `app-sidebar.tsx:134` faz `ICON_MAP[item.iconKey] ?? LayoutDashboard` — `iconKey` desconhecido rende ícone plausível porém errado, sem sinal (RA-05 em grau leve); tipar `iconKey` como união derivada de `ICON_MAP` resolve em compilação; resolver na Onda 3, junto do primeiro item de menu novo; (b) o item inativo da sidebar usa `--color-sidebar-text` = `rgba(255,255,255,0.92)` enquanto o protótipo usa `rgba(255,255,255,0.68)` (`Layout.tsx:322`) — o literal veio do plano (L1100) e o token é pré-existente (decisão 1 proíbe rename), mas é diferença visível; alinhar quando o token for revisitado; (c) a "Estrutura de arquivos" do plano marca `app/(admin)/page.tsx` como NOVO e a decisão 26 afirma que "hoje `/` não tem `page.tsx` (404)" — o arquivo já existia em `develop` com `redirect('/gestao/dashboard')`; a implementação corretamente o substituiu, sem impacto funcional, mas o pressuposto do plano era falso; (d) `EXECUCAO-STATUS.md` alterado dentro do PR em commit próprio do Executor (`a88ee5c`), coerente com a prática aceita na Onda 1 apesar de o plano L4 vedá-lo ao Worker; (e) `conferente` e `logistica` ficam com zero itens de menu e caem no aviso explícito — autorizado pelas decisões 11/12/26 e diferido para a Onda 3, sem falha silenciosa. **Merge bloqueado até (1)-(2).** Nota de processo: este commit de veredito avança o head do PR além de `e160321` e junto com o commit doc-only `42374be` do Executor, invalidando o run `30155114508`; a diferença é doc-only, e o Executor deve reexecutar o CI pelo rito de lease PUBLIC→PRIVATE antes de qualquer merge. Nenhum force-push foi feito e o PR **não** foi mergeado por este Monitor. |
| 2026-07-25T08:24:31-03:00 | 2 | 2 | aprovado | PR [#16](https://github.com/sammuka/alpha-carnes/pull/16) head `4154f1b` (== `headRefOid`, OPEN/MERGEABLE), worktree `.worktrees/onda2-impl` após `git fetch --all --prune` + `git pull --ff-only` ("Already up to date"), árvore limpa antes e depois; **CI 10/10 pass** no run `30155901001` (`headSha` `4154f1b`): lint 1m0s, type-check 43s, test-backend 7m11s, coverage 7s, test-frontend 1m28s, build 1m21s, audit 36s, secret-scan 6s, Vercel ×2 — nenhum job de 0 step/~1s (billing); log do job `89674206605` lido: `Coverage total: lines=98.38%, branches=86.09% (min=80%)` + `OK: cobertura total e 0 service(s) tocado(s) dentro do limiar`; `gh repo view --json visibility` = **PRIVATE** ao final (lease PUBLIC→PRIVATE restaurado); comandos=`git fetch/pull/log/show/diff/status/rev-parse` + `gh pr view/checks 16` + `gh run view 30155901001` + `gh run view --job 89674206605 --log` + `gh repo view` + **script `tsx` independente do Monitor** que faz *parse* do `getBreadcrumb` (L222-266) e do `ALL_NAV_GROUPS` (L133-218) direto de `alpha-carnes-prototipo/src/app/components/Layout.tsx` (`feature/completude-v1.1`, `8d32aa4c`) e confronta as 39 rotas contra `resolveBreadcrumb`/`formatMenuGroupTitle`/`MENU_V2` reais do app + **suíte Jest completa reexecutada** (`npm test` em `app/frontend`: 25/25 suítes, 153/153 casos PASS) + **1 mutação de verificação** + `sha256sum` das 5 evidências × `alpha-carnes-prototipo/src/imports/*.png` + **leitura das 4 imagens** (2 de referência + 2 da app) + `rg` de hex/`rgba`/rótulo banido/`setInterval`/`setTimeout`/`@Cron`/`success: true`/segredos no diff `develop...HEAD` + leitura de `breadcrumb-v2.ts`, `admin-header.tsx`, `breadcrumb-v2.test.ts`, `capture-prototipo-ref.mjs`, `README.md` das evidências, `.github/workflows/ci.yml`, `quality-gates.md` §"Onda 2 — Shell + DS" | Recheck do Portão 2 da Onda 2 (segunda instância; veredito anterior `db8f166` = `ajustar` com 2 achados). Pré-condições OK: Portão 1 `aprovado` (`061a2d5`, sha256 `9a9f8b36…`); head do PR == head auditado; CI 100% verde e **real** no head atual; repo PRIVATE ao final. **Os 2 achados FECHARAM, e fecharam por verificação executada, não por relato.** **(1) Breadcrumb — FECHADO e verificado além do pedido.** Não me limitei à rota cobrada: escrevi um script que extrai as 39 duplas `[path, "Grupo / Item"]` do `getBreadcrumb` do protótipo e os 39 `{name, path}` do `ALL_NAV_GROUPS` **direto do arquivo-fonte** e as confronta com o app real — resultado **0 divergências / 39 rotas no breadcrumb e 0 divergências / 39 itens no menu**. Confirmei a premissa do achado no protótipo (`Layout.tsx:253` = `Cadastros & Regras / Fornecedores`, enquanto `:201` = `Fornecedores / Frigoríficos` no menu) e a forma da correção: `BREADCRUMB_ITEM_OVERRIDES` em `breadcrumb-v2.ts:16-19` preserva a derivação única da decisão 7 (o mapa continua gerado de `MENU_V2`, com um único ponto de exceção comentado como "Única divergência menu×breadcrumb — protótipo Layout.tsx getBreadcrumb"), mantém o rótulo longo no menu (a evidência `03-shell-sidebar-9-grupos.png` mostra `CADASTROS & REGRAS` fechado e o `02-shell-dashboard.png` mostra a sidebar completa) e é imune ao *prefix match* (`/cadastros/fornecedores/<id>` também resolve `Fornecedores`, como o `startsWith` do protótipo). **Mutação:** troquei a chave do override para uma rota inexistente e `breadcrumb-v2.test.ts` ficou **vermelho** (`Expected "Fornecedores" / Received "Fornecedores / Frigoríficos"`), provando que o caso afere o comportamento e não o título; revertido, árvore limpa. **(2) Evidências — FECHADO.** Os dois PNGs de referência deixaram de ser cópias de `src/imports`: `sha256` agora `d454bb87…` (login) e `d41f9358…` (shell), **distintos** de `bdffafca…`/`21a0d4c5…` dos falsos e de todos os 19 arquivos de `alpha-carnes-prototipo/src/imports/`. **Abri as duas imagens e elas são o protótipo de verdade**, não a app: `02-shell-prototipo.png` mostra os **9 grupos** na ordem canônica (`COMERCIAL`, `GESTÃO`, `RECEBIMENTO & BALANÇA`, `DESOSSA`, `ESTOQUE`, `CARGA`, `FATURAMENTO`, `CADASTROS & REGRAS`, `ADMINISTRAÇÃO`), `<h1>` **`Painel Geral da Operação`**, breadcrumb `Gestão / Painel Geral da Operação`, os 6 itens de `GESTÃO` e o chip `Escopo: Todos` — o oposto do PNG falso anterior (4 grupos, `Dashboard Operacional`); `01-login-prototipo.png` traz o split 45/55, painel escuro, `Bem-vindo de volta`, CTA `Acessar Sistema`, credencial pré-preenchida, `Esqueci a senha`, `Lembrar minhas credenciais` e o rodapé `Protótipo de alta fidelidade — Design System Aplicado`, tudo ausente na app por autorização da decisão 20. A procedência agora é verdadeira e reprodutível: `README.md` nomeia a origem (repo, branch, dev server `127.0.0.1:5173`, viewport 1280×800, as duas URLs, data da captura) e o script `app/frontend/scripts/capture-prototipo-ref.mjs` materializa exatamente isso (Chromium 1280×800, `goto /login` → screenshot, clique em `Acessar Sistema`, `waitForURL **/gestao/dashboard`, screenshot); o alerta contra contaminar as Ondas 3–10 também entrou (`README.md:43` — "Não usar `src/imports/*.png` — são screenshots antigos da jornada E2E da app, não do protótipo atual"), atendendo à segunda parte do achado. **DoD `quality-gates.md:259` ("screenshot de shell comparado ao protótipo") cumprido:** comparei eu mesmo app × protótipo e o shell confere — sidebar 256px em gradiente com os 9 grupos na mesma ordem e mesmos rótulos (inclusive a elipse de `Pendências de Overbooki…`), `GESTÃO` aberto com os 6 itens, item ativo destacado, breadcrumb `Gestão / Painel Geral da Operação` e `<h1>` `Painel Geral da Operação` idênticos; o login confere com as 5 divergências autorizadas da decisão 20 (o chip `Ambiente: Desenvolvimento` × `Produção` é a variável de ambiente, não divergência de design). **Sem regressão no que já havia passado:** o diff `db8f166..HEAD` são só 6 arquivos (teste novo, override, script de captura, README, 2 PNGs) mais o *empty commit* de lease `4154f1b`; `rg` independente reencontra hex em **um** arquivo (`chart.tsx:58`, 3×`#ccc` + 2×`#fff`, todos dentro de seletores `[stroke='…']` — o inventário pinado da decisão 23), **zero** `rgba(` fora de `globals.css`, **zero** rótulo banido (as 3 ocorrências de "marca" são o verbo em nomes de teste), **zero** `setInterval`/`setTimeout`/`@Cron` e **zero** `success: true` no diff; nenhum endpoint, migration, permissão ou perfil novo (a única mudança de `app/backend/src` segue sendo o `perfil-permissoes.snapshot.json` autorizado); sem segredos (os dois literais são o placeholder `change-me-admin-password`, atrás de `process.env.SEED_ADMIN_PASSWORD ?? ROOT_ENV… ??`, e a senha de fixture `segredo-123` num teste jsdom — `secret-scan` verde). A suíte subiu de 24/24·152 para **25/25·153**, o delta exato do caso novo. **Observações e dívidas registradas (não reprovam, nenhuma nova reprovação):** (a) `BREADCRUMB_ITEM_OVERRIDES` com chave que não exista em `MENU_V2` é **silenciosamente ignorada** — foi o que minha mutação expôs: nenhum erro, só o *fallback* para o rótulo do menu; hoje é inofensivo (uma entrada, coberta por teste), mas tipar a chave como união dos `href` de `MENU_V2` transformaria erro de digitação em erro de compilação; resolver junto de (a) do veredito anterior, na Onda 3; (b) **permanecem abertas, sem regressão, as dívidas (a)–(e) do veredito `db8f166`** — `ICON_MAP[item.iconKey] ?? LayoutDashboard` sem sinal, `--color-sidebar-text` em `rgba(255,255,255,0.92)` contra `0.68` do protótipo (`Layout.tsx:322`), pressuposto falso do plano sobre `app/(admin)/page.tsx`, `EXECUCAO-STATUS.md` alterado dentro do PR pelo Executor, e `conferente`/`logistica` com zero itens de menu (decisões 11/12/26, Onda 3); (c) o corpo do dashboard ainda difere do protótipo (4 KPIs em *loading* × 10 KPIs com dados, subtítulo e seletor de operação) — **fora do escopo da Onda 2**, cuja DoD é shell + DS, e endereçado nas ondas de tela; (d) as capturas `02`/`03` da app trazem o *badge* do indicador de dev do Next.js sobreposto ao rodapé da sidebar — artefato do screenshot, não da UI. **Veredito: `aprovado`.** O Executor pode fazer **squash-merge** de `feature/onda2-shell-ds` → `develop` e atualizar `EXECUCAO-STATUS.md` (status `mergeada` + SHA). Nenhum force-push foi feito e o PR **não** foi mergeado por este Monitor. |
| 2026-07-25T10:31:24-03:00 | 3 | 1 | ajustar | Plano [`2026-07-25-onda3-cadastros-admin.md`](../superpowers/plans/2026-07-25-onda3-cadastros-admin.md), 6593 linhas lidas integralmente, sha256 **confirmado** `2aa62549c3a85565d2bcd0458411043a7f9b5c4718c291583868197cf09390b3` (`sha256sum`); branch `feature/onda3-plano-cadastros-admin` @ `e0d76ef`, worktree `.worktrees/onda3-plan`, PR [#18](https://github.com/sammuka/alpha-carnes/pull/18) **não mergeado**. Comandos/conferências: `rg` de proibidos (`TBD`/`TODO`/`FIXME`/"a definir"/"implementar depois") — zero; `rg` do rótulo banido v1.1 §6.8 — zero (só o verbo "marca"); matriz v1.1 linhas 3–41 recontada à mão = **126** atribuições, idênticas à transcrição `MATRIZ_RASTREABILIDADE` do plano e às 11 contagens por perfil da Task 2 (adm 39, gestor 36, comercial 8, compras 5, conferente 1, corte 3, diretoria 6, expedicao 10, faturamento 8, logistica 3, receb. 7); `perfil-permissoes.snapshot.json` real (adm 55, gestor 48, exped. 13, receb. 20, corte 18) — aritmética da Task 3.4 (61/54/17/21/19) confere; `menu-rbac.test.ts` da Onda 2 — `PERDAS_DECLARADAS` (26) e `EXTRAS_DECLARADOS` (11+3=14) idênticas às tabelas do plano; protótipo `F:\Projetos\alpha-carnes-prototipo` @ `feature/completude-v1.1` — os 12 `.tsx` existem e estão citados, com leitura de `Caminhoes.tsx`, `Motoristas.tsx`, `Parametros.tsx`, `ModelosEtiqueta.tsx`, `Produtos.tsx`; `DECISOES.md` (AD-01..AD-08); plano mestre §7 (P1–P15); `app/backend/src/database/database.module.ts` (`export const DRIZZLE = Symbol('DRIZZLE')`); `ls app/backend/test/integration` (`cadastros-diversos`/`cadastros-f7`/`rbac` existem); todos os 10 códigos de permissão citados existem em `permissoes.ts`. | **AJUSTAR — 14 correções.** Bom: escopo = exatamente linhas 30–41 da matriz, sem escopo de Ondas 4–10; dependência Onda 2 `mergeada` (`18f98bb`); as 3 dívidas da Onda 2 (dec. 25/27/31) têm invariante e teste nomeado (DoD-05..08, DoD-12, DoD-33..35, DoD-65..69); migração expand puro com ROLLBACK; 126 atribuições e aritmética RBAC corretas. **Correções:** 1) **Reabre AD-01** — dec. 25 e Task 2.1 semeiam `operacao.composicao_boi_casado` com `provisorio: true`/`P11` e DoD-31 asserta `provisorio === true`, mas AD-01 determina que o badge Provisório da composição **sai** das telas, Parâmetros inclusive. 2) **Reabre AD-02** — `fiscal.emissao_fiscal` fica `provisorio: true`; AD-02 manda o badge sair e entrar nota "aguardando homologação". 3) **Reabre AD-06** — `fiscal.expiracao_reserva_rascunho` fica provisório "pendente de decisão registrada"; AD-06 já decidiu: sem TTL, liberação explícita e auditada. 4) **Pendências mal atribuídas** — P9 é §16.12 "campos finais da etiqueta" (uso correto na Task 18, errado em `fiscal.emissao_fiscal`) e P15 é "marco exato de fechamento do pedido", não expiração de reserva; corrigir o Global Constraint 6, a dec. 25 e recomputar DoD-61 (hoje fixa "5 provisórios"; honrando AD-01/02/06 sobram 2: P1 e P12). 5) **Task 14 inventa dados (Princípio I + RA-06)** — `Caminhoes.tsx` tem placa, descricao, capacidadeKg, rotaPadrao, status(Ativo/Inativo); a Task 14 cria colunas/campos `modelo`, `tipo` (proprio/agregado), `ano`, `observacoes` e status `manutencao`, que não existem no protótipo, nem no schema da Task 1.4, nem no DTO da Task 4.1 (enum `['ativo','inativo']` + CHECK) — e contradizem a própria seção "Referências do protótipo" ("tabela de 6 colunas"); a coluna real "Rota padrão" (há `rotaPadraoId` no schema) ficou de fora da tela. 6) **Task 15 idem** — `Motoristas.tsx` tem nome, documento, telefone, caminhaoPadrao, status; a Task 15 cria `cpf`, `cnh`, `categoriaCnh`, `validadeCnh` (com regra nova de alerta de vencimento em 30 dias) e `observacoes`, ausentes do protótipo, do schema e do DTO da Task 4.2 (que usa `documento`), contradizendo a linha da própria tabela de referências. 7) **Task 18.2/18.3** — `ModeloEtiqueta.descricao` é lido na tela mas não existe em `modelos_etiqueta` (Task 1.5/1.7) nem nos DTOs (Task 5.1), e o protótipo também não o tem. 8) **Rota de entrada de `comercial` contraditória** — dec. 9 fixa `/comercial/clientes` (doc 013 §2.4) e a Task 11.2 + `ROTAS_ENTRADA_ESPERADAS` + DoD-09 fixam `/comercial/pedidos`; o Worker fica escolhendo. 9) **Código literal que não compila/roda** — `app.get('DRIZZLE')` nas Tasks 8.6 e 9.7 falha (`DRIZZLE` é `Symbol`, não string); `sql\`${frotaCaminhoes.deleted_at} IS NULL\`` na Task 4.4 usa propriedade inexistente (é `deletedAt`). 10) **Nomes/arquivos contraditórios** — dec. 5 e a Architecture dizem `filtrarMenuPorRotas` em `app-sidebar.tsx`, a Task 11.2/11.4 implementam `filtrarMenuPorMenusVisiveis` em `(admin)/layout.tsx`; `components/cadastro-tabela-drawer.tsx` (lista de arquivos) × `components/cadastros/...` (Task 12.1); `api/admin/auditoria/exportar` × `.../export` (Task 24.4 e DoD-68); `api/admin/parametros/[chave]` × `.../parametros/chave/[chave]` (Task 25.5); `e2e/cadastros-admin.spec.ts` × `e2e/onda3-cadastros-admin.spec.ts` (DoD-72/73); `docs/evidencias/onda3-cadastros-admin/README.md` × `docs/execucao/evidencias/onda3/README.md` (Task 27.1). 11) **Arquivos órfãos na estrutura** — `src/lib/menus-canonicos.ts`, `src/lib/parametros.ts`, `__tests__/menus-canonicos.test.ts`, `__tests__/perfis-menus.test.tsx`, `__tests__/parametros.test.tsx`, `__tests__/auditoria.test.tsx` e `test/unit/menus-canonicos.spec.ts` são listados mas nenhuma task os cria e nenhum DoD os referencia; e a lista de 18 rotas BFF do DoD-70 inclui `admin/parametros/[id]/route.ts`, ausente da estrutura. 12) **Tasks por referência (proibido — pipeline §6)** — 4.4 e 5.2 ("mesmo desenho de 4.3"), 4.6 e 5.3 (controllers só em prosa) não trazem código literal; escrever os arquivos por extenso como nas demais tasks. 13) **Rótulos da etiqueta divergem do protótipo sem divergência registrada** — plano usa "Código da peça/parte", "Cliente / Pedido", "Origem (Frigorífico)", "NF / Lote", "Data e Hora", "Código de Barras"; `ModelosEtiqueta.tsx` usa "Cliente/Pedido", "Origem/Frigorífico", "NF/Lote", "Data/hora", "Código de barras". 14) **Lacunas menores** — matriz linha 38 pede "representantes permitidos" no usuário e a dec. 34 não entrega (Princípio II); P1 muda o nome fixado no mestre (`operacao.cadencia_dias_semana`, default [1,3,5]) para `operacao.cadencia_operacoes` com `valor: ''`; DoD-77 não tem teste nomeado (só "gate do CI") e DoD-11 mapeia dois `it` a um nome; dec. 27 concede `MODELOS_ETIQUETA_LER` a `recebimento_pesagem`/`corte` além da matriz linha 37 — justificada, mas registre como divergência autorizada no README de evidências. |
| 2026-07-25T11:18:52-03:00 | 3 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-25-onda3-cadastros-admin.md` (7501 linhas); sha256=`b774c48c833af39a4616411026b8ee128cd745eb01833c2bc4a2a19ef6b47133` (**confere com o pin**); worktree=`.worktrees/onda3-plan`, branch `feature/onda3-plano-cadastros-admin`, HEAD=`6ee7ded3fe160f560f5c1fe9c5e8bd1ea8c826da` (correção `9c97698` + 2 empty-commits de lease de CI `2d5fc2f`/`6ee7ded`; conteúdo do plano intacto), árvore limpa antes e depois; PR [#18](https://github.com/sammuka/alpha-carnes/pull/18) OPEN/MERGEABLE, `headRefOid` == HEAD do worktree, **não mergeado**; run `30160980561` (lint/type-check/build/audit/secret-scan/test-frontend pass, test-backend em curso — irrelevante para o Portão 1, que é docs-only); comandos=`sha256sum` + `git log/rev-parse/status` + `gh pr view/checks 18` + leitura integral do plano + leitura dos schemas reais (`representantes.schema.ts`, `rotas.schema.ts`, `auth.schema.ts`, `database.module.ts`) e de `app/frontend/src/lib/representantes.ts`, `app/frontend/src/app/(admin)/page.tsx` + **script `node` do Monitor** recalculando a cobertura dos 6 prefixos de módulo da Task 9 contra `DESCRICOES_PERMISSOES` real (55 códigos + as 6 novas → 0 códigos sem módulo) + recálculo manual de `filtrarMenuPorMenusVisiveis` × `esperadoDe` para os 11 perfis + protótipo `F:\Projetos\alpha-carnes-prototipo` @ `feature/completude-v1.1`: leitura de `Representantes.tsx` (341 L), `Caminhoes.tsx` (247 L), `Motoristas.tsx` (250 L), `ModelosEtiqueta.tsx` (221 L) + `DECISOES.md` (AD-01..AD-08) + matriz de rastreabilidade + `menus-canonicos`/`permissoes.ts` | **AJUSTAR — 6 correções. 13 dos 14 achados anteriores FECHARAM**, verificados na fonte e não no relato: (1)-(3) AD-01/02/06 honrados — L345/348/349 marcam `operacao.composicao_boi_casado`, `fiscal.emissao_fiscal` e `fiscal.expiracao_reserva_rascunho` como **não** provisórios, com D25.a/b/c explicando cada um (L358-366); (4) DoD-61 (L727) fixa **exatamente 2** badges (`operacao.cadencia_dias_semana` P1 e `operacao.regras_transformacao_tz` P12) e P9 aparece só na tela de etiqueta (L5579); a chave da cadência voltou ao nome do mestre §7 (L371); (8) rota de entrada de `comercial` unificada em `/comercial/clientes` na decisão 9 (L243), na prosa (L257) e em `ROTAS_ENTRADA_ESPERADAS`; (9) `app.get(DRIZZLE)` como Symbol (L3012/L3197) e zero ocorrência de `deleted_at` em código TS; (10) nomes/caminhos únicos — `filtrarMenuPorMenusVisiveis` é o único nome no plano inteiro (declarado em L209), `components/cadastros/cadastro-tabela-drawer.tsx`, `api/admin/auditoria/export`, `api/admin/parametros/chave/[chave]`, `e2e/onda3-cadastros-admin.spec.ts` e `docs/execucao/evidencias/onda3-cadastros-admin/README.md` aparecem numa forma só; (11) DoD-70 = 17 rotas, batendo com a estrutura; (12) Tasks 4.4 (L1694), 4.6 (L1953), 5.2 (L2185), 5.3 (L2338), 5.4 (L2406) e 20.3 (L6001) trazem código literal; (13) os 12 rótulos da etiqueta batem palavra a palavra com `ModelosEtiqueta.tsx` e D18.a cobre o preview sem dados falsos; (14) decisão 43 difere "representantes permitidos" para a Onda 4 com escopo declarado e proibição de campo inerte (L479-494), DoD-77/78 têm teste nomeado e a permissão extra da decisão 27 está registrada. Reconferido sem regressão: 126 atribuições da matriz, aritmética RBAC, os 6 prefixos de módulo da Task 9 cobrem 100% do catálogo, expand puro + ROLLBACK, zero `TBD`/`TODO`/"a definir", zero rótulo banido. **6 ajustes obrigatórios — o achado 5-7 (fidelidade) NÃO fechou; foi corrigido em Caminhões/Motoristas/Etiqueta e reaparece em Representantes e no componente que serve as três telas:** (1) **Task 13 inventa uma entidade inteira (Princípio I + RA-06 + contradição interna).** `Representantes.tsx` tem nome, canal (Interno/Representante), contato, status, observação, clientes vinculados e usuários vinculados — e o próprio plano descreve isso na tabela de referências (L136: "3 filtros (busca, canal, status) + contador à direita; banner informativo azul; tabela de 7 colunas com linhas zebradas; drawer `w-[520px]` com nome, canal, contato, toggle de status, observação, clientes vinculados, usuários vinculados") e no baseline (L167: "`representantes` já tem `tipo_canal`, `contato`, `observacao` e `status`; nada de estrutura falta"). Mas a Task 13 (L4538-4667) entrega colunas Código/Nome/Contato(e-mail + telefone)/Região/Comissão/Status e drawer com `email`, `telefone`, `regiao`, `comissaoPercentual`, `dataAdmissao` e `observacoes`. Verifiquei `representantes.schema.ts`: as colunas são `id, codigo, nome, tipo_canal, contato, status, observacao, created_at, updated_at, deleted_at` — **nenhum** dos seis campos existe, `src/lib/representantes.ts` também não os tem, a migração 0015 não os cria e nenhuma task altera o DTO/serviço de representantes; ainda assim a Task 13.3 afirma "o backend já os devolve", o que é falso. Some-se o nome errado do campo real (`observacao`, não `observacoes`). Contraste que mostra que o critério existe e não foi aplicado aqui: a Task 14 (L4689-4693) escreve "Nenhum outro campo existe no protótipo, no schema da Task 1.4 ou no DTO da Task 4.1; nenhum é criado aqui (Princípio I e RA-06)". Corrigir: reescrever a Task 13 sobre os campos reais do protótipo/schema (nome, canal, contato, status, observação e as duas listas de vínculos) ou, se as listas dependerem de tabela nova, criá-la explicitamente nesta onda ou diferi-la com AD-xx — nunca configurar a tela sobre campos inexistentes. (2) **O componente da Task 12 não reproduz o protótipo, e as três telas delegam tudo a ele (Princípio I + RA-06).** A decisão 35 (L440-443) afirma que "o componente é escrito com as classes exatas do protótipo", mas comparei linha a linha com os três `.tsx`: **(a) filtros** — protótipo tem `select` de status (`Caminhoes.tsx:185-188`, `Motoristas.tsx`) e busca + canal + status em Representantes, mais o contador `ml-auto` ("N caminhões", `:189`); a Task 12 entrega busca + um botão "Filtros" cujo `onClick` é `() => void carregar()` (L4267-4270), isto é, **controle inerte** — exatamente o que o próprio plano proíbe ao justificar a decisão 43 (L494, "nada de controle inerte na tela — RA-06") — e nenhum `select`; **(b) ações** — protótipo usa `Pencil` + `Power`/`PowerOff` para alternar status (`Caminhoes.tsx:220-226`, `Motoristas.tsx:223-229`, `Representantes.tsx:313-319`) e **não tem exclusão**; a Task 12 (L4322-4337) usa `Pencil` + `Trash2` com `window.confirm` e `DELETE`; **(c) largura do drawer** — `Caminhoes.tsx:57` e `Motoristas.tsx:57` são `w-[460px]` (o plano registra isso em L134) e só `Representantes.tsx:92` é `w-[520px]`, mas a decisão 35 e a Task 12 fixam 520 para as três; **(d) zebra e clique na linha** — protótipo tem `i % 2 !== 0 ? "bg-[#FAFAFA]"` e `cursor-pointer onClick={openEdit}` (`Caminhoes.tsx:205-206`) e o plano promete "linhas zebradas" em L136; a Task 12 não tem nenhum dos dois; **(e) contador** — a decisão 41 (L471-473) promete "o contador do protótipo ('N representantes', 'N caminhões')", mas o componente entrega um rodapé "Mostrando N de M" (aferido em L4496) que não existe em nenhum dos três `.tsx`. Nenhuma dessas diferenças está entre as divergências autorizadas do plano (decisões 27, 29, 31 e D18.a). Corrigir: portar filtros, ações, zebra, clique na linha, contador e largura variável para o componente, ou registrar cada divergência como autorizada com justificativa. (3) **Task 18 troca checkbox por Switch, contra o protótipo e contra o próprio plano.** `ModelosEtiqueta.tsx:198` usa `<input type="checkbox">` e a tabela de referências (L143) diz literalmente "12 *checkboxes* em 2 colunas"; DoD-44 (L710) e a Task 18.3 exigem `Switch`, e DoD-47 fala em "os `Switch` ficam desabilitados". O layout também diverge sem registro: protótipo `w-[260px]` / flex-1 / `w-[380px]` (`:165`, `:210`) contra `lg:grid-cols-12` com `col-span-3/5/4`. Corrigir: usar `Checkbox` (já listado entre os componentes de DS reutilizados, L150) e alinhar DoD-44/47, ou registrar a divergência. (4) **DoD-10 falha como escrito — o teste literal da Task 11.7 não passa em 8 dos 11 perfis.** `grupos` vem de `filtrarMenuPorMenusVisiveis(...)` na **ordem canônica** do `MENU_V2` (L3980), mas `esperadoDe` devolve `[...lista].sort()`, isto é, **ordem alfabética** (L3899-3902), e `toEqual` em array é sensível à ordem. `TODOS` (L3858-3861) está em ordem canônica, logo `administrador` e `gestor` também quebram. Exemplos que recalculei: `compras` produz `['GESTÃO','RECEBIMENTO & BALANÇA','CADASTROS & REGRAS']` e espera `['CADASTROS & REGRAS','GESTÃO','RECEBIMENTO & BALANÇA']`; `diretoria` produz `['COMERCIAL','GESTÃO','FATURAMENTO','ADMINISTRAÇÃO']` e espera `['ADMINISTRAÇÃO','COMERCIAL','FATURAMENTO','GESTÃO']`. Só passam os perfis de grupo único (`corte`, `conferente`, `logistica`). A verificação da própria task promete a suíte verde, e o cabeçalho do plano manda o Worker parar e reportar — trava a execução. Corrigir: ordenar os dois lados (`[...grupos].sort()`) ou fixar `GRUPOS_ESPERADOS` na ordem canônica e não ordenar. (5) **DoD-19 não é provado pelo seu teste (mapa DoD→teste 1:1 quebrado) e o teste contradiz a decisão 21.** O invariante é "os 6 modelos de etiqueta do protótipo existem **após o seed**, com os campos da decisão 21" (L675), mas o caso `seed cria os 6 modelos com os campos do prototipo` (Task 5.5, L2438-2470) **não chama `seedModelosEtiqueta`** — ele mesmo cria os 6 por `POST` no `beforeAll` com campos escritos à mão e depois afirma que existem: tautologia sobre dado que o próprio teste inseriu. Pior, os valores divergem do seed (L1343-1360): o teste usa `caracteristicas: false` para os 6, enquanto o seed liga `caracteristicas` em `peca-desossa` e `parte-pedido`, e usa `destino: true` para os 6, enquanto o seed só sobrepõe `destino` em `peca-estoque`. O plano já tem o idioma correto ao lado — o teste de parâmetros faz `await seedParametros(app.get(DRIZZLE).db)` (L3197) e o de menus `seedMenusVisiveis(...)` (L3012). Corrigir: chamar `seedModelosEtiqueta` no `beforeAll` e aferir os campos exatos da decisão 21. (6) **Dois arquivos alterados por tasks estão fora das listas de arquivos, e um deles com instrução ambígua (autossuficiência).** A Task 8.5 (L2939) altera `auth.service.ts`, que não consta do bloco "Backend — arquivos alterados" (L533-566, que lista apenas `rbac.service.ts`), e ainda escreve "em `auth.service.ts` (**ou onde `getMe` monta o payload do usuário**)" — o Worker não pode escolher o arquivo. A Task 11.5 (L3743) altera `app/frontend/src/app/(admin)/page.tsx`, ausente do bloco "Frontend — arquivos alterados" (L617-640); a referência "trocar a linha 9" está correta (conferi o arquivo real) e a assinatura `rotaDeEntrada(menusVisiveis, perfis)` bate com L3709. Corrigir: acrescentar os dois arquivos às listas e nomear o arquivo exato na Task 8.5. **Sem ressalvas — reprovado para ajuste.** PR #18 **não** mergeado e sem force-push; este commit de veredito avança o head do PR (doc-only), então o Executor deve reexecutar o CI pelo rito de lease PUBLIC→PRIVATE antes de qualquer merge. |
| 2026-07-25T12:09:44-03:00 | 3 | 1 | ajustar | plano=`docs/superpowers/plans/2026-07-25-onda3-cadastros-admin.md` (8284 linhas); sha256=`885a94e9fafd2b2b53f2933c649e98b6341d7b0256f718c661df9cc01f53489b` (**confere com o pin**); worktree=`.worktrees/onda3-plan`, branch `feature/onda3-plano-cadastros-admin`, HEAD=`cbcfb2a4fe5806f3ea998670cfdafc30f5a7c09b` (correção `8474744` + empty-commit de lease de CI `cbcfb2a`; conteúdo do plano intacto), árvore limpa; PR [#18](https://github.com/sammuka/alpha-carnes/pull/18) OPEN/MERGEABLE, `headRefOid` == HEAD do worktree, **não mergeado**; comandos=`sha256sum` + `git log/rev-parse/status/show 8474744` + `gh pr view 18` + leitura integral do plano + protótipo `F:\Projetos\alpha-carnes-prototipo` @ `feature/completude-v1.1` (leitura de `Representantes.tsx` 341 L, `Caminhoes.tsx` 247 L, `Motoristas.tsx` 250 L, `ModelosEtiqueta.tsx` 221 L e inspeção dirigida de `RegraDesdobramento.tsx`, `Itinerarios.tsx`, `PerfisAcesso.tsx`, `Parametros.tsx`, `Auditoria.tsx`, `Fornecedores.tsx`) + leitura de `app/frontend/src/lib/menu-v2.ts` (ordem canônica dos 9 grupos) + `grep` dos exports reais de `produtos-client.tsx` e `usuarios-client.tsx` + `DECISOES.md` (AD-01..AD-08) + `EXECUCAO-STATUS.md` (Onda 2 `mergeada`) | **AJUSTAR — os 6 achados do recheck 2 FECHARAM, todos verificados na fonte; 10 correções NOVAS, da mesma classe (Princípio I), nas telas que os ciclos anteriores ainda não haviam aberto.** Fechados: (1) **Task 13** foi reescrita sobre o schema real — L4990-5003 declara os seis campos de `representantes.schema.ts` e proíbe nominalmente `email`/`telefone`/`regiao`/`comissaoPercentual`/`dataAdmissao`/`observacoes`; o cliente (L5335-5358) usa só `codigo, nome, tipoCanal, contato, observacao` + Status do componente; D13.a/b/c e as decisões 44/45 aterram canal livre, "Usuários vinculados" diferido e o campo Código; DoD-82..86 provam filtros reais, contagem vinda de `clientes.representante_id`, as 6 colunas e a ausência dos campos inventados. (2) **Task 12** agora reproduz os três `.tsx`: `select` nativos por filtro sem botão "Filtros" (L4593-4612), contador `ml-auto` `{total} singular/plural` (L4613-4615) sem rodapé "Mostrando N de M", zebra `bg-table-zebra` + `cursor-pointer` + `onClick` com `stopPropagation` na célula de ações (L4665-4686), `Pencil` + `Power`/`PowerOff` por `PATCH { status }` e **zero** `Trash2`/`window.confirm`/`DELETE` (L4688-4713, provado por DoD-80). (3) **Task 18** usa `Checkbox` do DS com `Label` (L6420-6434), 12 campos em `grid-cols-2`, e as três colunas `w-[260px]`/`flex-1`/`w-[380px]` de `ModelosEtiqueta.tsx:165,190,210`; DoD-44/47 falam em `Checkbox`, não em `Switch`. (4) **DoD-10** passa como escrito: `esperadoDe` não ordena mais (L4116-4120), `GRUPOS_ESPERADOS` está na ordem canônica e o próprio caso guarda a invariante com `expect(esperado).toEqual(TODOS.filter(...))` (L4199); recalculei os 11 perfis contra `menu-v2.ts` (COMERCIAL, GESTÃO, RECEBIMENTO & BALANÇA, DESOSSA, ESTOQUE, CARGA, FATURAMENTO, CADASTROS & REGRAS, ADMINISTRAÇÃO) e as 9 listas são crescentes nessa ordem. (5) **DoD-19** virou prova do seed: o `beforeAll` importa e chama `seedModelosEtiqueta(app.get(DRIZZLE).db)` (L2617-2618) e o `ESPERADO` é transcrição independente; conferi os 6 modelos campo a campo contra `ModelosEtiqueta.tsx:33-69` — batem, inclusive `caracteristicas` em `peca-desossa`/`parte-pedido` e `destino` em `peca-estoque`. (6) `auth.service.ts` (L655) e `(admin)/page.tsx` (L743) entraram nas listas e a Task 8.5.2 pina o arquivo e o método `montarMe` (L3141-3149), sem alternativa para o Worker. **Largura do drawer: a escolha `larguraDrawer: 460 \| 520` é ACEITA** — conferi `Caminhoes.tsx:57` e `Motoristas.tsx:57` (`w-[460px]`) e `Representantes.tsx:92` (`w-[520px]`); fixar 460 nas três afastaria Representantes do protótipo, e a decisão 35 (L471-475) mais a DoD-81 registram e testam os dois valores. **10 ajustes obrigatórios:** (1) **Cabeçalho de `/admin/perfis` e `/admin/parametros` não é o do protótipo (Princípio I).** `PerfisAcesso.tsx:140-142` e `Parametros.tsx:152-154` têm trilha `Administração / …` em `text-[11px]`, `h1` em `text-[20px]` e subtítulo em `text-[12px]` com texto próprio ("Matriz de permissões por perfil e menus visíveis para cada perfil.", "Regras gerais de negócio, agrupadas por Comercial, Operação e Fiscal."); as Tasks 22 (L7237-7241) e 23 (L7478-7482) escrevem `h1 className="text-2xl font-bold text-foreground"` **sem trilha** e com subtítulo reescrito em `text-sm`. Contradiz a decisão 29 ("a estrutura visual é idêntica ao protótipo") e o cabeçalho que a decisão 35 fixa para o resto da onda (L458). Corrigir ou registrar divergência. (2) **Task 20 troca rótulos literais do protótipo e contradiz o próprio plano.** Aba 2: o plano diz "Transformação na Desossa" (L6647, L6656, decisão 37 em L482-485 e referências em L142); `RegraDesdobramento.tsx:541` diz **"Transformação de Desossa (TZ)"**. Simulador da desossa: o código da Task 20.3 escreve `<h3>Simulador</h3>` (L6852) enquanto o protótipo (`:424`) e o próprio plano (decisão 37 L486-488, referências L142) dizem **"Simulador de Disponibilidade"**. Campos: plano "Produto a reservar" (L6868) e "Quantidade" (L6884) contra `:440` **"Reservar produto"** e `:453` **"Quantidade a reservar"**. (3) **Task 16 descreve o protótipo errado.** L5765-5766 afirma que `Fornecedores.tsx` tem abas "Dados Cadastrais", "Parâmetros Operacionais" e "Histórico"; o arquivo **não tem `Tabs`** — tem quatro seções com ícone em duas colunas: "Dados Principais" (`:136`), "Endereço e Contato" (`:156`), "Parâmetros Operacionais" (`:183`) e "Histórico & Ocorrências" (`:203`) — exatamente como a tabela de referências do próprio plano registra em L138. Contradição interna sobre a fonte de verdade da tela. (4) **Task 17 (paradas) diverge do protótipo sem registro.** L5981-5983 manda `GripVertical` e o título "Paradas / Bairros"; `Itinerarios.tsx:6,95` usa **`MoveVertical`** e `:91` traz **"Sequência de Paradas / Bairros"**. Os botões subir/descer e o `Trash2` por linha (L6040-6053) também não existem no protótipo — são necessários para editar, mas precisam de divergência numerada, como D18.b fez com "Salvar Modelo". (5) **Valores iniciais fabricados nos simuladores (RA-06 + Princípio I).** `useState('100')` em L6697 (quantidade a comprar) e L6802 (TZ livre); o protótipo usa `useState(10)` em `RegraDesdobramento.tsx:65` e `:323` e `useState(3)` em `:325`. Pior, o teste da DoD-50 (L6938) amarra a expectativa ao `100 × 2 = 200` fabricado, petrificando o desvio. (6) **Dois testes do plano não compilam como escritos (autossuficiência).** L6595-6596 manda `import ProdutosClient from '@/app/(admin)/cadastros/produtos/produtos-client'` (default), mas o arquivo real exporta nomeado (`produtos-client.tsx:131`, `export function ProdutosClient`); L7064-7065 manda `import UsuariosClient from '@/app/(admin)/admin/usuarios/usuarios-client'`, e o arquivo real exporta `UsuariosAdminClient` (`usuarios-client.tsx:14`) — nome **e** forma errados. O Worker não pode escolher: pinar `import { ProdutosClient }` e `import { UsuariosAdminClient }`, ou incluir o rename como substep. (7) **Task 21.3 — o drawer de `/admin/usuarios` só existe em prosa.** L7045-7059 descreve os campos numa tabela e as rotas em texto, sem uma linha de JSX, embora seja a única forma de criar/editar usuário nesta onda e a decisão 34 exija o padrão de drawer de `Representantes.tsx` (`w-[520px]`) — largura que o texto não fixa. Todas as telas irmãs (Tasks 12, 13, 17, 18, 22, 23, 24) trazem o componente inteiro. (8) **Task 25.3 usa o padrão proibido "repetir a task anterior".** L7926: "Repetir 25.1 e 25.2, trocando apenas o caminho do backend" para `/api/cadastros/frota-motoristas` e `/api/cadastros/modelos-etiqueta` — o rito (`pipeline-execucao.md` §6) exige código literal por task, e o Portão 1 reprova "similar à Task X". (9) **`/admin/auditoria` — rótulo do `select` de usuário.** L7622 usa `placeholder="Todos"` e `<SelectItem value="todos">Todos</SelectItem>`; `Auditoria.tsx:44` traz **"Todos os usuários"** (o "Todos" seco é do `select` de módulo, `:51`). A decisão 30 promete os filtros "exatamente os do protótipo". (10) **Duas inconsistências menores de referência.** L594 atribui `--color-warning-surface` ao "banner P11" de `ModelosEtiqueta.tsx:158`, mas o banner é da pendência **P9** (constraint 6, L109-110; Task 18 L6360 já diz "Banner P9"). E o preview da Task 18.3 escreve "ALPHA CARNES" (L6467) enquanto `ModelosEtiqueta.tsx:91` escreve **"ALFA CARNES"**; se a correção é intencional, entra em D18.a. Reconferido sem regressão nesta rodada: 27 tasks, 46 decisões, 86 invariantes com teste nomeado, 126 atribuições da matriz, expand puro (0015) com ROLLBACK, AD-01/02/04/06 honradas, 2 badges Provisório (P1 e P12) mais P9 na etiqueta, zero `TBD`/`TODO`/"a definir", zero rótulo banido pela v1.1 §6.8, dependência Onda 2 `mergeada`. **Sem ressalvas — reprovado para ajuste.** PR #18 **não** mergeado e sem force-push; este commit de veredito avança o head do PR (doc-only), então o Executor deve reexecutar o CI pelo rito de lease PUBLIC→PRIVATE antes de qualquer merge. |
