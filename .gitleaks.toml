[extend]
useDefault = true

[allowlist]
description = "Placeholder literal conhecido; cache do graphify contém apenas hashes SHA-256 de conteúdo (sem segredos)"
stopwords = ['''change-me''']
paths = [
  '''graphify-out/cache/.*''',
]
